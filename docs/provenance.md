# Provenance

somfy-rs is an independent project. Its source code stands on its own: comments
explain what the code does and why, in this project's own terms, without
pointing at another codebase.

That independence must not cost **auditability**. Many of this firmware's
constants and algorithms are not free choices — they are dictated by the Somfy
RTS protocol and by the behaviour of motors already paired in the field. Where a
value was *derived* rather than invented, this document records where it came
from and when it was checked, so a reader can verify it without the code
advertising its ancestry.

**This file is the only place the reference implementation is cited.**

## The reference

- **ESPSomfy-RTS** (C++ / Arduino), released into the public domain (Unlicense).
- Verified against locally at `/home/dhia/Sources/personal/ESPSomfy-RTS`,
  firmware version **v2.5.6**.
- somfy-rs is independently licensed **GPL-3.0-only**. The Unlicense imposes no
  attribution requirement; this document exists for engineering auditability,
  not for licence compliance.

## Rules

1. **Deriving is required; citing in code is not.** Never invent protocol
   behaviour. Read the reference, verify the value, then record it here — not in
   a source comment.
2. **Preserve the reasoning, drop the pointer.** When removing a citation, the
   *knowledge* it carried must survive in the comment, restated in this
   project's own terms. A comment that asserts a constraint with its
   justification deleted is worse than either extreme.
3. **Inline references are permitted only as a documented exception.** If a
   comment cannot convey its meaning without naming the reference — typically
   where the point *is* a deliberate divergence — keep it, and add a row to
   [Inline exceptions](#inline-exceptions) saying why. An undocumented inline
   reference is a defect.
4. **Ground truth beats derivation.** Where a value has since been confirmed
   against real hardware (captured pulses, a real device backup), say so. That
   evidence outranks the reference implementation, which is itself only a
   reading of the protocol.

## Format

Each crate gets a section. One row per derived value or algorithm:

| Item | Where it lives now | Derived from | Verified |
|---|---|---|---|
| `TIMINGS::HALF_SYMBOL` = 640 µs | `somfy-rts/src/pulse.rs` | reference transmitter's `SYMBOL` define | 2026-08-15 — confirmed against real wall-remote capture |

`Derived from` names the reference construct in prose (function or field name),
not a line number: line numbers rot against a codebase we do not control, and a
prose name is what a reader actually searches for.

---

## somfy-rts

| Item | Where it lives now | Derived from | Verified |
|---|---|---|---|
| `TIMINGS::WAKEUP_HIGH` = 10920 µs, `TIMINGS::WAKEUP_LOW` = 7357 µs | `somfy-rts/src/pulse.rs` | reference transmitter's wake-up delay constants | Hardware-verified (see "Hardware-verified values" below) |
| `TIMINGS::HW_SYNC_HALF` = 2560 µs, `TIMINGS::SW_SYNC_HIGH` = 4850 µs | `somfy-rts/src/pulse.rs` | reference transmitter's sync-pulse timing constants | Hardware-verified against captured wall-remote frames (see "Hardware-verified values") |
| `TIMINGS::HALF_SYMBOL` = 640 µs, `TIMINGS::INTER_FRAME_GAP` = 27434 µs | `somfy-rts/src/pulse.rs` | reference transmitter's bit-timing and inter-frame-gap constants | Hardware-verified against captured wall-remote frames; also exercised end-to-end by the golden hardware-capture suite (`somfy-rts/tests/golden.rs`) |
| Sync-pulse counts per frame kind (56-bit: 2 hardware syncs on the first frame, 7 on a repeat; 80-bit: 12 on the first frame, 6 on a repeat) and gap suppression on 80-bit frames | `somfy-rts/src/pulse.rs::render_pulses` | reference transmitter's frame-preamble routine | 56-bit counts hardware-verified (see "Hardware-verified values"); 80-bit counts verified only by round-trip decode tests (`somfy-rts/tests/frame80.rs`) — no 80-bit hardware capture available yet |
| RX sync-acquisition threshold (4 hardware-sync half-pulses minimum) and the ±25% timing tolerance window | `somfy-rts/src/rx.rs::MIN_HW_SYNCS`, `somfy-rts/src/rx.rs::within` | a simplification of the reference receiver's timing-tolerance bounds | Verified by the tolerance-boundary tests in `somfy-rts/tests/rx_loopback.rs` (a +24% stretched half-pulse still decodes, +26% aborts) |
| Bit-length detection from the accumulated hardware-sync count | `somfy-rts/src/rx.rs::RxDecoder::detect_bit_length` | the reference receiver's frame-length disambiguation logic | Verified by `somfy-rts/tests/frame80.rs::rx_decoder_recognizes_80_bit_frames` and the 56-bit loopback tests in `somfy-rts/tests/rx_loopback.rs` |
| 56-bit frame byte layout, forward-XOR obfuscation, and nibble-XOR checksum | `somfy-rts/src/frame.rs` (`encode56`, `decode56`, `checksum`, `obfuscate`, `deobfuscate`) | the reference transmitter's 56-bit frame-encoding routine | Verified by round-trip tests in `somfy-rts/tests/frame56.rs` and by the golden hardware-capture suite (`somfy-rts/tests/golden.rs`) |
| 80-bit frame byte layout and its two independent checksums (nibble-XOR over bytes 0-6, tail parity over bytes 7-9) | `somfy-rts/src/frame.rs` (`encode80`, `decode80`, `calc80_checksum`) | the reference transmitter's extended-command frame-encoding routine | Verified by round-trip tests in `somfy-rts/tests/frame80.rs` |
| Extended-command tail byte values per command, and the byte-7 repeat progression (`start + 4*repeat`, cycling by −15 once the sum would exceed 255) | `somfy-rts/src/frame.rs` (`encode80_tail`, `encode80_byte7`) | the reference transmitter's extended-command tail-encoding routine | Verified by `somfy-rts/tests/frame80.rs` (`byte7_progresses_by_four_per_repeat_and_wraps_at_15`, `favorite_and_stop_flip_byte7_on_later_repeats`, `base_command_tails_match_cpp`) |
| Command discriminant values, including the three extended-command byte values (StepUp = 0x8B, Favorite = 0xC1, Stop = 0xF1) | `somfy-rts/src/command.rs::Command` | the reference firmware's command enumeration | Verified by `somfy-rts/tests/frame56.rs::command_nibble_mapping_matches_cpp_enum` and the 80-bit round-trip tests in `somfy-rts/tests/frame80.rs` |
| `MEASURED_MAX_INTRA_FRAME_SEGMENT_US` = 17738 µs — the longest segment of *either level* occurring *inside* a real transmission | `somfy-rts/src/pulse.rs` | **Not derived at all.** Measured directly from this repository's own wall-remote captures. It exists precisely because the transmit-side constant that looks like it should answer the same question (`TIMINGS::WAKEUP_LOW`, 7357 µs) is wrong by a factor of 2.4 against real hardware | Hardware-measured: the post-wake-up gap reads 17738 µs in `somfy-rts/tests/fixtures/up_56bit_1.pulses`, 17722 in `down_56bit_1.pulses` and 17711 in `my_56bit_1.pulses`. Re-derived from those three files on every test run by `somfy-rts/tests/measured.rs`, which also asserts each capture contains exactly one such gap and that it is the wake-up gap. **Level-agnostic deliberately:** the hardware rule it feeds ends a reception when no *edge* arrives, so a long HIGH counts exactly as a long LOW does. The maximum happens to be a LOW today — the longest HIGH is the ~10.2 ms wake-up pulse — but a LOW-only bound would leave the wake-up pulse outside anything checking it — so the constant cannot drift from its evidence, and a re-capture forces a re-derivation. **Bounds the intra-frame case only:** the capture ISR stops recording when a frame completes, so no committed capture contains a real remote's inter-frame silence |
| Key-byte derivation (`0xA0 \| (rolling_code & 0x0F)`) and the rolling-code storage convention (this crate stores the NEXT-TO-SEND code; some deployed implementations store the LAST-SENT code instead) | `somfy-rts/src/rolling.rs::RollingCode` | real remotes' wire convention for the key byte, and the "persist before TX" invariant | Wire convention verified by round-trip frame tests; the storage-convention distinction is a design decision recorded in `docs/specs/2026-07-15-rust-rewrite-design.md` §4, not independently hardware-verified |

## somfy-domain

| Item | Where it lives now | Derived from | Verified |
|---|---|---|---|
| `Command::Stop` is never planned by the domain; `My` is the only stop frame a 56-bit motor accepts, and non-basic commands are downgraded to `My` on the wire | `somfy-domain/src/shade.rs` (module docs, `Shade::handle`) | the reference firmware's shade command-dispatch routine, which downgrades non-basic commands to `My` on 56-bit motors | Verified by `somfy-domain/tests/shade.rs::stop_is_never_emitted_only_my` |
| Mid-range arrival stop: a seek that lands on an intermediate position needs an explicit `My` frame because the motor only self-stops at its hard limits (fully open/closed); hard-limit and Step-originated seeks never schedule one | `somfy-domain/src/shade.rs` (`Shade::tick`, `stop_on_arrival`) | the reference firmware's movement-tracking routine, guarded by its position-seek-in-progress flag | Verified by `somfy-domain/tests/shade.rs::goto_midrange_emits_stop_on_arrival` and `goto_full_limit_does_not_emit_stop`, and by `somfy-domain/tests/overheard.rs::overheard_arrival_at_midrange_does_not_plan_stop` |
| Step size default (100 ms of travel per Step press) and the step-target formula `FULL_RAW * STEP_TRAVEL_MS / travel_ms`, direction-matched to the travel time of the direction being stepped — yielding a 1% nudge at the default 10 s travel time | `somfy-domain/src/shade.rs::STEP_TRAVEL_MS`, `Shade::step_target` | the reference firmware's default per-motor step size and its non-tilt Step target-math routine | Verified by `somfy-domain/tests/shade.rs::step_commands_nudge_target_and_emit_extended_commands` and `step_up_nudges_toward_open_and_clamps_at_zero`; the direction-matched guard/divide (rather than mismatching the zero-travel guard to the other direction's time) was cross-checked by re-reading the reference's Step branches during the port and is exercised by the same tests |
| Overheard-frame handling: `Up`/`Down` retarget the hard limits, `My` while moving freezes the estimate, `My` while idle recalls the favorite, and `Step` nudges the estimate one step — all without transmitting, since the wall remote already drove the motor; a remote frame also abandons any of our own in-flight positioning | `somfy-domain/src/shade.rs::Shade::apply_overheard` | the reference firmware's frame-processing routine for frames from a non-internal source | Verified by `somfy-domain/tests/overheard.rs::overheard_down_moves_estimate_without_retransmit`, `overheard_my_while_moving_halts_estimate`, `overheard_my_while_idle_tracks_favorite`, `overheard_step_down_moves_estimate_by_one_step_without_tx`, `overheard_step_up_moves_estimate_up_without_tx` |
| Frame ownership check order: a shade's own remote address is checked before its linked-remote list | `somfy-domain/src/shade.rs::Shade::is_linked` | the reference firmware's frame-ownership test | Verified by `somfy-domain/tests/overheard.rs::is_linked_covers_own_and_linked_addresses` |
| Linked-remote registry bound of 7, and rejection of the sentinel addresses (0 / 0xFFFFFF) as invalid remotes | `somfy-domain/src/shade.rs::Shade::linked`, `Shade::link_remote` | the reference firmware's fixed-size linked-remotes array and its address-validity guard | Verified by `somfy-domain/tests/overheard.rs::link_remote_enforces_limit_and_duplicates` |
| DEVIATION: on real hardware the default behaviour for `My` pressed idle with no software-tracked favorite is to send a raw `My` frame, letting the motor recall a favorite stored in its own hardware; this crate always simulates positions in software instead, so idle-`My`-without-favorite is a no-op rather than a hardware recall | `somfy-domain/src/shade.rs::Shade::handle` (the `ShadeCommand::My` arm) | the reference firmware's default (simulate-My-off) behaviour, contrasted with this crate's always-simulate design | Verified by `somfy-domain/tests/shade.rs::my_while_idle_without_favorite_is_noop`; reproducing the hardware-recall path is deferred to Plan 4 |
| `Pos` stores position as a fixed-point `u16` in hundredths of a percent rather than a float | `somfy-domain/src/types.rs::Pos` | deployed controllers' floating-point 0.0-100.0 position model, replaced with an integer representation for deterministic, reproducible math | Design decision documented in the crate docs; exercised by the full `somfy-domain` test suite (204 passed) |
| `ShadeKind` discriminants (`Roller`=0x00, `Blind`=0x01, `DraperyLeft`=0x02, `Awning`=0x03, `Shutter`=0x04, `DraperyRight`=0x07, `DraperyCenter`=0x08) and `TiltMode` discriminants (`None`=0x00, `TiltMotor`=0x01, `Integrated`=0x02, `TiltOnly`=0x03, `EuroMode`=0x04) | `somfy-domain/src/types.rs::ShadeKind`, `somfy-domain/src/types.rs::TiltMode` | the shade-type and tilt-type byte values used in deployed device backups | Verified by parsing a real v25 device backup with zero field misalignment (see "Hardware-verified values") |
| Unsupported-on-import shade kinds: garage (0x05/0x06), drycontact (0x09/0x0A), gate (0x0B-0x10) fall back to `Roller` with a warning surfaced to the user | `somfy-domain/src/types.rs::ShadeKind::from_raw` | the fuller set of shade kinds deployed devices can contain, of which v1.0 models only a subset | Plan 6 policy decision recorded in the crate's README contracts |
| Radio-protocol discriminant `0x00` is the one this firmware speaks, so a shade importing with any other value is flagged as undrivable rather than provisioned silently | `somfy-config/examples/provision_shades/import.rs::TRANSMITTED_PROTOCOL` | **Not read off a protocol table.** Derived from the value the backup reader substitutes when a shade record carries no protocol field at all (`somfy-migrate/src/records.rs`, the `v > 6` gate, which defaults it to `0`): a file with no protocol field describes shades driven the ordinary way, so the ordinary way is what `0x00` spells. No claim is made about what the other discriminants mean, because nothing needs it — only that a shade set to one of them was deliberately set away from the default and `ShadeConfig` has no field to honour that | Verified against a real v25 device backup: every shade in it imports with neither the protocol nor the frame-width caveat raised (`import_tests.rs::a_real_backup_imports_to_the_shape_the_parser_reports`, `#[ignore]`d — the fixture is private). A mixed installation would fail that assertion, which is the intent |
| Frame width 56 bits is the one this firmware transmits, so a shade importing at any other width is flagged as undrivable | `somfy-config/examples/provision_shades/import.rs::TRANSMITTED_BIT_LENGTH` | `somfy_tasks::TxProfile::default`, which is per-controller because `ShadeConfig` has no per-shade width field — a limitation that profile's own docs record | Verified by the same real-backup test above |
| Movement direction sign convention: -1 toward open, +1 toward closed, 0 idle | `somfy-domain/src/types.rs::Direction`, `somfy-domain/src/motion.rs::Motion::direction` | the position-tracking sign convention deployed firmware uses | Verified by the `somfy-domain` motion and shade test suites |
| `TiltMode` trap: only `TiltMode::None` has command semantics in Plan 2; `TiltOnly`/`EuroMode` need long-press redirection of Up/Down onto the tilt axis, and `TiltMotor`/`EuroMode` need a half-second hold to disambiguate a tilt press from a lift press | `somfy-domain/src/types.rs::ShadeConfig::tilt_mode` | the reference firmware's per-tilt-mode input handling, which this crate has not yet ported | Not yet ported or tested; tracked as a Plan 3/4 gap so the API must not advertise tilt as functional until it lands |
| `ShadeConfig` factory defaults: 10s up, 10s down, 7s tilt | `somfy-domain/src/types.rs::ShadeConfig::new` | the factory defaults deployed devices ship with | Verified by parsing a real v25 device backup with zero field misalignment (see "Hardware-verified values") |
| Address plausibility guard: 0 and 0xFFFFFF are rejected as invalid sentinel addresses | `somfy-domain/src/types.rs::ShadeConfig::new` | the "unset" sentinel addresses used by deployed devices | Verified by the `somfy-domain` type test suite |
| Open-loop dead-reckoning position estimator: direction is recomputed from position vs. target every tick; while moving, position is `start_offset + elapsed` as a ratio of the direction's travel time, integrated from the open end (down) or the closed end (up) | `somfy-domain/src/motion.rs::Motion::tick` | the reference firmware's movement-tracking routine, required because RTS is a one-way protocol and the motor never reports position back | Verified by the `somfy-domain` motion test suite |
| Zero-travel-time guard: a direction with no configured travel time jumps instantly to the target instead of dividing by zero | `somfy-domain/src/motion.rs::Motion::tick` | the reference firmware's zero-travel-time handling | Verified by the `somfy-domain` motion test suite |
| Up-branch integer-division floor placement (computing `consumed` before subtracting, rather than an algebraically equivalent rearrangement) | `somfy-domain/src/motion.rs::Motion::tick` | the reference firmware's specific floor placement in its up-branch integration math, kept faithfully rather than "simplified" because the two orderings round differently | Verified by the `somfy-domain` motion test suite. `docs/specs/2026-08-15-position-accuracy-requirements.md` documents that this estimator faithfully reproduces the reference's dead-reckoning inaccuracy by design, though it does not call out this specific rounding placement |
| Snap-to-target-on-crossing rule: a tick that computes past the target reports the target position exactly, with `arrived = true`, instead of the overshot value | `somfy-domain/src/motion.rs::Motion::tick` | the reference firmware's arrival-detection logic | Verified by the `somfy-domain` motion test suite |
| Integrated-tilt sequencing gate: with `TiltMode::Integrated`, the lift axis cannot move up until tilt is fully open, and cannot move down until tilt is fully closed | `somfy-domain/src/tilt.rs::tilt_first` | the reference firmware's integrated-tilt-motor sequencing rule | Verified by the `somfy-domain` tilt test suite |
| Registry fixed capacity: 32 shades, 16 groups, 16 rooms, and at most 32 shades per group | `somfy-domain/src/registry.rs::MAX_SHADES`, `MAX_GROUPS`, `MAX_ROOMS` | the capacity bounds deployed configurations can contain | Verified by parsing a real v25 device backup with zero field misalignment (see "Hardware-verified values") |
| Registry ids are stable slot indices that survive removal of other entries, with holes reused by the next add before growing | `somfy-domain/src/registry.rs::Registry` | the reference firmware's fixed-array addressing contract, needed because ids are stored elsewhere (group/room membership) and must not silently repoint | Verified by the `somfy-domain` registry test suite |
| Overheard `My`-while-idle recall is immediate: the domain does not replicate a physical remote's ~500 ms tap-vs-hold disambiguation window, since it receives an already-decoded command rather than raw button timing | `somfy-domain/src/lib.rs` (module docs); behavior lives in `somfy-domain/src/shade.rs::Shade::apply_overheard` | the reference firmware's My-button hold-to-set timing logic, which distinguishes a tap (recall) from a hold (set new favorite) | Not independently verified; the resulting immediate-recall behavior is exercised by `somfy-domain/tests/overheard.rs::overheard_my_while_idle_tracks_favorite`, but the absence of a timing window is a design difference, not something a unit test can positively prove |
| Step command (`StepUp`/`StepDown`) transmits its frame unconditionally, even when the position is already at the hard limit and cannot move further | `somfy-domain/src/shade.rs::Shade::handle` (the Step arms) | the reference firmware's unconditional step-frame transmission | Verified by `somfy-domain/tests/shade.rs::step_up_nudges_toward_open_and_clamps_at_zero` (asserts `Command::StepUp` is still transmitted from position ZERO) |
| Pairing is `Command::Prog` transmitted at the shade's **own** remote address while the motor is in programming mode; the controller cannot put the motor into that mode itself | `somfy-domain/src/shade.rs::Shade::handle` (the `Pair` arm), `somfy-domain/src/pairing.rs` | the reference firmware's pairing routine, which transmits `Prog` at the shade's address | **Verified on hardware 2026-08-17 by the owner** — a shade added through the running firmware took an allocated address (bit 23 set, from the `0x88B2FF` base this board derives), was paired to a real motor, and then moved on command, reaching position 98. That is the whole chain — allocate, pair, command — and it is the first time this controller has driven a motor as **its own** virtual remote rather than as the identity imported from the previous one. Host-verified as far as a host can: `somfy-domain/tests/pairing.rs::pair_plans_one_prog_frame_at_the_shades_own_address` |
| A `Prog` frame must not move the position estimate, in either direction: neither when this controller plans one nor when one is overheard | `somfy-domain/src/shade.rs::Shade::handle` (the `Pair` arm), `Shade::apply_overheard` (the catch-all arm) | **Not derived.** The reference's estimator is driven by its movement routine, which pairing does not enter; this states the same outcome as an invariant rather than as a consequence of routing | Verified by `somfy-domain/tests/pairing.rs::pairing_moves_neither_the_position_nor_the_target` and `an_overheard_prog_frame_moves_nothing` |
| A pairing event **disarms a pending mid-range arrival stop**, whether this controller initiated it or overheard it | `somfy-domain/src/shade.rs::Shade::handle` (the `Pair` arm), `Shade::apply_overheard` | **Not derived — found in review of this change, and a correction to what its own first draft claimed.** In programming mode `My` is not a stop: it is how a favourite position is stored. A mid-range seek's arrival `My` is planned by `Shade::tick` on a clock the pairing command has no say over, so without the clear it fires seconds after the `Prog`, inside the window a person has just opened at the shade, and silently rewrites a setting only a visit can undo. Dropping it instead leaves the shade running to its physical limit and the estimate wrong until the next move reaches one — recoverable by any later command. The trade is one-sided | Verified by `somfy-domain/tests/pairing.rs::pairing_disarms_a_pending_arrival_stop` and `an_overheard_prog_frame_plans_no_stop_into_a_programming_window`. **Open question for the reference:** whether a PROG press aborts an in-progress movement on a real motor. If it does, the abandoned seek is also physically over and the estimate is merely stale; if it does not, the motor runs on. Neither changes which frame it is safe to transmit |
| Moving a provisioned board to the A/B partition layout is a **rename, not a migration**: it keeps its rolling codes, its credentials and its shade table | `crates/firmware/partitions.csv`, `crates/firmware/build.rs` (fails the build if a later edit moves any data region), `docs/hardware-checklist.md` → Partition table | Not derived — forced. `rollcode` is pinned at 0x200000 and an app partition must start on a 64 KB MMU page from 0x10000, so `ota_0` = 0x1F0000 is the arithmetic ceiling rather than a choice, and it happens to equal what `factory` already was. The app slot therefore does not move or change size, and the image lands on the sectors it already occupied | **Verified on hardware 2026-08-17**, spare ESP32-S3, first flash of the new table onto a board carrying real codes. Bootloader listed all eight entries with `rollcode`/`wificfg`/`shades` at their old offsets; `store: survey slots=32 valid=16 blank=16 damaged=0 newest_seq=Some(15) addresses=4` matched the pre-flash state, and each shade **kept its stored rolling code over the provisioned starting value** (196 vs 190, 401 vs 401, 122 vs 120) — which is the specific evidence that flash was read, not reinitialised. `App/part. size: 586,480/2,031,616` confirms espflash sized the app against the new table. An 8 KB backup of `rollcode` was taken first and kept outside the repository |
| `PAIR_REPEATS` = 2 (a **3-frame** burst) is a tap, and is far below the burst length that erases a remote | `somfy-domain/src/pairing.rs::PAIR_REPEATS` | **Derived from the reference, 2026-08-17, closing an open question that had been left as the author's judgement.** Frame-count semantics are identical on both sides — total frames = repeats + 1 (`somfy-tasks/src/radio.rs:395` `for repeat in 0..=request.repeats`; reference `Somfy.cpp:4010` sends one frame then loops `repeat` more). The reference has **no pairing-specific constant**: pairing goes through its generic command path at the shade's configured `repeats`, default **1** → 2 frames. Unpairing is the only place it hardcodes a count — **7 for 56-bit** → 8 frames — so on 56-bit shades **the repeat count is the whole difference between pairing and erasing a remote**. That is what makes `Repeats::Exactly` load-bearing rather than tidy: a generous transmit profile reaching this path would not be redundancy, it would be an unpairing | Reference call sites read directly: `Web.cpp:1757` (`Prog, 7`, 56-bit, in the handler that sets `paired = false`), `Web.cpp:1759` (`Prog, 1`, 80-bit), `Somfy.h:239` (`repeats = 1` default). **Empirically:** this project's ordinary commands are also 3-frame bursts (`DEFAULT_REPEATS` = 2) and have been transmitted at real motors repeatedly through Plan 4a/4b without ever unpairing one — so 3 frames is a tap by observation, not only by arithmetic |
| `ShadeCommand::Pair` is refused for a **group**, before anything is planned | `somfy-domain/src/controller.rs::Controller::command_group`, `somfy-domain/src/types.rs::DomainError::NotAGroupCommand` | **Not derived — found in review.** Every other command is a movement somebody can watch and undo. Pairing works only while a person standing at one motor has just put it into programming mode and has no inverse a later command applies, so a fan-out is a `Prog` burst at every shade in the house with nobody at any of them | Verified by `somfy-domain/tests/pairing.rs::a_group_cannot_be_told_to_pair`, with `one_shade_can_still_be_paired_through_the_controller` as the other half |
| `PAIR_REPEATS` = 2 repeat frames after the first, and the fact that a **long** `Prog` burst removes a remote from a motor rather than adding one — the reference sends `Prog` with 7 repeats on 56-bit frames to remove a remote | `somfy-domain/src/pairing.rs::PAIR_REPEATS` | the reference firmware's remote-removal routine, for the 7-repeat figure and therefore for the fact that burst length carries meaning. **The value 2 is not the reference's pairing figure** — it is this project's own default burst (`somfy_tasks::DEFAULT_REPEATS`), chosen because a pairing press is an ordinary short press | **Settled, and then verified.** The reference's count was read on 2026-08-17 (see the row above: pairing runs at the shade default of 1, unpairing hardcodes 7), and the same day the owner **paired a real motor on hardware with this 3-frame burst** — it added a remote and did not remove one, which is the only test that could distinguish the two outcomes. The domain still pins it with `Repeats::Exactly` rather than letting a configured profile supply it, because the margin between pairing and erasing is a repeat count — `somfy-tasks/tests/state.rs::a_pairing_burst_ignores_a_generous_profile` |
| Linked-remote bound of **7 per shade** | `somfy-domain/src/shade.rs::MAX_LINKED_REMOTES` | the reference firmware's fixed-size linked-remotes array, whose bound is 7 | Adopted unchanged. What the record can hold is a *shared* pool of 58 across the whole table rather than 7 x 32, because 7 x 32 does not fit a 2048-byte slot — see the `somfy-config` section |
| `FrameWidth` discriminants (56, 80) and `RadioProtocol` discriminants (`Rts`=0x00, `Rtw`=0x01, `Rtv`=0x02, `GpRelay`=0x08, `GpRemote`=0x09) | `somfy-domain/src/types.rs::FrameWidth`, `RadioProtocol` | the bit-length and radio-protocol byte values deployed device backups store, and the reference's `radio_proto` enumeration | Verified against the backup parser, which reads both fields and whose field map is pinned by `somfy-migrate`'s tests. **Carried, not honoured on the wire**: the transmit width is still per-controller (`somfy_tasks::TxProfile`) and only `Rts` is implemented, so the firmware reports a shade it cannot drive rather than driving it |
| DIVERGENCE: there is no `paired` flag | `somfy-domain`, `somfy-config` (absent by design) | **Deliberately not adopted.** The reference has `SomfyShade::paired`, persisted and exposed on the wire — but it is set from the HTTP request body (`Web.cpp:1719`, `:1760`) and cleared in the constructor, never observed. RTS is one-way, so no controller can learn whether a motor accepted a `Prog`. A stored `paired` is therefore a user's belief recorded as a fact | The honest gate is address origin, which *is* observable: `RemoteIdentity::is_allocated`, verified by `somfy-domain/tests/pairing.rs::every_address_this_allocator_produces_reports_as_allocated` and `no_address_the_other_scheme_can_produce_reports_as_allocated`. **Refined, not reversed, by `PairingState` below** — that field records a *person's report*, never the device's knowledge, which is the distinction this row is about |
| `PairingState::{AwaitingConfirmation, ConfirmedByOperator}`, and that a shade has **no Home Assistant entities** until it is confirmed | `somfy-domain/src/types.rs::PairingState`, `somfy-domain/src/shade.rs::Shade::confirm_pairing`, `crates/firmware/src/tasks.rs::announce_shade`, `crates/firmware/src/inventory.rs::Inventory::snapshot` | **Not derived — and the reference is the trap here rather than the model.** See [Why there is a pairing state after all](#why-there-is-a-pairing-state-after-all) below | Host-verified: `somfy-domain/tests/pairing.rs` (`a_freshly_allocated_shade_is_awaiting_confirmation`, `transmitting_a_pairing_burst_confirms_nothing`, `confirming_plans_no_frame_and_moves_no_position`, `reconfiguring_carries_neither_direction_of_the_report`), `somfy-config/src/catalog.rs` tests, `somfy-config/tests/shade_v2.rs`. **What only a flash can confirm** is listed in `docs/hardware-checklist.md` → Pairing a shade |
| An allocated address never moves: the allocation and the placement are both inside the branch where the registry slot was empty | `somfy-domain/src/pairing.rs::allocate_if_absent` | **Not derived — the same shape as `somfy_store::seed_if_absent`, for the same class of loss.** A motor obeys the address it was paired at and nothing in a one-way protocol can tell it otherwise, so a reallocation is a shade that stops responding and a walk to the motor to fix. The reference's `getNextRemoteAddress` is called afresh whenever a shade is created and has no such structure; the architecture (base + shadeId, probe upward on a clash) is its | Verified by `somfy-domain/tests/pairing.rs::an_allocated_address_is_never_reallocated`, `a_second_shade_gets_its_own_address_and_leaves_the_first_alone`, `a_refused_name_allocates_nothing` |
| The allocator's `taken` predicate covers **linked remotes** as well as shades | `somfy-domain/src/pairing.rs::allocate_if_absent` | the reference's `getNextRemoteAddress`, which checks shades *and* groups — the same principle, applied to the other thing that transmits at an address this controller must not claim | Verified by `somfy-domain/tests/pairing.rs::an_allocation_steps_over_a_linked_remotes_address`. A wall remote is a physical object that cannot be renumbered; the allocation can |
| Per-shade address allocation from a controller-wide base, `base + shadeId`, with an upward probe past any address the table already holds | `somfy-domain/src/pairing.rs::RemoteIdentity::address_for` | the reference firmware's next-remote-address allocator, including its increment-on-collision loop | Verified by `somfy-domain/tests/pairing.rs` (`distinct_shades_of_one_board_get_distinct_addresses`, `an_address_already_in_the_table_is_stepped_over`, `a_run_of_taken_addresses_is_walked_past`) |
| **DELIBERATE DIVERGENCE — the base address is derived from the device-unique MAC bytes, not the vendor OUI** | `somfy-domain/src/pairing.rs::RemoteIdentity::from_mac` | **Deliberately not derived.** See [Why the address derivation was not copied](#why-the-address-derivation-was-not-copied) below | Measured on two boards; see the same section |

### Why the address derivation was not copied

The architecture above **is** the reference's: one base address per controller,
`base + shadeId` per shade, and an upward probe when the result is already
taken. The derivation of the base is not, and this is the reasoning, recorded
here because rule 1 of this document says a divergence must be written down
rather than left to be rediscovered.

The reference computes its base as the low twenty bits of the eFuse MAC word.
That word is little-endian, so its low twenty bits are the low three MAC bytes
in reverse — which are the **vendor OUI**. The OUI identifies the chip maker and
is byte-for-byte identical on every board they ship.

Measured on two boards from one bench on 2026-08-17, differing only in the
device-unique half of the MAC: the derivation produced the **same** base for
both, and that base matched the one a deployed controller of the same make was
already using. The MAC values are not reproduced here — this repository holds no
real MAC or radio address (see [Rules](#rules) and the note in
`somfy-domain/tests/pairing.rs`) — but the property is asserted directly in
`somfy-domain/tests/pairing.rs::the_oui_derivation_gives_two_different_boards_the_same_start`,
which reimplements the OUI derivation for that single purpose.

So every controller using that derivation allocates the same addresses, and any
two of them in radio range are one remote with two independent rolling-code
counters. A motor rejects any code at or below the last it accepted, so whichever
falls behind stops being obeyed — the failure that prompted this work, observed
live.

This project derives from `mac[3..6]` instead, folding all twenty-four of those
bits into twenty so the top nibble of `mac[3]` cannot be silently dropped, and
sets bit 23 on every address. The fold is still sixteen-to-one — twenty-four
bits of identity cannot be injective into a twenty-four-bit address that must
also carry a marker bit, a shade offset and a collision probe — so two arbitrary
boards share a base about one time in a million. **That is a coincidence, not
this defect**, and the difference is the whole point: an OUI derivation collides
for every pair of boards of one make, with certainty. The bit is what makes the separation structural
rather than probabilistic: an OUI-derived base is a twenty-bit value plus at most
a `u8` shade id, so nothing that scheme can produce reaches `0x80_0000`.
`somfy-domain/tests/pairing.rs::no_allocated_address_is_reachable_by_the_oui_derivation`
checks that for every shade id and a spread of MACs rather than for the one board
that prompted it.

### Why there is a pairing state after all

An earlier ruling in this project refused a `paired: bool` outright, on the
grounds that RTS is one-way and the device cannot observe pairing. **That
reasoning stands and nothing here weakens it.** What it does not establish is
that there is no state at all, and this section is the distinction.

The reference implementation has `SomfyShade::paired`. It is persisted, exposed
on the wire, and **set from an HTTP request body** — `Somfy.h:305` declares it,
`Web.cpp:1719` assigns it from `obj["paired"]` — and nothing ever observes it.
That is a user's belief rendered as a device fact, and it is the trap rather
than the model.

The field here is a different claim, and the names carry the difference:

- It is never set by the device from its own transmission. `Shade::handle`'s
  `Pair` arm plans a `Prog` frame and leaves the state alone, which
  `transmitting_a_pairing_burst_confirms_nothing` asserts directly.
- It is set by exactly one route,
  `POST /api/v1/shades/{id}/confirm-pairing`, which the UI reaches only after
  the operator has commanded the shade and answered "yes, it moved".
- It is spelled `ConfirmedByOperator`, not `Paired`, so a reader of the field
  cannot mistake whose knowledge it is.
  `somfy-api/tests/ts_export.rs::pairing_state_says_whose_knowledge_it_is`
  refuses the words `paired` and `boolean` in the generated wire type.

**What it buys is the thing the boolean could not.** The failure being closed is
not "the flag was wrong"; it is that a shade could be created, announced to Home
Assistant, and left unpaired — a cover entity that accepts Open and Close,
transmits a perfectly formed frame, and is ignored by every motor in the house.
That is the same shape as the discovery defect
`docs/specs/2026-08-15-mqtt-ha-discovery-requirements.md` was written after.
Gating the announcement on a human's report is what makes the half-finished
state unobservable from Home Assistant rather than indistinguishable from a
finished one.

**Why the report is a functional test and not the jog.** The pairing procedure's
only acknowledgement has always been the motor jogging, and the jog is kept — the
send step says to watch for it. It is not what commits, for two reasons. A jog
is a small movement that is easy to miss, so missing it is indistinguishable
from it not happening. More importantly a jog proves only that *a frame
arrived*: it does not prove the shade can be driven, which is a different
question with its own failure modes — a rolling code at or below what the motor
has already accepted being the sharpest, since it produces a shade that paired
perfectly and then ignores everything. Pressing Open and watching the shade open
tests the whole path, and it is the path the user will actually use.
`docs/hardware-checklist.md`'s own sequence already ended there; this moves it
inside the flow instead of leaving it as advice after the end.

**The migration is the same error-direction argument as the announced bitmap.**
A shade record written before this field existed (versions 1 and 2) has a zero
in the byte the field now occupies, and zero is `AwaitingConfirmation` — which
would un-announce every shade on a board that is working today. So the *version*
decides, not the byte, and an older record's shades decode as
`ConfirmedByOperator`. `somfy-config/tests/shade_v2.rs` asserts that against a
byte-for-byte capture of what the previous build's encoder emitted, including
the assertion that the capture really does carry a zero there — otherwise the
test would prove nothing.

## somfy-migrate

**This crate is a documented exception: it names the reference implementation
freely, in code, and that is correct.**

Every other crate here implements the Somfy RTS protocol, which is a property of
the motors — the reference implementation was a source of knowledge about it, not
the subject. `somfy-migrate` is different in kind. Its entire purpose is to read a
backup file **produced by** the C++ ESPSomfy-RTS firmware, so an existing
installation can move to somfy-rs without re-pairing motors at the wall.

There, the other codebase *is* the subject matter. A comment saying which routine
wrote a given field is describing the input this parser must accept, not confessing
an implementation history. Stripping those names would make the crate harder to
maintain and would obscure the one thing a reader most needs to know: what produced
the bytes.

The rest of this document therefore does not attempt to catalogue this crate's
derivations — the code itself is the record, and it is allowed to be explicit.

Two things still hold here:

- **Verification outranks derivation.** The parser was validated against a real
  backup exported from a live device (format version 25), which parsed with zero
  field misalignment and no skipped resyncs. That evidence is stronger than any
  reading of the writer's source.
- **Nothing else may follow this crate's convention.** If a future crate wants to
  name the reference, it needs its own entry in
  [Inline exceptions](#inline-exceptions), justified on its own terms.

## somfy-api

| Item | Where it lives now | Derived from | Verified |
|---|---|---|---|
| DTO wire shape (camelCase field names, whole-percent `u8` positions rather than floats) | `somfy-api/src/entities.rs::ShadeDto`, `GroupDto`, `RoomDto`; `somfy-api/src/events.rs::ShadeStateEvent` | the reference firmware's REST/WS payload shape, kept for backup/migration parity with deployed devices | Verified by `somfy-api/tests/entities.rs::shade_dto_serializes_to_stable_json` (camelCase keys, whole-percent values) and `shade_dto_roundtrips`; the TypeScript shape is cross-checked by `somfy-api/tests/ts_export.rs::entities_use_camelcase_and_heapless_overrides` |
| `kind`/`tiltMode` reuse the numeric discriminants deployed devices already emit, rather than a string union | `somfy-api/src/entities.rs::ShadeDto` | the reference firmware's numeric `kind`/`tiltMode` wire fields (same discriminants recorded in the `somfy-domain` table above) | Verified by `somfy-api/tests/entities.rs::shade_dto_serializes_to_stable_json` and `somfy-api/tests/ts_export.rs::entities_use_camelcase_and_heapless_overrides` (asserts `kind`/`tiltMode` stay `number`, not a string union) |
| `direction` reuses the same sign convention on the wire (-1 up, 0 idle, +1 down) | `somfy-api/src/entities.rs::ShadeDto`, `somfy-api/src/events.rs::ShadeStateEvent` | the reference firmware's direction sign convention on the wire (same convention recorded for `somfy_domain::Direction` above) | Verified by `somfy-api/tests/entities.rs::shade_dto_snapshots_live_state` / `shade_dto_serializes_to_stable_json` |

## somfy-cc1101

Two different kinds of source meet in this crate, and the distinction matters.

The **target radio parameters** — what frequency, what modulation, what
bandwidth — came from the known-working reference configuration. They are a
statement about what the motors in the field are listening to, so they are
derived knowledge in exactly the sense the rules above describe, and they are
recorded here rather than in the source.

The **register byte values** were then worked out independently from the CC1101
datasheet formulas, and the arithmetic sits in the source next to each constant.
The datasheet is a primary vendor document for a part this project uses, not a
borrowed codebase, so naming it in a comment is not a reference-implementation
citation and Rule 1 does not apply to it. `docs/provenance.md` remains the only
place the *reference firmware* is named.

Where the two disagree, the disagreement is recorded rather than smoothed over.
Several values in this crate are **not derived at all**, and say so in their own
doc comments as well as here.

A third kind of source enters with the AGC registers: TI's own design note
**DN022, "CC110x/CC111x OOK/ASK Register Settings" (SWRA215E)**. The datasheet
declines to answer the ASK/OOK gain question itself — it states that the
settings its design tool produces for FSK/MSK "are not optimum" for OOK/ASK and
points at DN022 — and DN022 answers with measured recommendations, not formulas.
It is a primary vendor document for this part, like the datasheet, so naming it
in a source comment is not a reference-implementation citation. Its
recommendations are recorded below as recommendations, and where this project's
own measurements required departing from them, the measurement is recorded as
the reason.

A fourth kind of source enters with those same registers, and its provenance
chain is worth spelling out because it runs *through* the reference. The
reference firmware **does not configure the AGC itself**: it drives the radio
through a third-party Arduino driver library,
**`ELECHOUSE_CC1101_SRC_DRV` (lsatan/SmartRC-CC1101-Driver-Lib), v2.5.7**, and
takes that library's defaults. So the values are not the reference's design —
they are a widely deployed library's defaults, which the reference inherits and
which are field-proven for OOK remote work on this exact silicon. They are
adopted here as what they are: **empirical, externally validated, not derived**,
and then re-measured against a real transmitter before being committed. That is
the same category `PATABLE` already occupies. Source comments name the library,
which is not the reference implementation; this file records the chain.

| Item | Where it lives now | Derived from | Verified |
|---|---|---|---|
| Target radio parameters: 433.42 MHz carrier, deviation 47.6 kHz, "rxBandwidth" 99.97 kHz, TX power 11, ASK/OOK, asynchronous serial, CRC off, sync mode 4, address check off | Realised as register values throughout `somfy-cc1101/src/config.rs` | the reference firmware's transceiver configuration block | Hardware-verified: read back off a live production ESP32-S3 unit currently controlling three shades (see "Hardware-verified values") |
| `FREQ2`/`FREQ1`/`FREQ0` = 0x10/0xAB/0x85 | `somfy-cc1101/src/config.rs::FREQ` | Derived, not borrowed: datasheet `f_carrier = (f_xosc / 2^16) * FREQ` at 26 MHz, rounded to nearest | Arithmetic asserted at compile time (`ACHIEVED_CARRIER_HZ == 433_419_952`, 0.11 ppm low) and pinned byte-for-byte by `init_writes_the_ook_async_serial_register_set` |
| `DEVIATN` = 0x47 (DEVIATION_E = 4, DEVIATION_M = 7) | `somfy-cc1101/src/config.rs::DEVIATN` | Derived: datasheet `f_dev = (f_xosc / 2^17) * (8 + M) * 2^E`; E = 4, M = 7 is the only pair inside the field widths | Compile-time assertion `ACHIEVED_DEVIATION_HZ == 47_607` (7.4 Hz over the 47.60 kHz target). Inert in ASK/OOK — the datasheet states the setting has no effect in this modulation — and written only so the radio's state is wholly deliberate |
| `MDMCFG4` channel-bandwidth half = CHANBW_E 3, CHANBW_M 0 → **101.5625 kHz, not the 99.97 kHz requested** | `somfy-cc1101/src/config.rs::CHANBW_E` | Derived: datasheet `BW = f_xosc / (8 * (4 + M) * 2^E)`. The two 2-bit fields admit exactly 16 bandwidths at 26 MHz and 99.97 kHz is not one of them | Compile-time assertion `ACHIEVED_CHANBW_HZ == 101_562`. **Deliberate divergence:** the requested figure is unreachable on this part; 101.5625 kHz is 1.6% high, the next setting down (81.25 kHz) is 18.7% low. Not yet confirmed on air |
| `MDMCFG4` data-rate half + `MDMCFG3` = DRATE_E 5, DRATE_M 248 → 1562.1 baud | `somfy-cc1101/src/config.rs::DRATE_E`, `DRATE_M` | **Not from the reference at all** — chosen by this project. The reference configuration specifies no data rate, and one must be picked because the field shares a register with the bandwidth. Derived from this repo's own hardware-verified 640 µs RTS half-symbol (1562.5 chips/s) | Compile-time assertion that the achieved rate is within 0.1% of one chip per half-symbol. Unused in asynchronous-serial TX, where the modulator follows the pin; it only shapes the receive path, so it is untested on air |
| `MDMCFG2` = 0x34 (MOD_FORMAT 3 = ASK/OOK, SYNC_MODE 4, Manchester off) | `somfy-cc1101/src/config.rs::MDMCFG2` | Derived: field values read straight off the datasheet's `MDMCFG2` bit table for the requested modulation and sync mode | Compile-time assertion on the composed byte; pinned by `init_writes_the_ook_async_serial_register_set` |
| `PKTCTRL0` = 0x32 (async serial, CRC off, infinite length), `PKTCTRL1` = 0x00 (address check off), `ADDR` = 0x00 | `somfy-cc1101/src/config.rs` | Derived from the datasheet bit tables. Note both `WHITE_DATA` and `CRC_EN` reset to 1, so these writes actively disable them rather than confirming a default | Compile-time assertion on `PKTCTRL0`; all three pinned by the register-set test |
| `FREND0` = 0x11 (PA_POWER = 1) | `somfy-cc1101/src/config.rs::FREND0` | Derived: the datasheet requires OOK to take its logic-0 level from `PATABLE[0]` and its logic-1 level from `PATABLE[PA_POWER]`, so PA_POWER must be non-zero. The byte 0x11 itself appears nowhere in the datasheet; it is composed from the two field values | Compile-time assertion on the composed byte |
| `PATABLE` = [0x00, 0xC0] — **not derived** | `somfy-cc1101/src/config.rs::PATABLE_OOK` | A datasheet **table lookup**, not a formula: the PA register is a group of bias and ramp fields with no published relation to output power. 0xC0 is the tabulated setting for +10 dBm at 433 MHz | **Known gap.** The reference configuration asks for TX power 11 and the datasheet's only 433 MHz table has no +11 dBm row — it runs −30, −20, −15, −10, 0, +5, +7, +10 and stops. 0xC0 is that table's maximum. A +11 dBm row with the same byte 0xC0 exists in a different table, for 868/915 MHz parts with wire-wound inductors, so 0xC0 is the answer under either reading — but the exact provenance of the number 11 is unresolved. Not yet confirmed against measured output power |
| `MCSM0` = 0x14 (FS_AUTOCAL = 1) | `somfy-cc1101/src/config.rs::MCSM0` | **Added by this project; the reference configuration does not mention it.** The datasheet makes the `STX` strobe conditional on this field — from IDLE it "performs calibration first if MCSM0.FS_AUTOCAL = 1" — and the field resets to 0, "never". Without it a driver that parks in IDLE between frames transmits on an uncalibrated VCO while every register read still reports a healthy chip | Compile-time assertion on the composed byte. The failure it prevents has not been reproduced on hardware; it is a reading of the datasheet's strobe table, not an observed fault |
| Post-reset settle delay of 1 ms, taken with chip select still asserted — **not derived** | `somfy-cc1101/src/config.rs::RESET_SETTLE_NS` | Nothing: the datasheet bounds `SRES` completion only by a handshake (the chip drives MISO low when ready) and publishes no time. That handshake is unobservable through `embedded_hal::spi::SpiDevice`, which owns the chip-select line | **Margin choice, not a measurement.** Over an order of magnitude beyond the longest oscillator-settling figure the datasheet quotes anywhere (~600 µs), and paid once per boot. Not validated on hardware |
| `AGCCTRL2` = 0xC7 (MAX_DVGA_GAIN 3, MAX_LNA_GAIN 0, MAGN_TARGET 7) — **not derived; a field-proven library default, re-measured here** | `somfy-cc1101/src/config.rs::MAX_DVGA_GAIN`, `MAX_LNA_GAIN`, `MAGN_TARGET` | **Not the reference firmware's own value — it configures no AGC at all** and inherits this byte from the `ELECHOUSE_CC1101_SRC_DRV` library described above. DN022 separately recommends `AGCCTRL2` = 0x03 to 0x07, which varies only `MAGN_TARGET` and leaves `MAX_DVGA_GAIN` at 0; that range is unusable, and the reason is arithmetic rather than opinion: 0x03 **is the datasheet reset value**, so the whole recommended range describes the behaviour that made the receiver slice its own noise floor. The library's byte sits at the top of DN022's `MAGN_TARGET` range and outside it on `MAX_DVGA_GAIN` | **Hardware-measured against a real transmitter** (see "Hardware-verified values"). **Supersedes 0x83 (MAX_DVGA_GAIN 2, MAGN_TARGET 3), which this project chose on its own sweep and which was wrong.** That sweep ranked settings by edges per second in an *empty* band and picked the smallest cap that silenced it; silence in an empty band turns out not to predict whether a frame survives. Re-ranked against confirmed transmissions with the signal walked out of the channel filter to stand in for distance, 0x83/0x91 decoded **0 of 6** complete frames where 0xC7/0xB2 decoded **12 of 12**. `MAX_LNA_GAIN` still 0, both because gain removed there costs noise figure directly and because `CS_THRESHOLD_DBM_TENTHS` is read along that row. **Two costs recorded, neither measured:** the datasheet warns that a high `MAGN_TARGET` "reduces the headroom for blockers, and therefore close-in selectivity" — untested, no blocker set up — and the carrier-sense table puts the DVGA cap 18 dB up, a figure that demonstrably did *not* predict reception and is retained as indicative only |
| `AGCCTRL1` = 0x00 — **not derived** | `somfy-cc1101/src/config.rs::AGCCTRL1` | A **DN022 recommendation taken verbatim**, with no derivation offered by either document. It differs from the reset value 0x40 in one bit, `AGC_LNA_PRIORITY`, for which the datasheet presents two strategies and no rule for choosing. The two carrier-sense threshold fields are left at their reset zeros | Made **no measurable difference** to the noise on hardware: the swept measurements ran with this register at 0x00 throughout and the reset baseline they are compared against had 0x40, everything else identical. Followed because it is the vendor's answer for this modulation, not because anything here demonstrates it |
| `AGCCTRL0` = 0xB2 (HYST_LEVEL 2, WAIT_TIME 3, AGC_FREEZE 0, 12 dB OOK decision boundary) — **not derived; the same field-proven library default** | `somfy-cc1101/src/config.rs::FILTER_LENGTH_OOK_12DB`, `WAIT_TIME_32_SAMPLES`, `HYST_LEVEL_MEDIUM` | DN022 offers "0x91 **or** 0x92" for the boundary field and does not choose; the library takes the wider one and additionally slows `WAIT_TIME` from the reset 1 (16 channel-filter samples) to 3 (32). `FILTER_LENGTH` is overloaded by modulation — an averaging length under FSK/MSK, the OOK/ASK decision boundary under this one | **Supersedes 0x91, and the reason the old value was chosen is now understood rather than discarded.** The earlier note recorded 12 dB as a trap: GDO2 stopped producing edges but sat **high 99–100% of the time**, a receiver stuck asserting a carrier that is not there and indistinguishable from success to an edge count alone. That measurement was real and was taken at `MAX_DVGA_GAIN` = 2. The two fields are coupled — a wider boundary sits lower relative to the peak, so it pins the line high only if the gain in front of it is too high for the band. Re-measured: 12 dB at `MAX_DVGA_GAIN` 2 gives **54–101 edges/s idle, 22–39‰ high**; the same boundary at `MAX_DVGA_GAIN` 3 gives **0 edges/s, 0‰ high**. `WAIT_TIME` = 3 is adopted as part of a proven set, **not on isolated evidence** — no measurement here separates it from the two threshold changes it arrived with. Boundary-to-dB mapping asserted at compile time as `OOK_DECISION_BOUNDARY_DB` |
| Accepted `VERSION` values {0x04, 0x14} | `somfy-cc1101/src/config.rs::KNOWN_VERSIONS` | Derived: 0x14 is the current datasheet reset value; 0x04 is what the datasheet published before revision I, for older silicon | Exercised by `init_accepts_every_known_silicon_version` and `init_rejects_an_unexpected_version`. **Trade-off recorded:** the datasheet annotates `VERSION` "subject to change without notice", so genuine future silicon could be rejected. An allowlist was chosen over a "not 0x00 or 0xFF" denylist because a miswired bus returning a plausible byte would otherwise pass as a working radio |

## somfy-rmt

| Item | Where it lives now | Derived from | Verified |
|---|---|---|---|
| RMT RX idle threshold `IDLE_THRESHOLD_US` = 22000 µs — how long the air must stay quiet before the peripheral calls a transmission finished | `somfy-rmt/src/lib.rs::IDLE_THRESHOLD_US` | **Not derived; chosen by this project**, and chosen against measured captures rather than against `TIMINGS`. Its floor is `somfy_rts::MEASURED_MAX_INTRA_FRAME_SEGMENT_US` (17738 µs, measured) and its ceiling is `TIMINGS::INTER_FRAME_GAP` (27434 µs). **Corrects the design spec**, which specified `WAKEUP_LOW < idle_threshold < INTER_FRAME_GAP` and chose 12000 µs — an inequality the committed captures contradict, and a value that would end the reception one pulse into every real first frame | Floor hardware-measured (see the `somfy-rts` row above). Both margins asserted at compile time. Behaviour host-tested by `somfy-rmt/tests/idle_threshold.rs`, which replays the three real captures through a model of the peripheral's split rule and pins that 22000 leaves each capture whole while 12000 splits every one of them. **Ceiling not verified:** 27434 µs is *our* transmitter's gap, and no committed fixture contains a real remote's repeat frame to confirm a remote uses the same. **Not confirmed on air** |
| `MAX_IDLE_THRESHOLD_TICKS` = 32767 — the narrowest RMT idle-threshold field across the supported chips | `somfy-rmt/src/lib.rs::MAX_IDLE_THRESHOLD_TICKS` | Vendor register widths, not the reference implementation: the field is 16 bits on the ESP32 and **15 bits on the ESP32-S3 and ESP32-C3**. A different register from the 15-bit *duration* field `MAX_TICKS` describes | Asserted at compile time in `crates/firmware/src/radio/rmt_rx.rs` against `esp_hal::rmt::MAX_RX_IDLE_THRESHOLD`, esp-hal's own per-chip constant, on all three chip builds. Note the design spec's claim of a 65535 µs ceiling holds only on the ESP32, one of the three |
| Reading a received symbol buffer back out as pulses: two entries per 32-bit symbol, and a zero-length entry ends the stream | `somfy-rmt/src/lib.rs::pulse_at`, `unpack`, `BurstCursor` | Not derived — the inverse of this crate's own `pack`, against the peripheral's documented end-marker convention. Note the terminator rule is **per entry**, not esp-hal's per-code `PulseCode::is_end_marker`; the two are not interchangeable, and using the latter would drop the last pulse of every odd-length burst | Host-tested by `somfy-rmt/tests/unpack.rs` (round trip of every frame shape through `build_symbols` and back) and `somfy-rmt/tests/cursor.rs`. `BurstCursor` holds the receive path's whole walk-a-burst state so the firmware keeps no index arithmetic of its own — the one place a receiver can silently hand back a stale pulse, and the reason it does not live in a crate no host test can reach. Not exercised against a real reception |
| Receive budget: a worst-case reception needs **95** symbols (188 recorded edges + the peripheral's terminator, two entries per symbol) — the same figure as the transmit worst case, but by different arithmetic | `somfy-rmt/src/lib.rs::MAX_SYMBOLS` (documented there; asserted in `crates/firmware/src/radio/rmt_rx.rs`) | Not derived. Computed from this project's own pulse trains | Pinned on the host by `somfy-rmt/tests/unpack.rs::a_worst_case_reception_fits_max_symbols` for all four frame shapes. **One symbol of slack** against `MAX_SYMBOLS` on the ESP32-S3 and ESP32-C3, where two RMT blocks is exactly 96 — a bound, not a margin. Whether the peripheral records anything beyond one entry per edge plus a terminator is **not measured** |

## somfy-mqtt

Two sources meet here and they pull in opposite directions, so the distinction
carries weight.

The **state topic layout** is reference-derived: the reference's MQTT
*publishing* works, and its topic shapes are what any existing broker
subscription or dashboard on a migrated estate is written against. The **Home
Assistant discovery contract** is not reference-derived at all — it comes from
Home Assistant, and it was verified against a live instance precisely *because*
the reference gets it wrong in every configuration. Copying the reference there
would reproduce the bug this crate exists to remove.

| Item | Where it lives now | Derived from | Verified |
|---|---|---|---|
| Per-shade state topic layout: `{state_root}/shades/{id}` as the base, with `position`, `direction`, `direction/set`, `target/set` and `name` below it | `somfy-mqtt/src/entity.rs::ShadeTopic::segments`, `somfy-mqtt/src/config.rs::SHADES_SEGMENT` | The reference firmware's own published topic layout, read off a live broker rather than out of its source: `espsomfyrts/shades/2/position :: 69`, `espsomfyrts/shades/3/name :: Roller shade br`, and the topic set named by its discovery payload (`position_topic`, `state_topic`, `command_topic`, `set_position_topic`). Adopted because the reference's *publishing* is the half that works, and because Plan 5 gains nothing by inventing a different arrangement of the same topics | Captured against a real broker on 2026-08-15; the capture is quoted in `docs/specs/2026-08-15-mqtt-ha-discovery-requirements.md`. Pinned on the host by `somfy-mqtt/tests/topics.rs::shade_topics_are_exact`. **Note this is topic *shape* only, not compatibility** — the plan puts C++ topic-layout compatibility explicitly out of scope, and the root segment differs |
| Availability at `{state_root}/status`, publishing `online`/`offline` | `somfy-mqtt/src/config.rs::STATUS_SEGMENT`, `MqttConfig::availability_topic` | The reference firmware's `espsomfyrts/status :: online`, with one deliberate departure: it must live under the **state root**, never under the discovery prefix. `{discovery_prefix}/status` is Home Assistant's own birth and will topic, so availability published there is overwritten by HA's birth message and reports the device available while it is offline | Pinned by `somfy-mqtt/tests/topics.rs::availability_is_under_the_state_root_not_the_discovery_prefix` and, from the payload side, by `somfy-mqtt/tests/round_trip.rs::the_check_actually_catches_availability_under_the_discovery_prefix`. The same collision is also reachable when the two roots are set to the same string — both individually valid — so `MqttConfig::new` refuses overlapping namespaces; pinned by `somfy-mqtt/tests/config_rejection.rs::overlapping_namespaces_are_rejected`. **This rule is not in the requirements spec**; it was found while implementing R4 and is reported as a spec gap |
| Discovery topic `{discovery_prefix}/{component}/{node_id}/{object_id}/config`, with the component **immediately** after the prefix | `somfy-mqtt/src/config.rs::MqttConfig::discovery_topic` | **Not the reference.** Home Assistant's MQTT discovery contract, which the reference does not implement correctly in any configuration | Verified empirically on 2026-08-15 by publishing both orderings to a live broker and observing which one Home Assistant acted on: `homeassistant/<device>/cover/1/config` was ignored, `homeassistant/cover/<device>/1/config` created an entity that read live position. Recorded in the requirements spec above; pinned by `somfy-mqtt/tests/topics.rs::component_is_the_segment_immediately_after_the_prefix` |
| `position_open: 0` / `position_closed: 100` in the cover discovery payload | `somfy-mqtt/src/entity.rs::CoverDiscovery::render` | **Not the reference.** Home Assistant's cover defaults are the other way round (100 open, 0 closed), while this project's `Pos` runs 0 fully open to 100 fully closed — see the `somfy-domain` table above. Stating both ends explicitly is what stops every shade reporting itself inverted | Not yet confirmed against a live Home Assistant. **This is the one value in this crate whose correctness a host test cannot establish**, and it is on the Plan 5 Task 5 integration checklist |
| `object_id` = `shade_{id}`, built from the shade's stable slot index and never from its name | `somfy-mqtt/src/ident.rs::ObjectId::for_shade` | **Not the reference, and a deliberate reading of R2.** R2 says `node_id` and `object_id` "MUST be sanitised to `[a-zA-Z0-9_-]`", which reads as though a shade's name flows into the object id. Building it from the id satisfies the character class more strongly — there is no user text to sanitise — and avoids a lifecycle cost R5 does not name: an object id that follows the name moves the discovery topic on every rename, stranding the old retained config as an orphan unless it is separately cleared. The requirements themselves note `object_id` "does not influence the entity_id", so the stable form costs nothing a user can see; the name still reaches Home Assistant through the payload's `name` field, which is where the display name comes from | Pinned by `somfy-mqtt/tests/topics.rs::renaming_a_shade_does_not_move_its_discovery_topic` and `a_shade_name_cannot_produce_topic_segments`, and as a property by `somfy-mqtt/tests/topic_props.rs::topics_are_invariant_under_the_shade_name` over hostile names (Unicode, slashes, wildcards, control characters, 400 bytes). Uniqueness across all 256 shade ids is exhaustively checked in `somfy-mqtt/src/ident.rs`'s own test module |
| The **payload's** `object_id` is device-scoped (`{device_id}_{object_id}`) while the **topic segment** of the same name is not | `somfy-mqtt/src/entity.rs::write_object_id`; the distinction is documented at `somfy-mqtt/src/ident.rs`'s module docs | **Not derived — a correction, found in review, to a claim this file previously made.** The row above quotes the requirements' "`object_id` does not influence the entity_id". That is true of the topic segment, which Home Assistant accepts and ignores. It is **false of the payload key**, which HA documents as *"used instead of `name` for automatic generation of `entity_id`"* and passes to the entity registry as the suggested object id. Emitted bare, the five diagnostics would claim `sensor.uptime`, `sensor.heap_free` and so on across the whole installation, and a second somfy-rs controller would get `sensor.uptime_2` — the exact collision the `device` block was added to prevent, one layer down, at the identifiers automations and dashboard cards point at. The device id makes the entity id as unique as the `unique_id` beside it, and neither half follows the shade's name | Host-tested by `somfy-mqtt/tests/device_entities.rs::the_payload_object_id_is_device_scoped_and_the_topic_segment_is_not`, which checks both halves and then the property directly: two configurations differing only in device id propose no entity id in common. Invariance under the name is checked as a property by `topic_props.rs::topics_are_invariant_under_the_shade_name`. **The HA behaviour itself is documentation, not an observation** — it is on the Task 5 checklist |

| Availability payloads `online` / `offline`, and removal as a **zero-length retained** publish | `somfy-mqtt/src/lifecycle.rs::ONLINE`, `OFFLINE`, `tombstone` | The `online`/`offline` vocabulary is the reference firmware's own (`espsomfyrts/status :: online`, captured on a live broker) and is also Home Assistant's default for `payload_available`. The zero-length rule is **MQTT's**, not either implementation's: a retained message is removed from a broker by publishing a zero-length payload to the same topic *with the retain flag set*, and nothing else removes one | Host-tested by `somfy-mqtt/tests/lifecycle.rs` — `the_will_is_offline_retained_at_the_availability_topic`, `removing_a_shade_publishes_a_zero_length_retained_payload_to_its_config_topic`, and `retiring_a_shade_clears_every_topic_the_firmware_ever_retains_for_it`. **Not yet observed against a real broker** |
| Every retained topic an announcement creates is cleared by the matching retirement, and both are derived from one `SHADE_COMPONENTS` array | `somfy-mqtt/src/lifecycle.rs::SHADE_COMPONENTS`, `MqttConfig::announce_shade`, `retire_shade` | **Not derived; chosen against the evidence.** The requirements spec records that clearing up after the experiments behind it needed 49 retained topics deleted by hand — and most of those were *state* topics, not configs, so R5's letter ("the config topic") is narrower than the failure it describes. Retirement therefore clears the per-shade state topics too, and does so without asking whether the shade had tilt, because a shade announced with tilt and retired without it would strand `.../tilt` | Host-tested by `retirement_clears_every_topic_an_announcement_retains` (both `has_tilt` values) and `the_tilt_topics_are_cleared_even_for_a_shade_that_never_had_tilt`. **This goes beyond R5 as written**; reported as a spec gap |
| Subscriptions to command topics suppress retained replay (`RetainHandling::Never`) | `somfy-mqtt/src/lifecycle.rs::Listen`, applied in `crates/firmware/src/mqtt.rs::Broker::execute` | **Not derived, and it is the half of R6 that is easy to lose.** R6 says commands are "published *and subscribed* with `retain = false`". The publish half is this device's own discipline; the subscribe half is not, because a retained message left on a command topic by anything else — a previous integration, a `mosquitto_pub -r` during debugging — is replayed to every new subscriber. Suppressing it is a per-subscription option and the only defence a subscriber has | Host-tested by `a_subscription_never_asks_for_retained_messages`, which walks every subscription of an announcement. **The mapping onto `minimq`'s `RetainHandling::Never` is executor-side and not host-testable**; it is on the Task 5 integration checklist |
| QoS 1 for every publish and every subscription | `crates/firmware/src/mqtt.rs::publish_bytes`, `execute` | Not derived; chosen. R6 permits QoS 0 or 1 for commands and says nothing about the rest. QoS 0 would lose a retained discovery config to one dropped packet with nothing reporting it — the entity would simply never appear — so there is no reason to run two policies | Not verified against a broker. The choice costs one round trip per publish and `minimq`'s TX arena is sized for the in-flight state it implies |

### R7 — the entity set, and the rule that decided its contents

**R7 says the reference's HACS plugin exposes roughly 24 entities and asks
somfy-rs to close the gap. somfy-rs publishes one cover per shade and five
device-level diagnostics, and the number is not the target.**

The rule is the one the requirements spec's own acceptance criterion states:
*"Appearing is not working; the C++ build produced three entities that were
permanently `unavailable`."* **An entity backed by nothing is worse than an
absent one**, because a control that never moves and a reading that never
changes both present as a device fault rather than as an unimplemented feature.
The tilt entry below is the precedent — it was a near-miss caught for exactly
this reason — and this table applies the same test to every candidate.

So each entity below is a value the firmware already holds, and every omission
is recorded after it with the condition for adding it.

**One correction to R7's wording, reported rather than followed.** It asks for
"discovery for sensors, binary sensors and diagnostics", which reads as three
kinds of entity. In Home Assistant only the first two are components;
`diagnostic` is an `entity_category`, an attribute *on* a sensor or binary
sensor. Following the sentence literally would put `diagnostic` where the
component segment goes — `homeassistant/diagnostic/…` — which Home Assistant
ignores without comment. That is the same silent-address failure the whole
requirements document was written from, reached from a different direction, so
it is worth naming: the five below are `sensor` entities carrying
`entity_category: diagnostic`.

| Item | Where it lives now | Derived from | Verified |
|---|---|---|---|
| Five device-level diagnostics — uptime, Wi-Fi signal, free heap, peak heap use, damaged rolling-code slots — as `sensor` entities under `{state_root}/device/{slug}` | `somfy-mqtt/src/entity.rs::DeviceEntity`, `somfy-mqtt/src/config.rs::DEVICE_SEGMENT`, `crates/firmware/src/mqtt.rs::Diagnostics` | **Not derived; chosen against what the firmware can actually report.** Every one is a figure this device already computes and already prints at boot: uptime from `embassy_time::Instant`, signal strength from `esp_radio::wifi::WifiController::rssi`, heap free and peak from `esp-alloc`'s `internal-heap-stats` counters (the same ones `heap::report` prints), damaged slots from the boot survey `store::FlashStore::survey` already produces. The topic layout is this project's own — the reference has no equivalent — and the segment is `device`, distinct from `shades` and `status` | Topics pinned by `somfy-mqtt/tests/device_entities.rs::device_topics_are_exact`; payload keys, `device_class`, unit and `state_class` by `every_diagnostic_carries_the_fields_that_make_it_readable`; round trip from rendered bytes by `somfy-mqtt/tests/round_trip.rs::every_diagnostic_payload_topic_is_a_firmware_topic`, with `the_diagnostic_check_actually_catches_a_reading_nobody_publishes` proving the check can fail. **No reading has been observed on a real broker**; that is Task 5 |
| The diagnostics are **device-level, not per-shade** — announced once whatever the shade count | `somfy-mqtt/src/lifecycle.rs::announce_device`, `retire_device` | Not derived. None of the five is a fact about a shade, so one per shade would report the same number several times; it also turns an announcement that costs `1 + pN + k` (for the `p` per-shade entities and command topics) into one that costs `k·N`, against a client that holds eight unacknowledged operations | Host-tested by `the_device_entity_count_does_not_follow_the_shade_count`, which checks the count is `DeviceEntity::ALL.len()` at zero, one and three shades |
| `entity_category: diagnostic` on all five | `somfy-mqtt/src/entity.rs::DiagnosticDiscovery::render` | Home Assistant's own attribute, named by the task brief. Without it every one lands on the device's primary dashboard card beside the covers, which an operator would have to undo entity by entity | Pinned by `every_diagnostic_is_categorised_as_one`. **Its effect in Home Assistant is not yet observed** |
| A `device` block — `identifiers`, `name`, `manufacturer` — on **every** payload, cover and diagnostic alike | `somfy-mqtt/src/entity.rs::write_device_block`, `MANUFACTURER`, `DEVICE_NAME_PREFIX` | **Not derived; a judgement, and it goes slightly beyond R7 as written.** Without it the five diagnostics appear as loose entities with no device to group them under, which on an estate with two controllers is two sensors called "Uptime" and no way to tell them apart — a different way of failing "MQTT alone is a complete integration". Every field is a fact already held: the identifier is the same stable `device_id` (the eFuse MAC) every `unique_id` is built from, so a user's rename in Home Assistant survives because it is stored against the identifier | Host-tested by `every_payload_names_the_same_device`, which checks the cover's block and all five diagnostics' blocks are byte-identical and carry the device id. **Grouping is not yet observed in Home Assistant.** `model` and `sw_version` are deliberately omitted — both are knowable (the chip is a compile-time constant, the version is `CARGO_PKG_VERSION`) and neither has a consumer until there is a firmware-update entity to compare against, which needs Plan 6's OTA |
| A diagnostic with no reading publishes **nothing** rather than a placeholder | `crates/firmware/src/mqtt.rs::Diagnostics::reading` | Not derived. The same rule as `Observed::seen` one section down: the value goes out **retained**, so a made-up figure outlives the boot that produced it and is handed to every later subscriber. Reachable today only for Wi-Fi signal, before the first association | Not yet observable. Home Assistant shows an entity with no state as unknown, which is what it is |
| `has_entity_name` is **not** emitted, so `name` is the friendly name verbatim | `somfy-mqtt/src/entity.rs::CoverDiscovery::render`, `DiagnosticDiscovery::render` | Not derived; a deliberate non-choice. Setting it would compose the device name into each entity's name, which is the modern convention for diagnostics and the wrong shape for a cover the user has already named. Left off means the shade shows the user's spelling, and two controllers on one estate both propose `sensor.uptime` — Home Assistant disambiguates the second and the `unique_id`s differ, so nothing is lost and the device block still tells them apart | **Not verified against a live Home Assistant.** On the Task 5 checklist beside `position_open`/`position_closed`, which has the same standing |
| `state_class: measurement` on uptime rather than `total_increasing` | `somfy-mqtt/src/entity.rs::DeviceEntity::state_class` | Not derived; chosen, and the alternative is named because it is defensible. `total_increasing` is Home Assistant's class for a monotonic counter that resets, which uptime literally is — but its long-term statistic is a *sum*, and nobody wants the total of all uptimes. `measurement` gives min/mean/max, which is what "how long has this been up" actually asks. HA's own idiom for uptime is a third option, `device_class: timestamp` carrying the boot instant, which this device cannot produce because it has no wall clock | Both combinations are accepted by Home Assistant, so this is a display and statistics choice rather than a correctness one. **Not observed** |
| Diagnostics republished every **60 s**, and once on every fresh broker session | `crates/firmware/src/mqtt.rs::DIAGNOSTIC_INTERVAL_S` | Not derived; chosen against the cost. Five publishes and five round trips a minute, against a shade in motion's twenty publishes a second. Published on announcement as well so a reconnect does not leave the entities unknown for up to a minute — and an interval is what makes uptime mean anything at all, since a figure published only on announcement would freeze at whatever it was when the session came up | An `embassy_time::Ticker` created once outside the session loop, so a delta or an inbound command does not restart the interval. Its `next` is documented cancel-safe (`embassy-time-0.5.1/src/timer.rs:269`), which is what makes it safe in a `select3`. **Not observed on hardware** |

#### The one per-shade entity besides the cover

| Item | Where it lives now | Derived from | Verified |
|---|---|---|---|
| A `button` per shade that transmits one `Prog` frame — `{state_root}/shades/{id}/pair/set`, announced and retired from the same `SHADE_COMPONENTS` the cover is | `somfy-mqtt/src/entity.rs::ButtonDiscovery`, `ShadeTopic::Pair`, `somfy-mqtt/src/lifecycle.rs::SHADE_COMPONENTS`, `crates/firmware/src/mqtt.rs::decode_command` | **Not derived.** Home Assistant's `button` component; the reference exposes pairing through its own web UI, not through MQTT. Per-shade rather than device-level because pairing is per-motor — a `Prog` frame carries one shade's address | Host-tested by `somfy-mqtt/tests/pair_button.rs`: the payload's topic resolves to a topic the firmware subscribes to, the button's `unique_id` differs from the cover's, and it is both announced and retired. **Nothing about it is observed in Home Assistant or on the air yet** |
| `payload_press` is **matched, not declared**: the discovery payload omits it and the firmware matches Home Assistant's default `PRESS` exactly | `somfy-mqtt/src/entity.rs::ButtonDiscovery`, `crates/firmware/src/mqtt.rs::PAYLOAD_PRESS` | Not derived; the same non-choice `CoverDiscovery` makes for `OPEN`/`CLOSE`/`STOP`. The exactness is the deliberate part: this is the only subscribed topic whose command puts `Prog` on the air, and a lenient parse would let a stray retained message or a mistyped `mosquitto_pub` re-pair or mis-pair a motor | Not observed. A mismatch fails safe in both directions — a button that presses and transmits nothing — which is the right direction for this control |
| `entity_category: config` on the pairing button | `somfy-mqtt/src/entity.rs::ButtonDiscovery::render` | Home Assistant's own attribute. Not decoration: without it the button lands on the shade's room card beside its open/close, where a mis-tap transmits `Prog` at a real motor | Pinned by `somfy-mqtt/tests/pair_button.rs::the_button_is_filed_as_configuration`. **Its effect in Home Assistant is not yet observed** |

**Why this passes the "an entity backed by nothing is worse than an absent one"
test even though it reports no state.** The rule is about entities that *look*
functional and are not. A `button` has no state to be wrong about: it is an
action, and this one performs a real one every time it is pressed. What it
cannot do is confirm the result, and that is a property of the protocol rather
than of the implementation — RTS is one-way and the motor never answers. The
confirmation is the motor jogging, seen by the person who put it into
programming mode, which is why `docs/hardware-checklist.md` treats pairing as a
procedure with a human in it rather than as a button press.

#### Deliberate omissions from R7, each with its revisit condition

Recorded rather than left as absences, in the shape of the tilt entry below.

| Not published | Why | Revisit when |
|---|---|---|
| **Sun / wind binary sensors, and sun-sensor switches** — the largest single block of the reference plugin's ~24 entities | `somfy-domain` defers sun, wind and dry-contact inputs post-1.0 (`somfy-domain/src/lib.rs`, `motion.rs`, `shade.rs`). There is no value to publish and no hardware input reading one | `somfy-domain` gains a sun/wind input. The entity is then a `binary_sensor` variant in `DeviceEntity` and a value in `Diagnostics::reading`, with no change to either half of the lifecycle |
| **A firmware-update entity** | There is no OTA until Plan 6, so the entity could report a version and never act on one. Home Assistant renders an update entity as an actionable control; one that cannot update is the "appearing is not working" failure with a button attached | Plan 6 lands OTA. `sw_version` in the `device` block becomes worth carrying at the same moment, for the same reason |
| **The last received frame** (address / command / RSSI) | Genuinely useful, and **not reachable without changing the frame path.** `somfy_tasks::FrameChannel` is a single-consumer `embassy_sync::channel::Channel` (`somfy-tasks/src/radio.rs:45`) whose one consumer is the state task; a second reader means making it a broadcast, which touches the radio→state seam that keeps the network out of radio control. Per-frame RSSI is further off still: reception is RMT pulse timing and `somfy-cc1101` never reads the RSSI register | The frame channel becomes a broadcast for some other reason, or the state task gains a "last frame seen" field it already publishes on the delta channel. The RSSI half needs a register read in the radio task, timed against the frame |
| **A Wi-Fi "connected" binary sensor** | Tautological over MQTT. It could only ever be published as `on` — a broker session exists only while the link does — and while the link is down the retained value is stale until the will fires. The availability topic already carries exactly this fact, correctly, because the *broker* publishes it on the device's behalf | Never, on this transport. It would make sense only on a second transport that outlives the link |
| **Tilt** | Unchanged; see the section below | Unchanged; see the section below |

#### Two things the diagnostics are honest about

| Claim | The limitation |
|---|---|
| Damaged rolling-code slots | **A snapshot taken at boot, not a live figure.** The store belongs to the state task from the moment `main` hands it over, and re-surveying it from the broker session would cross the boundary that keeps a broker from being able to affect radio control — `crates/firmware/src/mqtt.rs`'s first structural claim. A slot damaged after boot is therefore reported at the next one. That is the same latency an operator reading the serial line has, and it is stated in `Diagnostics`' own docs rather than implied. **Revisit** when the state task publishes something on the delta channel that can carry it |
| Peak heap use | The figure `heap::RADIO_HEAP_BYTES` is sized from. **The obligation Plan 5 carried since Task 2 is discharged** — the 46,660 bytes recorded there were taken with association *failing*, and the real worst peak under a live broker is 55,040, 8,380 higher; see "Hardware-verified values". Publishing it still matters, and the measurement is why: the peak is reached within a second of CONNACK and never moves again, and it varies by 4,216 bytes from boot to boot, so a serial cable catches one second of one sample where a graph catches the distribution |

### Deliberate deviation from R8 (tilt)

**R8 requires tilt-capable shades to expose `tilt_command_topic` / `tilt_status_topic`. somfy-rs implements the mechanism but will publish them for no shade until the domain's tilt port lands.**

`somfy-domain` states in its own docs that `TiltMode` is config-carriage only: it
is stored and round-tripped for backup and migration, but no command drives a
tilt axis, and `Shade::tilt_pos` always reports `Pos::ZERO`
(`somfy-domain/src/shade.rs`, `somfy-domain/src/types.rs::ShadeConfig::tilt_mode`,
which additionally warns that the API layer "MUST NOT surface tilt as functional
until that port lands, or it will advertise moves the domain does not make").

Publishing tilt discovery for a shade whose stored `TiltMode` is non-`None`
would give Home Assistant a tilt control that reports 0 forever and moves
nothing. That is the failure acceptance criterion 5 of the requirements spec
calls out — "Appearing is not working" — and it is worse than an absent control,
because a control that does nothing reads as a device fault rather than an
unimplemented feature.

| Item | Where it lives now | Derived from | Verified |
|---|---|---|---|
| Tilt topics are carried only when the caller asks for them, via an explicit `has_tilt` argument rather than a read of the stored `TiltMode` | `somfy-mqtt/src/entity.rs::ShadeTopic::for_shade`, `somfy-mqtt/src/config.rs::MqttConfig::cover_discovery` | **Not derived. A deliberate deviation from spec R8**, resolving it against `somfy-domain`'s own contract, which wins because it describes what the firmware can actually do today | Omission is pinned by `somfy-mqtt/tests/topics.rs::tilt_topics_are_omitted_for_non_tilt_shades` and `somfy-mqtt/tests/round_trip.rs::non_tilt_shades_carry_no_tilt_keys`. **Revisit when the domain's tilt port lands** — when `Shade::tilt_pos` reports a real position and a command drives the tilt axis, the caller starts passing `true` and R8 is satisfied with no change to this crate |

## somfy-config

Almost nothing here comes from the reference implementation. The Wi-Fi rules are
802.11's and the driver's; the record layout is this project's own, copied in
shape from `somfy-store` because it lives on the same kind of flash region and
fails in the same ways.

| Item | Where it lives now | Derived from | Verified |
|---|---|---|---|
| SSID bounded to **32 bytes**, and counted in bytes rather than characters | `somfy-config/src/credentials.rs::MAX_SSID_LEN` | IEEE 802.11's SSID information element, whose length field the standard caps at 32 octets. Bytes and not characters because the element carries octets: a 24-character SSID of two-byte characters is 48 octets and would be truncated by the driver into a different network name | Host-tested by `somfy-config/tests/credentials.rs::an_ssid_at_the_limit_is_accepted_and_one_over_is_not` and `the_ssid_limit_counts_bytes_not_characters`. Cross-checked against `esp_radio::wifi::sta::StationConfig::validate`, which rejects above 32 |
| Passphrase bounded to **8..=64 bytes**, or empty for an open network | `somfy-config/src/credentials.rs::MIN_PSK_LEN`, `MAX_PSK_LEN` | WPA-PSK's own key-derivation rule (8–63 characters for a passphrase, 64 hex digits for a raw key) and `esp_radio::wifi::sta::StationConfig::validate`, which rejects above 64. The empty case is the driver's own convention for no authentication | Host-tested by `a_passphrase_shorter_than_the_wpa_minimum_is_refused`, `a_passphrase_at_each_limit_is_accepted`, `a_passphrase_over_the_limit_is_refused`, `an_open_network_has_an_empty_passphrase` |
| Either field containing a NUL byte is **refused, not truncated** | `somfy-config/src/credentials.rs::check` | Not a standard: the driver hands both fields to a C API that terminates at NUL, so `Home\0Office` reaches the radio as `Home` — a different network, or none. There is no way to repair it without guessing which half was meant | Host-tested by `an_interior_nul_is_refused_in_either_field`. The truncation itself is a property of the C string convention, not something measured on this hardware |
| `CONFIG_RECORD_LEN` = **512** bytes, CRC-32 (ISO-HDLC) over the whole record, magic `RTSW`, version **2** | `somfy-config/src/record.rs` | Not derived. Two of the three flash constraints `somfy_store::RECORD_LEN` records — a whole number of 4-byte write words, an exact divisor of the 4 KB erase sector — with a distinct magic so a region mounted at the wrong offset is reported rather than half-read. **The third is deliberately given up:** at 512 a record is two SPI NOR page programs rather than one, which was a wear and latency nicety and never an atomicity guarantee — a page program is not atomic against a power cut either, which is why every record carries a CRC and why the ring never overwrites the newest one. 256 no longer fits: the MQTT settings need 102 bytes of broker plus 64 of namespaces beside the 96 of Wi-Fi credentials | The CRC variant is pinned by a known-answer test (`the_checksum_is_the_standard_crc32`, the standard 0xCBF43926 check value). Rejection is walked exhaustively on the host: every 4-byte truncation point (`every_truncation_of_a_write_is_rejected`) and every single-bit flip in all 256 bytes (`every_single_bit_flip_is_rejected`). Round-tripped on real flash — see "Hardware-verified values" |
| Broker address stored as an **`Ipv4Addr`**, never a host name | `somfy-config/src/mqtt.rs::MqttSettings` | Not derived. A consequence of this firmware's own build: `embassy-net` is compiled without its `dns` feature, so a host name is a value the record could hold and the network layer could do nothing with. Storing an address makes an unresolvable broker unrepresentable rather than merely unlikely | Host-tested by `somfy-config/tests/mqtt.rs::an_address_no_tcp_connection_could_reach_is_refused`, which walks unspecified, loopback, broadcast and multicast. **The DNS-feature claim is a fact about `crates/firmware/Cargo.toml`, not a measurement** — enabling `dns` and budgeting a resolver socket out of `SOCKETS` would remove the constraint |
| The two stored namespaces are validated by **`somfy-mqtt`'s own** validators, including the cross-field overlap rule | `somfy-config/src/mqtt.rs::MqttSettings::new` | Not derived. R3 requires rejection at the point of entry; restating the rules in a second crate is how the two would drift, and a namespace the record accepts and the topic builder refuses is a device that boots without MQTT and says so three flashes after the value was typed | Host-tested by `the_two_roots_are_validated_by_the_topic_rules_that_will_use_them` and `two_roots_naming_the_same_namespace_are_refused`, the latter including the `home` / `homeassistant` boundary that is *not* an overlap |
| The passphrase and the broker password are stored **in the clear** | `somfy-config/src/record.rs`, `crates/firmware/src/config.rs` | Not a decision with a source; a limitation stated rather than mitigated. Flash encryption is not enabled on this device, so `espflash read-flash` recovers the passphrase. An obfuscation layer would need its key in the same flash and would change only how safe the reader felt | Not applicable. `WifiCredentials`' hand-written `Debug` redacts the passphrase so it cannot reach a serial console through the ordinary `{:?}` error path — pinned by `debug_redacts_the_passphrase_and_keeps_the_ssid` — which is a much smaller and different claim than protection at rest |
| Shade record **version 3**: the entry's last padding byte (offset 23) carries `somfy_domain::PairingState`, and **versions 1 and 2 decode as `ConfirmedByOperator`** | `somfy-config/src/shade.rs` (`VERSION_PAIRING`, `ENTRY_PAIRING`, `Layout::per_shade_pairing`, `decode_entry`) | Not derived. The offset is forced — 24..56 is the name field and 20..23 were spent on kind, tilt, width and protocol, so byte 23 is the only spare byte in an entry and nothing moves. The **migration** is the same error-direction argument the announced-shade bitmap already used one field along: the byte is zero in every v1 and v2 record ever written, zero is `AwaitingConfirmation`, and reading it that way would silently un-announce every shade on a board that works today. See [Why there is a pairing state after all](#why-there-is-a-pairing-state-after-all) | `somfy-config/tests/shade_v2.rs` decodes a **byte-for-byte capture of what the previous build's encoder emitted** — taken by running that build, not reconstructed by this one — and asserts both halves: that the capture really carries a zero at each entry's offset 23, and that all three shades still decode as confirmed. `shade_v1.rs` asserts the same for the older layout, and both check that a re-encode writes version 3 and round-trips. A byte outside the two states is `ShadeRecordError::Pairing`, refused rather than defaulted, because here *both* defaults are wrong — `an_unmodelled_pairing_state_is_rejected` |

## firmware

| Item | Where it lives now | Derived from | Verified |
|---|---|---|---|
| Heap size `RADIO_HEAP_BYTES`, **per chip**: ESP32 60 KiB, ESP32-S3 163 KiB, ESP32-C3 150 KiB | `crates/firmware/src/heap.rs::RADIO_HEAP_BYTES`, `STACK_BUDGET_BYTES`, `DRAM_FOR_STACK_AND_HEAP` | **Derived, and the derivation is in the code rather than only in the comment.** The heap and the main stack are two shares of one fixed quantity — esp-hal's linker script gives the stack whatever DRAM is left once the statics are placed, so a byte of heap is a byte of stack, exactly and with no slack. So the *stack* is chosen once and the heap is the remainder: `RADIO_HEAP_BYTES = (DRAM_FOR_STACK_AND_HEAP − STACK_BUDGET_BYTES)` rounded **down** to a whole KiB, the rounding going down so the residue lands on the stack, which is the side that fails silently. `STACK_BUDGET_BYTES` = 49,592 + 16,688 = **66,280**: `main::REQUIRED_STACK_BYTES` plus a margin equal to the largest single stack frame emitted in any of the three images, which covers the one thing the frame-by-frame derivation cannot see — what an interrupt handler *calls*, since `esp_radio`'s `Handler::dispatch` runs the closed Wi-Fi driver on this very stack and neither it nor masked ROM carries stack-size metadata. **This replaces a shared 56 KB** that was bracketed rather than derived, and that was sized by the smallest chip then in the matrix | Every step read off real artefacts on 2026-08-17. `DRAM_FOR_STACK_AND_HEAP` (ESP32 128,348; ESP32-S3 233,700; ESP32-C3 220,456) from `_stack_start_cpu0 − _stack_end_cpu0` in each release ELF plus the 57,344-byte heap that build carried. The margin from the `.stack_sizes` section `-Zemit-stack-sizes` emits: largest frame ESP32 14,320, ESP32-S3 14,320, **ESP32-C3 16,688** — the C3's is `entry` with `start` inlined into it, and it is *not* the same frame as the other two, so a margin taken from the ESP32 alone would have been smaller than one worst-case frame on a chip that ships. Resulting stacks predicted and then confirmed against the relinked ELFs to the byte: 66,908 / 66,788 / 66,856. Confirmed on the spare ESP32-S3, which printed `stack: 66788 bytes available, 49592 required`. **SUPERSEDED twice, 2026-08-17** — first by the web server, which changed every DRAM figure here (see the `DRAM_FOR_STACK_AND_HEAP` row below), and then by the discovery that the 49,592 requirement and the 16,688 margin were both wrong. The *rule* stands unchanged; only its inputs and the account of the margin were replaced. Current figures live in `heap.rs` and in the four stack rows at the end of this section |
| ~~**The ESP32-S2 builds without the broker session** (`mqtt` cargo feature)~~ — **void: the ESP32-S2 was dropped on 2026-08-17 and the `mqtt` feature with it** | removed from `crates/firmware/Cargo.toml` and `crates/firmware/src/main.rs`; a note on why the feature existed is kept in `Cargo.toml` | The feature existed to compile the broker session out for one chip that could not hold it, and had no other caller — nothing else wants a build without a broker, because a board with no broker *provisioned* is an ordinary runtime state `start_mqtt` already handles without a feature flag. The chip itself went because it could not hold the Wi-Fi heap and a bootable stack at once, its pin map was never checked against a board, and nobody had ever booted one | The arithmetic that justified the feature is preserved in `Cargo.toml`; it is no longer a live claim about anything this crate builds |
| MQTT packet buffers: rx 512, tx **1664**; TCP socket buffers rx 768, tx 1024 | `crates/firmware/src/mqtt.rs::MQTT_RX_BYTES`, `MQTT_TX_BYTES`, `TCP_RX_BYTES`, `TCP_TX_BYTES` | Not derived; bounded from both sides. The rx figure is advertised to the broker as MQTT 5 `MaximumPacketSize`, so it is a *ceiling the broker is told to obey* rather than local storage — and every payload subscribed to is a single word or a number (`OPEN`, `CLOSE`, `STOP`, `PRESS`, or a position). The tx figure has to hold the largest outbound packet, a retained discovery config whose payload `somfy-mqtt` bounds at **1152** after Task 4 widened it, plus its topic and header — about 1,310 bytes at the widest configuration the crate accepts, and under 600 at the one this firmware builds. It grew from 1536 with the payload bound. **One packet, never two:** `perform` settles every operation, and without that two of the widest configs would already overrun this figure | Every byte here comes off the main stack, so the sizes are visible in the per-chip table above. **Not yet exercised against a real broker**; the discovery config a 32-character shade name actually renders to has never been measured on the wire |
| Broker reconnect: 1 s doubling to a 60 s ceiling, reset only after a 30 s session | `crates/firmware/src/mqtt.rs::RETRY_MIN_MS`, `RETRY_MAX_MS`, `STABLE_SESSION_MS`; schedule in `somfy_tasks::Backoff` | **Not derived; chosen, and the choice is the requirement.** Same figures and same argument as the Wi-Fi loop. `STABLE_SESSION_MS` is the part that matters: a broker that accepts a connection and then closes it — wrong credentials on a broker configured to disconnect rather than refuse, an ACL that rejects the first publish, a second client using the same client id — reports success on every attempt, and an unconditional reset would pin the retry at 1 s forever in exactly the case the bound exists for | Schedule host-tested in `somfy-tasks/src/backoff.rs`, `succeed_after` included. **The broker loop itself has not been observed on hardware** |
| **Every broker operation is acknowledged before the next is issued**, and there is exactly **one** function that puts anything on the wire | `crates/firmware/src/mqtt.rs::perform`, which is `execute` followed by `settle` and is the only route to `publish_bytes` or `subscribe` | **Not derived — read out of `minimq` 0.13's source, and it is a hard limit rather than a tuning choice.** `Connection::publish` and `Connection::subscribe` take a slot in `outbound.retained`, capacity `MAX_RETAINED = 8` (`src/mqtt_client/outbound.rs:11`), and a slot is freed only by `outbound.ack_packet`, reached only from `session/inbound.rs` via `drive_packet` — that is, only from `recv`, `poll` or `drive`. Publishing flushes outbound and never reads, so a plan walked without reading fails at the ninth operation with `InflightExhausted` and repeats identically on every reconnect. The retained packets also hold `MQTT_TX_BYTES` until acknowledged, and three unacknowledged discovery configs fill it — the tighter of the two ceilings. **Plan 5 Task 4 restructured this so that forgetting it is not expressible:** Task 3 called `settle` at four call sites, Task 4's device diagnostics add a fifth and a sixth, and "remember to settle" does not survive that — every publish and every subscribe now goes through `perform`, which settles before it returns. The arithmetic moved too: an announcement costs `1 + 5N + k` operations for `N` shades and the `k = 5` device entities (a cover and a pairing-button config, plus three command subscriptions, per shade), and the firmware follows it with `N` names, `2N` states and `k` readings — `1 + 8N + 2k` for a fresh session, which is **eleven with no shades provisioned at all**, the ordinary state of a freshly flashed board. In Task 3 the same burst was `1 + 6N` and needed two shades to exceed eight. The plan alone crosses eight at one shade | Found in review against `minimq`'s source rather than its documentation. Host-tested in `somfy-mqtt/tests/lifecycle.rs` against a **model of the client's eight slots**: `walking_a_plan_without_settling_runs_out_of_slots_partway` walks a real plan through it and asserts the failure lands on the ninth operation, and `settling_after_every_operation_completes_a_plan_of_any_length` walks a 40-plus-operation plan through the same model *with* settling and completes. `an_announcement_for_one_shade_already_exceeds_the_clients_inflight_slots` fails if the plan ever shrinks back under the limit, which is what would quietly stop the discipline being load-bearing. **Still not observed against a real broker** |
| An announcement and the republish that follows it cannot be asked for separately | `crates/firmware/src/mqtt.rs::Broker::resync` | **Not derived; found while adding the device diagnostics, and it fixes a latent Task 3 fault.** Retiring a superseded configuration that shares the *state root* tombstones the very state topics the current configuration publishes to — `somfyrs/shades/1/position`, and now `somfyrs/device/uptime` as well. Task 3's `ConnectEvent::Reconnected` branch ran the announcement and not the republish, so that path would have left every position and every reading cleared on the broker until something changed, which for a shade nobody touches is days. `resync` is the announcement *and* the republish, and there is no way to obtain one without the other | Latent rather than observed: `session_expiry_interval(0)` makes `Reconnected` a branch nothing takes today, which is exactly why it was worth closing before something starts taking it. **Not verified against a broker** |
| A packet that will not fit **never ends the session**, whichever limit it exceeded | `crates/firmware/src/mqtt.rs::publish_bytes`, `report_oversize` | **Not derived — found in review, and it closes a gap between a stated policy and the code.** `execute`'s policy is that only failures reconnecting could fix may end a session. Task 3 applied it to `PubError::Payload(())` — the payload overrunning the transmit arena — and not to `Error::Resource(_)`, whose three variants are all local. The reachable one is `ResourceError::PacketTooLarge`, which comes from the **broker's** advertised MQTT 5 `MaximumPacketSize` (`minimq-0.13.0/src/mqtt_client/session/state.rs`) rather than from anything sized here: a broker configured below a discovery config would refuse every one, identically, on every reconnect, at the 60-second ceiling, forever — precisely the loop the policy forbids, and the one case the compile-time capacity proofs cannot rule out because the limit is not ours. `BufferTooSmall` is the same condition as `Payload(())` reached from the header or topic half, and was taking the opposite branch | Read out of `minimq`'s source. Not reachable against a default Mosquitto, which advertises no limit, so **not observed**. The wildcard arm defaults a future variant to the same policy rather than to a reconnect loop, because every variant `ResourceError` has is local |
| Log lines whose rate a **remote peer** sets are counted, not printed each time | `crates/firmware/src/mqtt.rs::report_rare`, `Rare` | Not derived. The same rule `net` and `tasks` already apply, extended to the two lines a broker can drive — an inbound message that is not a command, and a command dropped by a full queue. `esp_println` writes inside a critical section and the radio task has ~5 ms to re-arm between a frame and its repeat, so an unbounded line here is the one route by which something outside the house reaches radio timing. The topic and the command are deliberately kept out of the message: both are peer-influenced and both lengthen it | Found in review. The counters are **plain `u32` on the session, not atomics** — `riscv32imc`, the ESP32-C3's target, has no atomic read-modify-write instruction, so `AtomicU32::fetch_add` does not exist there. That was caught by the four-chip matrix and by nothing else, which is the matrix's whole purpose |
| A shade with no observed state publishes **nothing**, rather than a retained zero | `crates/firmware/src/mqtt.rs::Observed::seen` | Not derived. `Known` is this task's own copy and it has no source for a position until a delta arrives; seeding at `Pos::ZERO` agrees with the state machine today only because both start there. The value would go out **retained**, so a wrong one outlives the boot that produced it and is handed to every later subscriber — a confidently wrong retained value, which is the failure class this integration is written around. An absent one leaves Home Assistant showing the position unknown, which is what it is | Found in review. Not yet observable: no shade exists to report one |
| Signal strength is sampled by the **Wi-Fi link task** and read by the broker session through one `static`, every **30 s** while the link is up | `crates/firmware/src/net.rs::SIGNAL_DBM`, `signal_dbm`, `hold_link`, `RSSI_POLL_S` | **Not derived; forced by the API.** `WifiController::rssi` takes `&self` on a controller `wifi_link` owns for the life of the program — dropping it deinitialises Wi-Fi — so the broker session cannot read it directly. The seam carries a *value*, never the controller, so nothing outside `net` can reach the radio through it. It is a `blocking_mutex<CriticalSectionRawMutex, Cell<Option<i32>>>` rather than an `AtomicI32` for the same reason `Rare`'s counters are plain integers: `riscv32imc` has no atomic read-modify-write. The interval is chosen against the cost — each sample drops and recreates a subscription to `esp-radio`'s event channel, whose slots that crate `expect`s rather than handles | **The no-lost-disconnect argument is read from `esp-radio` 0.18.0's source, and it rests on an ordering rather than on timing.** `wait_for_disconnect_async` (`src/wifi/mod.rs:2967`) subscribes on each call and drops the subscription with its future, so a disconnect published while the timer branch wins *is* missed by that subscription. It is not lost, because `event_post` calls `state::update_state(event)` — storing `WifiStationState::Disconnected` — at `src/wifi/os_adapter/mod.rs:674`, **before** it publishes to `EVENT_CHANNEL` three lines later, and `wait_for_disconnect_async`'s first act is `if !self.is_connected() { return Err(WifiError::NotConnected) }` reading that same state. The flag an unsubscribed caller reads is therefore set strictly earlier than the message it could have missed, so the worst case is one `select` of latency. Exactly one subscription is alive at a time. **Not observed on hardware**; a first sample is taken immediately after association so the entity is never announced without a reading |
| The link's last signal strength is **cleared** when the link drops | `crates/firmware/src/net.rs`, the `record_signal(None)` after `hold_link` returns | Not derived. Same rule as `Observed::seen` below: the reading goes out retained, so leaving the last figure in place would keep publishing a number about a link that no longer exists | Not yet observable. In practice the broker session dies with the link and its will fires, so the stale value would sit under an unavailable device — which is why this is cheap insurance rather than a fix for an observed fault |
| MQTT **session expiry 0** — a clean session, so the broker keeps nothing for this device while it is away | `crates/firmware/src/mqtt.rs`, the `session_expiry_interval(0)` on the config builder | **Not derived; chosen, and it is R6's hazard reached by a second route.** A session that survives disconnection is one the broker *queues for*: every QoS 1 command published while the device is away is delivered the instant it returns, so a shade can act on an hour-old instruction after a broker restart. That is worse than the retained-command form R6 names, because several can arrive at once and nothing distinguishes them from fresh ones. It is `minimq`'s default, but it is stated explicitly so the decision is visible rather than inherited | Not verified against a broker. The cost is that `ConnectEvent::Reconnected` becomes rare — a fresh session means the whole announcement runs again on every reconnect, which is a handful of idempotent retained publishes. The `Reconnected` branch is still handled, because a broker may resume for its own reasons |
| The MQTT client id is the eFuse MAC, hex-encoded, and so is `device_id` | `crates/firmware/src/mqtt.rs::device_id` | Not derived. R4 requires `unique_id` to be stable across reboots, configuration changes and firmware updates, and the factory MAC is the only value on this board that is. It is not a secret — it is in every Wi-Fi frame the device sends — and it is not derived from anything a user edits. Reusing it as the MQTT client id matters for a second reason: a broker disconnects the older of two clients sharing an id, so an unstable one reads as a broker repeatedly dropping the device | Read through `esp_hal::efuse::base_mac_address`. **Not yet observed on a device** |
| `SCHEDULER_HEAP_BYTES` = 4 KB for `tx-check` | `crates/firmware/src/heap.rs::SCHEDULER_HEAP_BYTES` | **Not derived, and the derivation this row used to give was wrong.** It said `esp_rtos::start` allocates its main-task bookkeeping through `allocator_api2::boxed::Box`. It does not: `task::allocate_main_task` writes into fields of the static `SCHEDULER` and adopts the existing main stack, and the heap reads 0 bytes used immediately after `esp_rtos::start` — see the 2026-08-17 row in "Hardware-verified values". What `start` really does is install `__getreent`, `_malloc_r` and `_free_r` into the ROM syscall table, so ROM-side allocation lands in this heap lazily, and a binary with no heap installed would meet the first such call with `handle_alloc_error`. The 4 KB is a reserve against that path, not a measured requirement | **The premise was falsified on 2026-08-17; the constant was kept anyway, deliberately.** It cannot be measured the way every other figure here was: `tx-check` is the only image in this crate that keys the transmitter by itself, so "run it and read the high-water mark" is not available. Shrinking a safety margin on an argument alone is the failure this file exists to prevent, so the number stands and the reasoning was replaced instead. It remains true that `tx-check` boots and transmits with this heap installed (2026-08-16) |
| Reconnect backoff 1 s doubling to a 60 s ceiling | `crates/firmware/src/net.rs::RETRY_MIN_MS`, `RETRY_MAX_MS`; schedule in `somfy_tasks::Backoff` | **Not derived; chosen, and the choice is the requirement.** Spec R9 asks for "bounded backoff" without naming numbers. The floor stops a refusing access point being retried as fast as the driver can scan — a radio transmitting continuously for no purpose. The ceiling is what makes a rebooted router rejoin without a human power-cycling the controller | Schedule host-tested in `somfy-tasks/src/backoff.rs` (growth, ceiling, reset-on-success, saturation near `u32::MAX`, and every delay inside the declared range). Observed end to end on hardware — see "Hardware-verified values" |
| The controller's panic handler **resets** where the bring-up harnesses halt, after a 100 ms drain | `crates/firmware/src/main.rs::panic`, `PANIC_DRAIN_MS` | **Not derived; forced by the dependency.** Halting was right while this image was radio and flash only — every reachable panic was this project's own code failing deterministically, and a frozen board with its message on the line is the best thing to hand a debugger. Wi-Fi adds panics this crate neither writes nor can catch: `esp_radio::wifi::WifiError::from_error_code` ends in `panic!` on any status code outside the six it maps (and `esp_wifi_connect` documents three it does not — `ESP_ERR_WIFI_NOT_INIT`, `_NOT_STARTED`, `_CONN`), its event-channel subscriber slots are `expect`ed, and an allocation failure anywhere reaches `handle_alloc_error`. None is reachable through `net::start`'s `Result`, so a halting handler would let a transient fault in a *degradable* service take the radio off the air until a human power-cycled the board — the outcome R9 exists to forbid | Both halves measured on hardware — see "Hardware-verified values". The drain length is a margin choice, roughly 3× what a long message needs at 115200 baud, not a measurement of the FIFO |
| `wificfg` partition: data/undefined at 0x202000, 8 KB (two 4 KB erase sectors) | `crates/firmware/partitions.csv`, `crates/firmware/src/config.rs::PARTITION_LABEL` | Not derived. Same shape and same reason as `rollcode`: two sectors is the minimum `somfy_store::SectorRing` accepts, because it is what guarantees erasing one sector can never take the newest record with it. A separate partition rather than more slots in `rollcode` because the two have very different write rates and very different consequences on loss, and one erase must never touch both. `rollcode` keeps its offset, so a board reflashed with this table keeps its rolling codes | Mounted, surveyed, written and read back on a real ESP32-S3 by `config-check` — see "Hardware-verified values" |
| The config store **reports** damage where the rolling-code store **refuses** it | `crates/firmware/src/config.rs::ConfigStore::load` vs `crates/firmware/src/store.rs::Scan::newest_or_refuse` | Not derived. A deliberate asymmetry from the cost of being wrong: guessing at a rolling code costs a physical re-pairing procedure at every shade, while an unreadable configuration costs a Wi-Fi connection and one re-provisioning step. Spec R9 additionally forbids the network affecting radio control, so refusing to boot over a damaged config region would violate the requirement this task exists to satisfy | The `damaged` count is printed at every boot either way, so the two cases stay distinguishable. Observed on hardware as `config: survey slots=32 valid=0 blank=32 damaged=0` on a freshly partitioned board |
| `shades` partition: data/undefined at 0x204000, 8 KB (two 4 KB erase sectors) | `crates/firmware/partitions.csv`, `crates/firmware/src/shades.rs::PARTITION_LABEL` | Not derived. Same shape and same two-sector reason as `rollcode` and `wificfg`. A **third** region rather than more fields in `wificfg` because the shades do not fit there: that record is 512 bytes and its last field ends at offset 280, leaving 228 bytes before the CRC — four shades' worth against the 32 the registry holds — and widening the record would strand the credentials already on provisioned boards. `rollcode` and `wificfg` keep their offsets, so a board reflashed with this table keeps its codes and its credentials | **Not yet mounted on hardware.** Geometry is checked at mount the same way the other two are, and the record/slot relationships are `const` assertions against `esp-storage`'s own figures on all four chip builds |
| Shade record **version 2**: 20-byte header, 56 bytes per shade, 32 shades, then a shared linked-remote pool of 58 words | `somfy_config::SHADE_RECORD_LEN`, `SHADE_TABLE_CAPACITY`, `MAX_LINKS`, `crates/somfy-config/src/shade.rs` | Derived, and the derivation is arithmetic. An entry holds address(4), seed code(2), kind(1), tilt mode(1), three travel times(12), a frame width(1), a radio protocol(1), a name length and its 32 bytes, padded to 56 for word alignment; the capacity is `somfy_domain::MAX_SHADES` rather than whatever the record could hold, so the bound a provisioning tool hits is the one the controller actually has, and 2048 is the smallest power of two that fits while dividing a 4 KB erase sector. The header grew by 8 to carry the announced-shade bitmap (4) and the pool's live count (2) plus 2 of padding, which is why the entries moved from 12 to 20. The entry did **not** grow: the frame width and the radio protocol went into two of the three padding bytes it already had at 21..24, so every existing field kept its offset. `20 + 32*56 + 4 = 1816`, and the 232 bytes left over are exactly 58 four-byte link words — so the record is full to the byte and `MAX_LINKS` is defined as what is left rather than chosen. **Seven links per shade does not fit and was not adopted for the record**: `20 + 32*(56+28) + 4 = 2712` against a 2048-byte slot. The two alternatives are recorded next to the constant — a 4096-byte slot doubles a stack buffer on a chip whose main stack is 14,588 bytes, and a 24-shade table stops the record holding a full `MAX_SHADES` registry | Pinned by `somfy-config/src/shade.rs::a_full_table_is_the_size_the_docs_claim` (which asserts the record is full to the byte) and by four compile-time assertions, **each proved to fire on 2026-08-17** by breaking it and restoring: a header length that leaves a non-multiple of four (`the linked-remote pool must fill the record exactly`), and a 40-shade capacity (`a full registry must fit inside one record`, `... a record of the first layout too`, `the announced-shade bitmap has one bit per shade and no more`, plus a compile-time subtraction overflow in `MAX_LINKS` itself) |
| Version **1** records are still read, with 56-bit RTS substituted for the two radio fields, no linked remotes, and every shade it holds marked announced | `crates/somfy-config/src/shade.rs::Layout`, `ShadeRecord::header` | Not derived. Forced by a provisioned board carrying a version-1 record: refusing it would make three real shades vanish at the next boot. The two substitutions are the only honest ones — 56-bit RTS is the only thing this firmware has ever transmitted, so it is what those shades have always been driven as. "Every shade announced" is chosen for the *direction* of its error: it is wrong only for a board that never reached a broker, and being wrong that way costs one zero-length publish to a topic holding nothing, where the opposite error leaves a retained discovery config on the broker forever | `somfy-config/tests/shade_v1.rs` decodes a **byte-for-byte capture** of what the previous build's `encode` emitted — recorded as its sparse non-zero bytes, checksum included, so a reconstruction that agreed with itself could not pass. Deleting the version gate (treating v1 as v2) was tried on 2026-08-17 and fails four of those tests |
| The announced-shade set is a bitmap of **registry ids**, not of entities per shade | `somfy_config::Announced`, `Catalog::orphans` | Not derived; it is what makes `MqttConfig::retire_shade` callable at all. A removed shade's retained discovery config outlives it on the broker, and clearing it needs an id that — once the shade is out of the registry and out of the table — nothing else in the system can produce. The **ordering** follows from that: the shade is removed and the record written with its bit still set, so flash names the orphan; the bit is cleared only after the broker has acknowledged the tombstones. A power cut anywhere in between leaves the orphan named, and the next boot sweeps it | Host-tested by `somfy-config/src/catalog.rs::a_removed_shade_stays_named_until_it_is_retired`, `re_adding_a_shade_at_an_orphaned_id_does_not_sweep_it`, and `a_removal_reaches_the_record_and_leaves_the_bit` |
| Removing a shade unpublishes its entities **before** the shade is forgotten | `crates/firmware/src/mqtt.rs::Broker::retire`, `somfy_config::Catalog::remove` | the reference firmware's `deleteShade`, which emits its removal event, calls `unpublish()`, and only then clears the shade — the same order, for the same reason | Host-tested by the `Catalog` tests above. The reference's retirement is also **unconditional** over a fixed topic list, which this project matches: `MqttConfig::retire_shade` clears every component a shade *can* own even when the announcement withheld one, because clearing a topic nothing was published to is a no-op and failing to clear one is permanent |
| Debounced shade-table writes: 2 s after the last change, at most 10 s after the first | `somfy_config::DEBOUNCE_MS`, `MAX_DEFER_MS` | **Policy figures, not measurements**, and the design spec's §6 requirement that config writes are debounced while rolling-code writes stay synchronous. Two seconds makes four shades added from a UI one flash write; the ten-second ceiling exists so a steady trickle of edits cannot postpone the write forever and lose all of them at once | Host-tested by `somfy-config/src/catalog.rs::a_burst_of_changes_is_one_write_after_the_last_of_them` and `a_steady_trickle_cannot_postpone_the_write_forever`. **Not measured on hardware** |
| A shade record's rolling code is applied **only when the store holds none for that address** | `somfy_store::seed_if_absent`, `crates/firmware/src/main.rs::seed_shades` | Not derived; it is the rolling-code invariant reached from the one direction the store's own rules leave open. The store refuses to invent a code, so the first one has to come from configuration — and configuration is re-read at **every** boot. Writing it every boot would move the counter backwards, and a motor rejects any code at or below the last it accepted, which presents as a dead transmitter and is undone only by re-pairing at the motor. The commit therefore sits inside the branch where the read found nothing, so overwriting is not something the function declines to do but something it cannot express. A missing code in a region reporting **damage** is refused rather than planted, for the reason `store-check` already refuses it: an empty read there may be a lost code rather than a new shade | Host-tested in `somfy-store/tests/seed.rs`: `a_reboot_cannot_walk_a_rolling_code_backwards` seeds, transmits three times, drops the store, re-mounts it over the same persistent cells, seeds again from the same record, and asserts the stored code did not move and the next frame carried 8 rather than the record's 5; `repeated_seeding_is_idempotent` runs 50 boots and asserts **no commit was issued at all**. **Not yet observed on hardware** — the second-boot serial line is the check, and `docs/hardware-checklist.md` says which wording proves it |
| A shade's id is its **position** in the record, and nothing stores it | `crates/somfy-config/src/shade.rs`, `crates/firmware/src/main.rs::report_shades` | Not derived; forced by the registry as it then was. `Registry::add_shade` assigns the lowest free slot and takes no id, so on an empty registry the record's order *is* the id order — and `somfy-mqtt` builds `shade_0`, `shade_1` … from those ids. The cost is real and is stated rather than hidden: appending is safe, and reordering or removing renumbers everything after the change, which in Home Assistant means new entities and retained orphans under the old ones | **The premise is now half false, and the row stands until the other half lands.** `Registry::add_shade_with_id` exists and places a shade at a caller-named id, refusing a duplicate (`DomainError::DuplicateId`) or out-of-range one (`DomainError::IdOutOfRange`), so a stored id is no longer a field the registry could not honour. What has not changed is the record and the firmware that reads it: `provision_shades` calls `add_shade`, so position is still what a board uses. Deliberately sequenced that way — an id column the tool writes and the device ignores would make a reorder *look* safe. **Not observed** — no board has yet been provisioned with two shades and had one removed |
| `add_shade_with_id` refuses a duplicate or out-of-range id rather than choosing another slot | `crates/somfy-domain/src/registry.rs::Registry::add_shade_with_id` | Not derived; it is this project's standing rule that invalid configuration is refused at the point of entry. Substituting a free slot for a taken one would put a shade under an entity name nobody chose, and overwriting the occupant would delete a provisioned shade to make room for one that asked for its place — both are the renumbering this method exists to stop, arrived at by a different route | Host-tested in `somfy-domain/tests/registry.rs`: placement at a chosen id, holes left below it, the two refusals and their order, the last in-range slot, `RegistryFull` proved unreachable, interoperation with `add_shade`'s lowest-free-slot rule, and the deletion and reorder cases stated as the bug. `somfy-domain/tests/controller.rs` drives a sparse id at both ends of the slot range through the delta cache, which is indexed by the raw id. **Not observed on hardware** — nothing calls it yet |
| ESP32-S3 CC1101 pin map (SCK=12, MOSI=11, MISO=13, CSN=10, GDO0=3, GDO2=4) | `crates/firmware/src/chip.rs::pins` (chip-s3 module) | — (not derived; read directly off a running board) | Hardware-verified against a real working ESP32-S3 device on 2026-08-15 (see "Hardware-verified values") |
| ESP32 and ESP32-C3 CC1101 pin maps | `crates/firmware/src/chip.rs::pins` (chip-esp32, chip-c3 modules) | the reference firmware's per-chip default pin assignments, one board-configuration table per chip | Not verified on hardware. Only the ESP32-S3 map above has been checked against a real device; treat these two as unverified placeholders until someone confirms them on the corresponding board |
| RMT source clock fixed at 80 MHz with a matching divider of 80 (giving 1 µs ticks) | `crates/firmware/src/chip.rs::RMT_CLOCK_MHZ`, `RMT_CLK_DIVIDER` | the reference firmware's RMT clock configuration, required because the ESP32's RMT peripheral only accepts an 80 MHz source clock | Not independently hardware-verified for this crate; carried over as a constraint of the RMT peripheral itself, consistent with the tick model already exercised by `somfy-rmt`'s test suite |
| `MEMSIZE_BLOCKS` = 2 RMT memory blocks for the TX channel | `crates/firmware/src/radio/rmt_tx.rs::MEMSIZE_BLOCKS` | **Not derived.** Measured: a worst-case 80-bit first frame packs to 95 symbols including its end marker, and one block is 48 symbols on the smallest-block chips. The reference implementation is not a source here — it uses a different transmit mechanism entirely | Measured on the host by sweeping every payload byte value (`somfy-rmt/tests/build_symbols.rs::worst_case_symbol_count_is_pinned_and_fits_max_symbols`, `no_payload_byte_pattern_overflows_the_budget`); enforced at compile time against `esp_hal::rmt::CHANNEL_RAM_SIZE` on all four chip builds. Recorded in `docs/specs/2026-08-15-plan4-firmware-radio-design.md` §5.2. Not confirmed on air |
| One frame per RMT transaction — repeats are separate transmissions, never one concatenated buffer | `crates/firmware/src/radio/rmt_tx.rs::RmtTx::transmit_frame` | A consequence of the frame structure already recorded in the `somfy-rts` table above: the 56-bit form carries its inter-frame gap inside its own pulse train, while the 80-bit form suppresses that gap and re-encodes byte 7 per repeat, so an 80-bit repeat is a *different* frame rather than the same one sent twice | Follows from `render_pulses`' gap suppression and `encode80_byte7`, both already verified in `somfy-rts`. The resulting inter-frame spacing for 80-bit frames is not yet timed on air |
| `TX_SETTLE_US` = 1000 µs between strobing the radio into TX and clocking the first RMT symbol — **not derived** | `crates/firmware/src/radio/rmt_tx.rs::TX_SETTLE_US` | Nothing: a consequence of this project's own `MCSM0.FS_AUTOCAL` choice (see the `somfy-cc1101` table above). `STX` from IDLE calibrates the synthesiser before enabling the transmitter, `Cc1101::set_tx` returns as soon as the strobe is on the wire, and no carrier is keyed until calibration finishes | **Margin choice, not a measurement**, in the same spirit as `RESET_SETTLE_NS`: comfortably beyond the calibration and PLL-settling figures the datasheet quotes, and paid once per transmit burst rather than per frame. Not checked on a scope. Without it, the leading edge of the 10920 µs wake-up pulse is transmitted into a radio that is not yet radiating, shortening the frame with nothing reporting it |
| TX channel idles with the data pin driven **low** | `crates/firmware/src/radio/rmt_tx.rs::tx_channel_config` | Not derived: in asynchronous serial mode the CC1101 keys its carrier to follow the data pin, so leaving the pin at whatever level the last symbol ended on would hold the carrier up between frames | Not confirmed on air; a reading of the modulation mode this project configures the radio into |
| RX `MEMSIZE_BLOCKS` = 2 RMT memory blocks, and `RX_SYMBOLS` derived from that reservation rather than written down | `crates/firmware/src/radio/rmt_rx.rs::MEMSIZE_BLOCKS`, `RX_SYMBOLS` | Not derived. Same block count and same reasoning as the TX row above (a worst-case frame is 95 symbols; one block is 48 on the smallest-block chips). The buffer length is `MEMSIZE_BLOCKS * esp_hal::rmt::CHANNEL_RAM_SIZE` because the ESP32 cannot wrap a reception around the end of channel RAM and esp-hal rejects a longer buffer outright | Compile-time assertion that `somfy_rmt::MAX_SYMBOLS` fits, on all three chip builds. **Not confirmed on air** — no reception of any length has yet been taken with this firmware |
| RX glitch filter left **off** (`filter_threshold = 0`) | `crates/firmware/src/radio/rmt_rx.rs::rx_channel_config` | Not derived. The field is a `u8`, so under any plausible reading of its unit it reaches at most a few hundred microseconds — short of the ~448 µs glitch class a real capture of this protocol contains (see `somfy-rts/tests/fixtures/README.md`), and not wideable to reach it | **A deliberate non-choice, not a measurement.** Enabling it would add an unobserved hardware behaviour in exchange for filtering nothing the decoder does not already reject as an out-of-family duration. Worth revisiting during on-air bring-up, with a measurement |
| CC1101 SPI clock of 4 MHz | `crates/firmware/src/main.rs::SPI_HZ` | Datasheet bound, not the reference implementation: the part accepts 10 MHz for single-byte access but only 6.5 MHz for the burst reads `somfy-cc1101` uses for status registers and the PA table | Margin choice below the tighter of the two datasheet limits. Not measured |
| RMT channel numbers: transmit on channel 0 everywhere, receive on channel **4** for the ESP32-S3 and channel **2** for the ESP32 and ESP32-C3 | `crates/firmware/src/chip.rs::rmt_channels!` | Derived from `esp-metadata-generated`'s per-chip RMT channel tables, which say which channel indices exist in which direction (ESP32: either direction on any channel; S3: 0-3 TX, 4-7 RX; C3: 0-1 TX, 2-3 RX). The receive channel is never the one immediately after the transmit channel because `memsize = 2` makes channel 0 own block 1 as well | Enforced by the type system on all three builds — `RxChannelCreator` simply is not implemented for a channel that cannot receive — and confirmed at run time on an ESP32-S3, where the receiver armed without reporting `InvalidDataLength`. **No reception has been decoded on air** |
| A/B app slots `ota_0` at 0x10000 and `ota_1` at 0x210000, **0x1F0000 = 2,031,616 bytes each**; `otadata` data/ota at 0x206000, 8 KB; table ends at 0x400000 | `crates/firmware/partitions.csv`, enforced by `crates/firmware/build.rs` | **Derived, and the size is forced rather than chosen.** `rollcode` may not move — it holds the rolling codes of motors in service — so the first app slot can only run from 0x10000 (the first byte above the ESP-IDF boot flow) to 0x200000 (where `rollcode` starts), which is 0x1F0000 and cannot be larger. A/B slots must be equal, or an image that fits the larger one bricks the device on the following update, so 0x1F0000 is both. `ota_1` then falls on 0x210000, the first 64 KB boundary above the data regions plus `otadata` — 64 KB because that is the flash MMU page size and `esp-idf-part` rejects anything else — and 0x210000 + 0x1F0000 = 0x400000 exactly, so the table still fits a 4 MB board. `otadata` is 0x2000 because `esp_bootloader_esp_idf::ota::Ota::new` refuses any other size. **No `factory` partition**, because a blank OTA record prefers `factory` over both slots when one exists, which would make every update silently never run | Sizes measured on the host with `espflash save-image` on 2026-08-17 — ESP32 **632,640** bytes, ESP32-S3 **585,376**, ESP32-C3 **581,040**, against the 2,031,616-byte slot, worst case 31.1% — and the table decoded back out of a `save-image --merge` image, confirming `ota_0` at 0x10000, `rollcode` still at 0x200000 and the app header landing in `ota_0`. **Not yet flashed to a device**: hardware validation is the owner's, deliberately, with a `rollcode` backup taken first |
| The radio is put into RX after `init` and after every burst | `crates/firmware/src/radio/air.rs::Air::listen`, `key_off`; `somfy-cc1101/src/lib.rs::Cc1101::set_rx` | Datasheet: `SRX` (0x34) is the receive strobe, and in asynchronous serial mode the packet handler is bypassed, so there is no packet end for `MCSM1.RXOFF_MODE` to act on and reception continues until something strobes the chip out of it | The strobe byte is pinned by `the_mode_strobes_are_srx_stx_and_sidle`. **The claim that reception persists is a reading of the datasheet, not a measurement** — an unstrobed radio and a quiet band look identical, which is exactly why the strobe was missing until now |

| Web-server connection limits designed **against** the reference rather than from it | `crates/firmware/src/api/mod.rs::HTTP_TASKS`, `REST_TASKS_RESERVED`; `crates/firmware/src/api/events.rs::WS_MAX` | **A recorded failure, not a value to copy.** The reference caps WebSockets at five through `WEBSOCKETS_SERVER_CLIENT_MAX`, a library default it never overrides and then re-states twice in its own structures. The sixth client is refused by accepting the TCP connection and dropping it — no HTTP status, no WebSocket close frame, and with `CORE_DEBUG_LEVEL=0` not even a serial line, so the device is completely silent about the condition. Its UI infers the cause from five consecutive unclean closes and then covers the whole single-page app, settings screen included, with a `position: fixed` overlay — which is what turns a connection limit into a lockout, while its REST API on ports 80 and 8081 stays perfectly reachable. A slot is also held for 30+ seconds after a tab dies, since its heartbeat is 20 s ping / 10 s pong / 3 misses | Read 2026-08-17. **Ours differs on all three axes and each is checked rather than argued:** a refused WebSocket answers `503` with `Retry-After`; `REST_TASKS_RESERVED = HTTP_TASKS − WS_MAX` is asserted `>= 2` at compile time, so WebSockets cannot consume REST capacity; and our own UI renders the connection state as a `role="status"` pill and never blocks on it. The assertion was proved to fire by raising `WS_MAX` to 3 and observing the build stop |
| No send queue and no backpressure on the reference's WebSocket path | `crates/firmware/src/api/events.rs` | **A failure mode, recorded so it is not reproduced.** The reference's `WebSockets::write` spins against a 5,000 ms TCP timeout inside its single Arduino `loop()` task; `broadcastTXT` iterates all five clients, so a stalled peer can hold the only task for up to 25 s against a 15 s panic watchdog, with no `esp_task_wdt_reset()` anywhere in that path. Its outbound JSON writer shares one 2 KB static across all clients and **silently drops a token on overflow**, then staples a `]` on, producing malformed JSON its own client swallows in a `try`/`catch` | Read 2026-08-17. Ours has no equivalent: each connection is an Embassy task that *awaits* rather than spins, deltas arrive through a `publish_immediate` subscription that drops for a slow subscriber instead of parking the publisher, and the buffer is per-connection |
| Pre-compressed assets served with `Content-Encoding: gzip` | `crates/firmware/build.rs::embed_web_ui`, `crates/firmware/src/api/assets.rs` | The reference gzips its UI at build time and sets the header by sniffing a `.gz` filename suffix, **with no `Accept-Encoding` negotiation at all** — every client gets compressed bytes whether or not it asked | Read 2026-08-17. The compression is adopted; the unconditional part is not. We embed both representations (74,928 bytes identity + 24,763 gzipped = 99,691 of flash, measured 2026-08-17) and choose on `Accept-Encoding`, because the failure otherwise is binary labelled `text/html` to anything that did not ask — `curl` with no flags, which is this project's own debugging tool |
| `DRAM_FOR_STACK_AND_HEAP` re-measured for all three chips, and the web server's cost | `crates/firmware/src/heap.rs` | Not derived from the reference. Measured on the host from the linked ELF by the procedure that constant documents: build with a deliberately small heap, subtract `_stack_start_cpu0 − _stack_end_cpu0`, add the probe heap back | 2026-08-17. **The old figures were stale by 3,520 bytes on every chip**, which is why a boot printed `stack: 63796 bytes available` where they implied 66,788 — a margin of 14,324 rather than the 16,688 the budget buys. New figures: ESP32 **125,116** (with `mqtt`; `http` does not fit), ESP32-S3 **159,908** and ESP32-C3 **146,672** (both with `mqtt` + `ui`). The web server costs about **70,000 bytes** — three measurements of one thing — of which 52,384 is four connection-task futures at 13,096 each (`-Zprint-type-sizes`), 14,336 is their buffers and the rest is four more `embassy-net` socket slots. Resulting stacks 66,748 / 66,724 / 66,800 against a 49,592 requirement, so every chip is back above the intended 16,688-byte margin. Heaps 58,368 / 93,184 / 79,872, leaving 3,944 / 38,760 / 25,448 over the worst peak measured — **the ESP32's is inside this heap's own ~4,216-byte boot-to-boot noise**, and it is a consequence of correcting the stale constant rather than of the web server, which that chip does not carry. **CORRECTION, 2026-08-17: the three DRAM figures in this row are right and the conclusion drawn from them was not.** The 49,592 requirement was itself stale — see the stack-overflow rows below — so "back above the intended 16,688-byte margin" was false on every chip, and on the ESP32-S3 the real margin was **negative by 4,844 bytes**. The DRAM figures were re-checked the same day against relinked images and are unchanged to the byte |
| The ESP32 cannot carry the web server | `crates/firmware/src/heap.rs`, the `compile_error!` beside `DRAM_FOR_STACK_AND_HEAP` | Not derived. Measured, as above | 2026-08-17. With `http` enabled the ESP32 has **54,556 bytes** for a **66,280-byte** stack budget: the image does not link, and `ld` says only `stack.x:11 cannot move location counter backwards`, naming neither the chip nor the feature. It is not a tuning problem — even one connection task instead of four leaves the Wi-Fi driver about 42 KB against a measured 47,464-byte working set. The refusal was proved by building `--features chip-esp32,http` and observing the `compile_error!`, and again on 2026-08-17 after the stack constants were rewritten |
| **The default ESP32-S3 build boot-looped on a stack-guard panic, and inlining is why** | `crates/firmware/src/main.rs::start_network`, now `#[inline(never)]` | **Not derived — diagnosed from the linked ELF and the panic's own saved PC.** `entry` makes two sequential calls, `start` then `start_network`, so their stack costs should alternate rather than add. LLVM inlined the second into `entry`; an inlined callee's locals are slots in the *caller's* frame, allocated in its prologue and live for the caller's whole lifetime, so the web server's bring-up sat underneath the 48,992-byte call to `start` instead of after it. The deepest chain became `main`/`run`/`run_inner` (144) → `TaskStorage<__embassy_main_task>::poll` (22,432) → `start` (19,536) → `tasks::state` (14,720) → `UninitCell::write_in_place` (14,736) = **71,568 against 66,724 bytes of stack**. `#[inline(never)]` takes `poll` to 3,856 and the chain to 52,992 — **18,576 bytes**, all of the overflow and 13,732 to spare | Measured 2026-08-17. `Saved PC:0x40378954` resolves to the **first instruction of `__default_naked_exception`** (`= __naked_user_exception = __naked_kernel_exception`) in `.rwtext`; its next two instructions are `addmi a1, a1, 0xffffff00` and `s32i a0, a1, 12`, so an interrupt entry dropped SP by 256 and stored through esp-hal's guard word at `_stack_end_cpu0 + 60`. The controlled experiment is one commit, one chip, one profile, features only: `--features chip-s3` gave `.stack` = 66,724 and a 71,568-byte chain (dies), `--no-default-features --features chip-s3,mqtt` gave 137,284 and 62,368 (runs), and `http` alone deepened `poll` by 9,200 bytes in a build where no HTTP code can have executed. Frames from `.stack_sizes`; every call edge on the chain read out of `objdump -d` (Xtensa far calls are `l32r` + `callx8`, so they are literal-pool references rather than branch targets); `write_in_place` confirmed a leaf that calls only `memcpy`. **Not yet flashed** |
| `REQUIRED_STACK_BYTES` = 54,720, a `max` over three measured chains rather than one figure | `crates/firmware/src/heap.rs::BOOT_CHAIN_BYTES`, `NETWORK_CHAIN_BYTES`, `REQUEST_CHAIN_BYTES`, `INTERRUPT_FRAMES_BYTES` | **Not derived from the reference; read off linked ELFs, and it replaces 49,592, which was stale.** The boot chain is 53,008 on the worst chip (ESP32; S3 52,992, C3 52,864) and is feature-independent; the network chain is 23,280 with the web server and 14,416 without, which is the only feature-dependent term; the request chain through `picoserve`'s router is 33,504 with the web server and does not exist without it. `INTERRUPT_FRAMES_BYTES` = 1,712 is carried unchanged from the previous derivation (5 × `XT_STK_FRMSZ` 256 + 432 of handler frames) and is now corroborated by the panic above, which fired inside exactly that entry sequence. A `max` rather than a sum because the executor is in one chain at a time, and a `max` rather than a single number so that a route added to `api::routes` or a buffer added to `api::start` changes the requirement instead of being re-derived by hand | Measured 2026-08-17 on this commit, and each figure re-checked against a relinked image after the fix: ESP32 `mqtt` 53,008 / 14,416, ESP32-S3 `mqtt`+`ui` 52,992 / 23,280 / 33,504, ESP32-S3 `mqtt` 52,992 / 13,936, ESP32-C3 `mqtt`+`ui` 52,864 / 12,352. `.stack` regions unchanged by the fix — 66,748 / 66,724 / 66,800 — so `DRAM_FOR_STACK_AND_HEAP` needed no re-measurement and no heap moved. Margins 12,028 / 12,004 / 12,080. **Not yet flashed** |
| `STACK_BUDGET_BYTES` stays 66,280, and a compile-time gate now stops it being outgrown | `crates/firmware/src/heap.rs::STACK_BUDGET_BYTES`, `STACK_MARGIN_FLOOR_BYTES`, and the `assert!` between them | **Not derived; fixed by the ESP32's heap, and the reason is arithmetic.** 125,116 − 66,280 = 58,836 → 58,368 of heap, which is 3,944 over the largest heap high-water yet measured (54,424). That 3,944 is the entire slack in the design and it is inside the heap's own ~4,216-byte boot-to-boot noise, so the division cannot move without hardware. The old spelling `49_592 + 16_688` had **both terms wrong and the sum right**: the margin this division buys was, and remains, 66,280 − 54,720 = 11,560. Keeping the sum is what keeps every heap figure ever measured against it valid. `STACK_MARGIN_FLOOR_BYTES` = 8 KiB is a **policy figure and is labelled one** — it reserves what no derivation can see, the closed Wi-Fi driver's interrupt-handler bodies, which carry no stack-size metadata | The gate was **proved to fire in both directions on 2026-08-17** and restored each time: `BOOT_CHAIN_BYTES + 3_369` (one byte past the 3,368 of slack the floor leaves) failed the build with the assertion's own message, and so did raising `STACK_MARGIN_FLOOR_BYTES` to 12 KiB. All seven CI configurations then pass clippy and build. **Not yet flashed** |
| The stack's high-water mark is **painted and read back on every boot**, not derived once | `crates/firmware/src/main.rs::paint_stack`, `stack_used`, `report_stack_use`, `STACK_PAINT`, `PAINT_HEADROOM_BYTES` | **Not derived — it exists because the derivation is what failed.** No crate in this stack offers it: esp-hal and esp-rtos have stack-overflow *detection* (guard value, pointer-range check, hardware watchpoint) and no watermark, and `embassy-executor` has none either, so there was nothing to adopt. `entry` paints the unused stack with `0xA5A5_5A5A` before anything else runs and `report_stack_use` prints how far down the paint was destroyed. Both bounds come from the linker's own symbols and are checked rather than assumed: the floor is one word above esp-hal's `__stack_chk_guard`, which is watched by hardware and must not be written, and the ceiling is `PAINT_HEADROOM_BYTES` = 4 KiB below a local of the painting frame — so an interrupt taken mid-loop lands above everything it touches. If the layout is not the one assumed, nothing is painted and nothing is claimed | The figure it prints is a **floor**: the shallowest 4 KiB is never painted, and live data equal to the pattern reads as virgin. Both err toward reporting less depth. The reading of the stack pointer is deliberately *not* `core::arch::asm!` — that is unstable on Xtensa, and `impl_trait_in_assoc_type` is meant to stay this crate's only unstable language feature; the address of a local in an `#[inline(never)]` function whose frame measures 48 bytes is exact enough against a 4 KiB reserve. **Not yet flashed — this is the row the orchestrator's next boot turns into a measurement**, and the expected reading on an ESP32-S3 is close to 52,992 + whatever the driver's interrupts add |
| The device answers mDNS for `<hostname>.local` and advertises `_http._tcp` on port 80 | `crates/firmware/src/mdns.rs`, `crates/firmware/src/identity.rs` | the reference firmware's network bring-up, which starts an mDNS responder on its configured hostname and registers an `http`/`tcp` service on port 80 alongside its own private service. The *service* choice is taken from it; the *name* is not — the reference defaults its hostname to a fixed product string, so two of its devices on one network claim the same name, and this firmware derives the name from the factory MAC instead | Read 2026-08-17. Adopted: the `_http._tcp` registration and the port, which is `crate::api::PORT` rather than a second literal 80, so the advertisement cannot drift from what the server binds. Rejected: the fixed default hostname. Its extra private service and its `serverId`/`model`/`version` TXT records are **not** ported — nothing discovers them, and each is a record in a response buffer whose overrun ends the responder. **Not yet observed on a network**: no `.local` name has been resolved by any client |
| The mDNS hostname is `somfy-` plus the eFuse MAC, hex-encoded — the same twelve characters as the MQTT `device_id` | `crates/firmware/src/identity.rs::hostname`, `mac_hex` | Not derived. It is R4's stable-identity requirement reached from a second direction: a bookmark to `http://<name>.local` that stops resolving is as bad as a `unique_id` that moves, and the MAC is the only value on this board that survives a reboot, a configuration change and a firmware update. Deliberately the **whole** MAC rather than the low three bytes, so an operator can match a browser tab against a Home Assistant device page by eye — a short form would be a second identifier, which is the thing this module exists not to invent | Validity is established at compile time rather than checked at run time, because there is no second name to fall back to: `is_hostname_label(PREFIX)` and `HOSTNAME_LEN <= 63` are `const` assertions (RFC 1035 §2.3.1 as relaxed by RFC 1123 §2.1), and the MAC half is legal by construction since `{:02x}` emits only `0`-`9` and `a`-`f`. **Both proved to fire on 2026-08-17** by writing `PREFIX = "somfy-"` and by adding 50 to `HOSTNAME_LEN`, then restoring. **`crate::mqtt::device_id` still computes the same twelve characters separately** and should call `identity::mac_hex`; the values are identical, so the two cannot disagree about what this device is |
| **No conflict probing**, which RFC 6762 §8.1 requires of a responder | `crates/firmware/src/mdns.rs` | Not derived. `edge-mdns` does not probe and this firmware does not add it. That is sound only because the name is derived from a globally unique hardware identifier: a collision needs a second device with this board's MAC | Recorded as a **deviation with a stated precondition**, so that the precondition is visible when it stops holding. The day the hostname becomes user-supplied — which is Plan 6 Task 2's config record — probing becomes a real obligation, and it is noted in `identity.rs` as well as here |
| Unsolicited mDNS announcements: three, at 0 s, 1 s and 3 s after a DHCP address arrives or changes, and **never on a timer** | `crates/firmware/src/mdns.rs::ANNOUNCEMENTS`, `FIRST_ANNOUNCE_GAP_MS` | RFC 6762 §8.3: at least two unsolicited responses one second apart, up to eight, with the interval at least doubling. Three is the minimum plus one, which buys tolerance of one dropped packet without spending more of a shared multicast group's attention than a device with nothing urgent to say deserves | Structural rather than asserted: `edge-mdns`'s broadcast half blocks on an `embassy_sync::Signal` that only `crate::mdns` can raise, so "the complete list of unsolicited packets is this burst" is a property of who holds the signal rather than a promise about a timer. The responder announces once on entry and the burst raises the signal twice more. **Not yet observed on a network** |
| mDNS record TTL 120 s, one value for records RFC 6762 §10 gives two | `crates/firmware/src/mdns.rs::TTL_SECONDS` | RFC 6762 §10: 120 s for records naming a host (A, and SRV because it contains one), 75 minutes for everything else. `edge-mdns` has a single TTL, so one had to be chosen | Chosen for the A record, because that is the one whose staleness a person meets — a moved DHCP lease and a cached address that reaches nothing, or somebody else. The cost is that the PTR and TXT records are re-queried far more often than they need to be, and it is bounded: §5.2 cache-maintenance queries come from queriers at 80-95% of the TTL and only while something is actively browsing, so a network with nobody browsing pays nothing. **Not measured on a network** |
| mDNS buffers: 1,024 bytes out, 512 in, and the asymmetry is deliberate | `crates/firmware/src/mdns.rs::RESPONSE_BYTES`, `REQUEST_BYTES` | Derived, and the derivation is arithmetic over the records this device owns. The largest message is the answer to a `_http._tcp` browse: header 12, A 40, the DNS-SD meta-PTR 56, the service PTR 72, SRV 87, TXT 65 — **332 bytes uncompressed**, and `domain` compresses names, so the real message is smaller | The margin is 3x rather than tight because **overrunning the send buffer is fatal rather than lossy**: `domain`'s builder answers a full buffer with a `PushError`, `edge-mdns` propagates it out of `Mdns::run`, and the responder ends for good. `RESTART_DELAY_MS` is the safety net and this figure is what keeps it unneeded. The receive side is smaller because overrunning *it* is not fatal — a truncated message fails to parse and is skipped, and what that costs is not appearing in a `_services._dns-sd._udp` enumeration that carried many known-answer records. **Neither figure has been exercised against a real querier** |
| SNTP server defaults to `pool.ntp.org` | `crates/firmware/src/sntp.rs::SERVER` | the reference firmware's NTP settings, whose `ntpServer` field defaults to the same pool and which it hands to the platform's own SNTP client at UTC with no offset | Read 2026-08-17. Adopted because there is no better answer available to a device that ships without knowing which network it will live on: it is a name rather than an address, so it survives servers being retired. **This is what put a resolver in the image** — see the `embassy-net/dns` row below. The reference's companion `posixZone` field is not ported: this firmware keeps UTC seconds and has no display layer to localise for |
| SNTP schedule: one exchange an hour, retries from 60 s to a 1 h ceiling, each attempt abandoned after 5 s | `crates/firmware/src/sntp.rs::RESYNC_INTERVAL_S`, `RETRY_MIN_MS`, `RETRY_MAX_MS`, `REQUEST_TIMEOUT_S` | **Policy figures inside a range a measurement permits, and said so.** The drift bound is the crystal's: at its ±20 ppm specification an hour is 72 ms and a day is 1.7 s, both far inside what a log timestamp or a TLS validity window needs — so the drift argument permits a day and the hour is chosen for a different reason, that it bounds how long a single bad answer persists. The 60 s floor is four times RFC 4330 §10's "MUST NOT under any conditions use a poll interval less than 15 seconds". The 5 s timeout exists because **`sntpc` has none of its own** and `get_time` would otherwise await a datagram a firewalled port 123 never sends | The floor is guarded at compile time — `RESYNC_INTERVAL_S * 1_000 >= RETRY_MIN_MS`, **proved to fire on 2026-08-17** by writing 10 and restoring — because it is the constant most likely to be tuned by somebody wanting the clock to settle faster. Kiss-o'-Death (RFC 5905 §7.4) sends the backoff to its ceiling rather than stopping for the boot, because the name is re-resolved each round and the pool hands out a different member. **Not yet observed against a server** |
| Wall-clock time is an **anchor plus monotonic elapsed time**, never a substitute for `Instant` | `crates/firmware/src/sntp.rs::Anchor`, `WALL_CLOCK`, `unix_seconds` | Not derived; it is the one correctness rule this feature could break. An SNTP correction can move the wall clock *backwards*, and the rolling-code debounce, the receive dedupe and the position estimator all measure durations. Four things keep them apart, and all four are structural rather than careful: the reading is only ever absolute — there is no `elapsed`, no `duration_since` and no `Sub` in the module, so a caller wanting an interval has to write the subtraction where the question is visible; it is `Option` and there is no default epoch; it is stored as `(unix_seconds, Instant)` so the monotonic clock is *underneath* the wall clock rather than beside it, and a correction replaces the anchor without touching what anything else measures; and **with the `sntp` feature off there is no wall clock in the image at all** | Nothing in this firmware calls `unix_seconds` today — the consumers are Plan 6 Task 6's TLS validity and the log timestamps — so the separation is checked by the compiler rather than by a test: the CI row `--no-default-features --features chip-s3,sntp` builds the clock with no other network service present, and every other row builds the rest of the firmware with no wall clock at all. **Not yet observed on hardware** |
| `sntpc`'s era pivot is the device's own wall clock, not uptime | `crates/firmware/src/sntp.rs::Pivot` | **Not derived — read out of `sntpc` 0.11's source, and it is why its own `sntpc-time-embassy` was rejected.** `NtpTimestampGenerator::timestamp_sec` is also the pivot `reconstruct_timestamp` picks an NTP era from, choosing whichever of the three eras nearest it best fits the wire value, *silently*: the crate's `test_reconstruct_timestamp_uses_bad_pivot_wrong_era` asserts that a stale pivot yields a plausible wrong answer with no error raised | **Sized honestly rather than dramatised.** A uptime pivot sits near the epoch, and with era-0 candidates below 1970 skipped the reconstruction stays *correct until roughly 2106* — so this is exactness, not urgency. Ours uses the wall clock once there is one and uptime before that, so the first exchange of a boot matches the crate's behaviour and every later one is exact. `sntpc-time-embassy` cannot be given a better pivot: private field, `Default` impl, no constructor |
| `embassy-net/dns` and one DNS socket, turned on by the `sntp` feature rather than unconditionally | `crates/firmware/Cargo.toml`, `crates/firmware/src/net.rs::resolve`, `SNTP_SOCKETS` | Not derived. Forced by the row above: the only sane default NTP server is a name. The resolver is a real cost — `embassy_net::new` adds a DNS socket to the set before any of this crate's code runs, and its query storage is a hard-coded `MAX_QUERIES = 4` of `dns::DnsQuery` that no feature reaches — so it is gated on the one feature that needs it and a radio-only build pays nothing | `net::resolve` bounds the lookup at 10 s, takes the first `A` answer, and filters to IPv4 — the last being a **guard rather than a preference**, since `sntpc-net-embassy` answers an IPv6 `SocketAddr` with `unreachable!()` and this firmware's panic handler resets the board. `dns_query` short-circuits a name that parses as an address literal, so a configured IP costs no packet. **This is also what makes `somfy_config::MqttSettings`'s broker able to become a hostname** — a field change and one call, rather than a feature and a socket budget. **Not yet resolved anything on a network** |
| The ESP32 is refused `sntp`, as it is already refused `http` | `crates/firmware/src/heap.rs`, the second `compile_error!` | Not derived. Measured, by disabling the refusal, building `--no-default-features --features chip-esp32,mqtt,sntp` and reading `.stack` back | 2026-08-17. DRAM 121,708 → heap 55,296 against the 54,424-byte measured peak: **872 bytes**, inside the heap's own ~4,216-byte boot-to-boot spread, and against a peak that was itself measured on an ESP32-S3. Unlike the `http` refusal this one **links, boots and runs** — the symptom would be a board that reboots on some boots and not others — which is why it is a `compile_error!` rather than left to the linker. The loss costs that chip nothing it can use: both consumers of a wall clock are things it cannot run either. Two-thirds of the cost is the resolver, so **what would change this** is an NTP server address in the config record instead of a name. Proved to fire on 2026-08-17 |
| **`REQUIRED_STACK_BYTES` re-derived to 56,384, and the runtime measurement agreed to 8 bytes** | `crates/firmware/src/heap.rs::BOOT_CHAIN_BYTES` | Not derived from the reference. Re-read off linked ELFs after the shade add-and-pair work merged, because three of the boot chain's four frames had grown again: `crate::start` by 640 bytes and the two state-task frames by 512 each, **+1,664** | **This is the row that turns the stack budget from a claim into a measurement.** A flash of the pre-Task-7 image printed `stack: 54,648 bytes used at the deepest point of boot` against the **54,656** the re-read table computes for that chip — 8 bytes apart, and the 8 is the paint's own resolution (`stack_used` scans word-aligned and stops at the first surviving pattern). **What that settles:** nothing on the boot path is invisible to a `.stack_sizes` walk. If the closed Wi-Fi driver, masked ROM or an unmodelled inlining were being touched before `report_stack_use` runs, the painted figure would *exceed* the derived one rather than fall short of it. The growth was real, was entirely accounted for, and what had gone stale was only the constant. It says nothing about `STACK_MARGIN_FLOOR_BYTES`, which stands behind handler bodies that run *after* boot and which neither measurement reaches. New chains: ESP32 54,672, ESP32-S3 54,656, ESP32-C3 54,544; the worst is taken, plus 1,712 of interrupt frames |
| `DRAM_FOR_STACK_AND_HEAP` re-measured a third time, and the ESP32's heap slack is down to 2,920 bytes | `crates/firmware/src/heap.rs` | Not derived. Measured from linked ELFs by the procedure that constant documents, with the two causes separated rather than summed | 2026-08-17. The shade add-and-pair work cost 512 bytes on the ESP32 and 1,984 on each of the other two **without this row being re-read**, which is the failure the row's own warning describes. Plan 6 Task 7 then cost the ESP32-S3 **7,824** and the ESP32-C3 **7,784**, measured as the difference between `--features chip-s3` and `--no-default-features --features chip-s3,mqtt,ui` on one commit. New figures ESP32 **124,604**, ESP32-S3 **150,100**, ESP32-C3 **136,904**; heaps 57,344 / 82,944 / 69,632; stacks predicted and then confirmed against relinked ELFs to the byte at **67,260 / 67,156 / 67,272**. **The ESP32 lost a whole kilobyte of heap to 512 bytes of statics**, because the division rounds to a whole KiB — the safe direction, and still a kilobyte off the chip with the least to spare. Its free-at-peak has gone 3,944 → **2,920**, well inside the ~4,216-byte noise. Two things soften that in both directions: the 54,424 peak is an ESP32-S3 reading, and no ESP32 has ever been booted with this firmware. It is a paper margin that is now too thin to reason from, rather than one shown to fail. **Flash, separately and not scarce:** the two services cost **54,896 bytes** of image (ESP32-S3 893,584 → 948,480, measured with `espflash save-image` on one commit), which is 2.7% of the 2,031,616-byte OTA slot and takes it to 46.7%. Most of that is `domain`, which composes and parses every DNS record type behind `AllRecordData` |

---

## Inline exceptions

Comments that still name the reference implementation, with justification.
Every entry here is a deliberate decision, reviewed. An inline reference that
is not listed here should be removed.

| Location | Why it must stay |
|---|---|
| **`crates/somfy-migrate/**` (whole crate)** | The crate reads backup files *produced by* the C++ ESPSomfy-RTS firmware. That codebase is the subject matter, not a source of borrowed knowledge — naming the routine that wrote a field describes the input, and removing it would obscure the single most important fact about the data. Scoped to this crate only; see [somfy-migrate](#somfy-migrate). |

No other exception has been needed. The four protocol/domain/API crates were
cleaned with **zero** retained references: every citation there proved
restatable in this project's own terms.

---

## Hardware-verified values

Values confirmed against real hardware, which outrank any derivation:

| Value | Evidence | Date |
|---|---|---|
| 56-bit first frame emits 2 hardware syncs; repeat emits 7 | Captured wall-remote frames reported `hwsync` 4 and 14 half-pulses respectively | 2026-08-15 |
| ESP32-S3 CC1101 pin map (SCK=12, MOSI=11, MISO=13, CSN=10, GDO0_TX=3, GDO2_RX=4) | Read directly off a running production ESP32-S3 board's wiring | 2026-08-15 |
| Wake-up pulse ≈ 10.9 ms HIGH | First pulse of a real capture measured 10229 µs | 2026-08-15 |
| Post-wake-up silence ≈ 17.7 ms LOW — **not** the 7357 µs `WAKEUP_LOW` this project transmits | Second pulse of all three real captures: 17738 / 17722 / 17711 µs. It is the longest LOW inside a real frame, and the only one above the hardware-sync family | 2026-08-15 (measured from the committed captures 2026-08-16) |
| Backup file format and record layout | Parsed a real v25 device backup with zero field misalignment | 2026-08-15 |
| Target radio configuration: 433.42 MHz, deviation 47.6, rxBandwidth 99.97, txPower 11, 56-bit frames | Read off a live production ESP32-S3 + CC1101 unit then controlling three shades, reporting `radioInit: true` | 2026-08-15 |
| A committed rolling code survives reset, a full reflash, and a ring wrap | `store-check` on a spare ESP32-S3 counted from `RollingCode(1)` to `RollingCode(43)` across ~45 reboots without ever restarting: the ring wrapped at the 33rd commit (`valid` 32 → 17 as a sector was erased, `damaged` 0 throughout), an `espflash flash` mid-run did not disturb it, and swapping the partition table out and back left the region intact. Procedure in `docs/hardware-checklist.md` | 2026-08-16 |
| A half-written record is rejected, and the commit after it steps over the wreckage | A sector image with a complete record in slot 0 and the first 64 bytes of one in slot 1 was written to the region directly. The store reported `valid=1 damaged=1`, loaded the **complete** record's code rather than the torn one, and committed into slot 2 | 2026-08-16 |
| Unconfigured AGC makes the receiver slice its own noise floor: **~750 edges/s on GDO2 with nothing transmitting**, line high ~70% of the time | A temporary bring-up binary — deliberately **not committed**, unlike `store-check` and `tx-check` — sampling the GDO2 pad directly as a plain GPIO input on a spare ESP32-S3, bypassing RMT entirely, with the radio confirmed in RX (`MARCSTATE` = 0x0D). Measured with `AGCCTRL2`/`AGCCTRL1`/`AGCCTRL0` written explicitly to their datasheet **reset** values 0x03/0x40/0x91 — i.e. the state this crate left them in — as 1461, 1598 and 1572 edges per 2-second window. This is also the state DN022's recommended range describes, so following the app note verbatim would not have fixed it | 2026-08-16 |
| Capping DVGA gain silences it: **~0.6 edges/s, line resting low** | The same uncommitted binary on the same board, unchanged, with `AGCCTRL2` = 0x83 now written by `Cc1101::init`: 36 consecutive 1-second windows gave 22 edges in total, 22 of the 36 windows completely flat, `high` = 0% throughout. Mean gap between events is seconds, against the 22 ms of quiet `somfy-rmt`'s idle threshold needs. Sweep behind the choice, per 2-second window at `MAX_LNA_GAIN` = 0 and `MAGN_TARGET` = 3: `MAX_DVGA_GAIN` 0 → 1461/1598/1572, 1 → 58/104/78, 2 → 0/2/0, 3 → 0/0/0 | 2026-08-16 |
| **A silent idle band does not mean a receivable one, and ranking AGC settings by idle noise picked the worst of them.** `AGCCTRL2`/`AGCCTRL0` = 0x83/0x91 receives whole frames only from a strong transmitter; attenuate it and the frame body is lost while the wake-up pulse still arrives — the exact symptom that prompted this work | A second uncommitted binary on the spare ESP32-S3, again sampling GDO2 as a plain GPIO input with RMT bypassed, stepping the three AGC registers between measurements and firing a real transmission at each setting from a second, known-good ESPSomfy-RTS device over HTTP. Every capture is backed by a **verified shade movement** (position 0↔100) and judged by running the recorded pulses back through this project's own `somfy_rts::rx::RxDecoder` + `decode56`: a capture counts only if it yields a **checksum-valid 56-bit frame**. Decoded frames carried the transmitter's real address with rolling codes advancing 96→155 across the runs, and commands matching what was fired. Distance was simulated by writing `FSCTRL0.FREQOFF` to walk the signal out of the 101.6 kHz channel filter (1586.9 Hz per count) — a test-time attenuator only, never written by `init`. Complete frames decoded, three attempts per cell: <br>`offset` → `38 kHz / 51 kHz / 64 kHz / 76 kHz` <br>`0x83/0x00/0x91` (old) → **0/3, 0/3, –, –** <br>`0xC7/0x00/0xB2` (new) → **3/3, 3/3, 3/3, 3/3** <br>`0x47/0x00/0x91` → 2/3, 3/3, 2/3, 3/3 <br>`0x87/0x00/0x91` → –, –, 1/3, 2/3 <br>Unattenuated, every one of these decoded 4/4, which is why the earlier quiet-room sweep could not rank them | 2026-08-16 |
| `AGCCTRL2`/`AGCCTRL1`/`AGCCTRL0` = **0xC7/0x00/0xB2** leaves the band silent *and* the line resting low | Sixteen 2-second idle windows across three runs, radio in RX, nothing transmitting: **0 edges and 0‰ high in every one**. Confirmed independently by the original `rx_raw` diagnostic against the committed configuration — 150 consecutive 200 ms windows over 30 s produced **no window with more than 4 edges**. The line-resting-low half is measured, not inferred, precisely because the setting this replaced was rejected for pinning the line high while producing no edges at all | 2026-08-16 |
| A **complete** 56-bit Somfy frame is captured in a single 200 ms window: `w240: edges=92 longest_high=10240us longest_low=29468us` | `rx_raw`, unmodified, running the committed 0xC7/0x00/0xB2 configuration, during a transmission confirmed by the shade travelling 100 → 0. 92 edges with a 10240 µs wake-up HIGH. **92 edges is a whole frame, not a fragment:** this repository's own golden capture of a real device reports `pulseCount=89` for a 56-bit frame (`somfy-rts/tests/fixtures/up_56bit_1.pulses`), so an edge count in the high 80s to low 90s is the complete article. The two neighbouring firings split across a window boundary (70+20 and 73+19 edges) purely because `rx_raw`'s windows free-run rather than triggering on the frame | 2026-08-16 |
| Wi-Fi heap high-water mark **under real MQTT traffic**: worst peak **55,040 bytes** of a 57,344-byte heap across fourteen boots; steady use **47,464**, unchanging for the life of a boot | `firmware` on the spare ESP32-S3 against a real access point and a real broker, sampled every five seconds by a throwaway instrumented build (**not committed**, like the earlier `rx_raw` diagnostics). **This supersedes the 46,660-byte mark this row replaces**, which was taken on 2026-08-16 with association *failing* and which `heap.rs` itself flagged as owing a re-measurement: the real figure is 8,380 bytes higher. Three things the single earlier reading could not show. **The peak is transient and it is the announcement** — it is reached within a second of the broker's CONNACK, where `heap: network up` a moment earlier reads about 49,000, and never moves again; so the picture is 9,880 bytes free at rest with a momentary trough of 2,304, not a heap sitting 96% full. **The peak is noisy** — fourteen boots of one unchanged image spanned 50,824 to 55,040, a 4,216-byte spread that depends only on how many dynamic RX/TX buffers are in flight at once, which is the bar any future heap comparison has to clear. **There is no leak, byte-exact** — with `internal-heap-stats`' running totals printed alongside the usage, `total_allocated - total_freed` equalled `current_usage` at every sample through 6.4 MB of allocation | 2026-08-17 |
| **Frame aggregation is not the cause of the transient peak, and disabling it is worth 316 bytes** | The standing hypothesis was that the ~5.4 KB spike at session start is AMPDU / Block-Ack reorder state, and a single-boot control appeared to confirm it. It does not survive replication. Fourteen boots with `ampdu_rx_enable`/`ampdu_tx_enable` at their default `true` against seventeen with both set `false` — same image otherwise, same access point, same broker: steady use **47,464 vs 47,148**, worst peak **55,040 vs 54,676**, best peak **50,824 vs 50,556**, allocator churn **~29,800 vs ~27,500 B/s**. The 316-byte steady saving is real and appears on every boot; the 364-byte worst-case difference sits inside the 4,216-byte boot-to-boot spread and is not a result. The spike survives with aggregation off on most boots, which is what rules Block-Ack state out. **The change was therefore not taken**: the `with_ampdu_*` setters are `#[builder_lite(unstable)]` — expanded by the derive to `#[cfg(feature = "unstable")]` — so reaching them means enabling `esp-radio/unstable`, which widens the API surface and, on `chip-esp32` alone, adds an ADC2 claim to `esp_radio::init` that panics if esp-hal holds it. Not a trade worth 316 bytes | 2026-08-17 |
| **The Wi-Fi country code `CN` is kept, and the two-letter code does not select the channel range** | `ControllerConfig::default()` sets `CountryInfo::from(*b"CN")`, and what reaches the driver via `esp_wifi_set_country` is `schan: 1, nchan: 13, max_tx_power: 20` with `WIFI_COUNTRY_POLICY_MANUAL` — channels 1-13 at 20 dBm, unconditionally. That is exactly the EU allocation this device is deployed under, so the behaviour is correct; it is correct **by inheritance**, since nothing in this repository chose it. Read `CountryInfo::into_blob` (`esp-radio-0.18.0/src/wifi/mod.rs:2019`): only `cc` comes from the two-letter code, while `schan`, `nchan` and `max_tx_power` are literals carrying esp-radio's own `TODO` that they ought to be configurable. So `.with_country_info(*b"DE")` would change the string and not the channels, and esp-radio 0.18 exposes no way to change them — a "correction" from `CN` to a European code would be cosmetic while reading as regulatory. **Deployment-dependent, not universally safe:** under FCC rules channels 12 and 13 are not permitted and no setting here would exclude them, so a board for that domain needs a change in `esp-radio`. Confirmed as a deliberate decision by the owner, not an oversight | 2026-08-17 |
| **An associated link with no application traffic churns ~30,000 bytes a second through the allocator** | `internal-heap-stats`' `total_allocated` climbed by 549,948–595,368 bytes per 20 seconds, indefinitely, while `current_usage` never changed by a byte — the Wi-Fi driver allocating and freeing a buffer per frame for beacons, ARP and the access point's own chatter. It is recorded because of what each costs: `esp-alloc` takes a critical section per allocation and per free, and `internal-heap-stats` lengthens it, so an idle link masks interrupts several hundred times a second. `heap.rs` correctly says nothing on the frame path allocates; it did not say that something else allocates at this rate with interrupts off, while `net.rs` argued carefully about `esp_println` holding one critical section per log line. The asymmetry was an accident. Not observed to matter — RMT clocks transmissions out of its own RAM and timestamps receptions in hardware — and now stated | 2026-08-17 |
| **`esp_rtos::start` allocates nothing**, contradicting the premise `SCHEDULER_HEAP_BYTES` rested on | `heap::report` called at three points of `main::start` on the spare ESP32-S3: `0 of 57344 bytes used, peak 0` immediately after `esp_rtos::start`, still 0 after both radio tasks are spawned, and still 0 on entry to `net::start`. The first byte is taken by `esp_radio::wifi::new`. The source agrees: `task::allocate_main_task` writes into fields of the static `SCHEDULER` and adopts the existing main stack as a slice of `_stack_end_cpu0 .. _stack_start_cpu0` (`esp-rtos-0.3.0/src/lib.rs:321-334`), and `Task::drop` frees only when `heap_allocated`, which only `SchedulerState::create_task` sets. What `start` *does* do is install `__getreent`, `_malloc_r` and `_free_r` into the ROM syscall table (`src/syscall.rs:111`), so ROM-side allocation lands in this heap lazily. **The 4 KiB was kept and the rationale replaced**: the binary that needs it, `tx-check`, keys the transmitter, so the high-water-mark method every other figure here uses is unavailable for it | 2026-08-17 |
| ~~Main stack per chip after Plan 5 Task 4: ESP32 71,092, ESP32-S2 14,644, ESP32-S3 176,444, ESP32-C3 163,200~~ | **Superseded twice and kept only as history.** These were read at the shared 56 KB heap, before `RADIO_HEAP_BYTES` became per chip; the ESP32-S2 column describes a chip that is no longer built at all. See the row below | 2026-08-17 |
| Main stack per chip at the **per-chip** heap: ESP32 **66,908** (60 KiB heap), ESP32-S3 **66,788** (163 KiB), ESP32-C3 **66,856** (150 KiB) | Read from `_stack_start_cpu0 − _stack_end_cpu0` in each release ELF with `nm`, then confirmed at run time on the spare ESP32-S3, which printed `stack: 66788 bytes available, 49592 required` — the ELF figure exactly. **This supersedes every earlier row of this shape** (56 KB heap: 71,004 / 176,356 / 163,112 measured the same day, themselves 32 bytes off the 71,036 / 176,388 / 163,144 recorded before them). The three now sit within 120 bytes of each other by construction rather than by luck: each is `DRAM_FOR_STACK_AND_HEAP` minus a heap rounded down to a whole KiB, so the spread is only the rounding residue. A table like this goes wrong silently every time a static is added, which is why `heap.rs` carries the commands that regenerate both it and the DRAM figures it derives from | 2026-08-17 |
| The controller's panic handler **resets the board, and the message survives the reset — but only because it waits 100 ms first** | A deliberate `panic!` added to `entry` on a throwaway build (**not committed**, like the earlier `rx_raw` diagnostics) on the spare ESP32-S3. First attempt, resetting immediately: the board rebooted cleanly (`rst:0x3 (RTC_SW_SYS_RST)`, repeatedly) and **no `PANIC:` line appeared on the serial output at all** — `esp_println` spins until the UART has room for each byte, not until the line has left the shift register, so the reset truncated it. With `Delay::delay_millis(PANIC_DRAIN_MS)` before the reset: `controller: running` / `PANIC: panicked at src/main.rs:258:5:` / `rst:0x3 (RTC_SW_SYS_RST)`, cycling. A panic that cannot be read is most of the cost of a panic, so the drain is not a nicety | 2026-08-17 |
| Retry logging is rate-limited and **observably goes quiet**: failures 1–7 logged (one per backoff step), 8 and 9 silent, 10 logged | `firmware` against the placeholder SSID over ~5 minutes on the spare ESP32-S3. This matters because a log line is not an await — `esp_println` writes inside a critical section, byte by byte, with interrupts disabled — and `tasks`' own docs put the cost of one at most of the ~5 ms available to re-arm the receiver between a frame and its repeat. Unbounded, an absent access point would have spent that budget twice a second forever | 2026-08-17 |
| Bounded reconnect backoff, observed end to end: **1000, 2000, 4000, 8000, 16000, 32000, 60000, 60000 ms** | `firmware` on the spare ESP32-S3 provisioned with the placeholder SSID `SSID_NOT_PROVISIONED`, which no access point answers. Eight consecutive `wifi: association failed … NoAccessPointFound` / `wifi: retrying in N ms` pairs over ~2 minutes, doubling and then holding at the 60 s ceiling, with the controller still running throughout — no panic, no hang, no restart | 2026-08-16 |
| A board with **no Wi-Fi credentials provisioned boots cleanly and runs the radio**, with the network never attempted | `firmware` on a freshly partitioned spare ESP32-S3: `config: survey slots=32 valid=0 blank=32 damaged=0 newest_seq=None`, then `network: no credentials provisioned — running radio-only`, then `controller: running`, with `heap: … peak 0`. The peak of zero is the evidence that the heap belongs to `esp-radio` alone — nothing else in the firmware allocated a byte. **On-air reception under a failing Wi-Fi link is NOT yet demonstrated**: it needs the reference transmitter fired while the controller is retrying, which is one `curl` on the owner's network | 2026-08-16 |
| The `wificfg` config region mounts, round-trips a record on real flash, and survives a reflash | `config-check` on the spare ESP32-S3: `config: survey slots=32 valid=0 blank=32 damaged=0 newest_seq=None`, `config: stored the placeholder credential`, `config: survey … valid=1 … newest_seq=Some(0)`, `config: reloaded ssid = Some("SSID_NOT_PROVISIONED")`. The controller flashed afterwards read the same record back (`newest_seq=Some(1)` after a second `config-check` run) and passed the SSID to the Wi-Fi driver, which is the end-to-end path | 2026-08-16 |
| Main stack available to the controller: **304,652 bytes** on an ESP32-S3 with 8 MB flash — **before Wi-Fi**; now 193,980 (see above) | `firmware` printed `stack: 304652 bytes available, 8192 required` at boot. esp-hal's linker script gives the stack whatever DRAM is left after the statics, so this figure is specific to this build and moves as statics are added — which is why it is checked at boot rather than asserted. It comfortably covers the ~6.5 KB `RmtTx::transmit_frame` needs, and settles the Plan 4a concern that an Embassy task might not get it: tasks have no stacks of their own, so this is the stack they all run on | 2026-08-16 |
