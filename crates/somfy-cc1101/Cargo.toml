[package]
name = "somfy-cc1101"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "no_std CC1101 driver for OOK asynchronous-serial transmission"

[dependencies]
embedded-hal = "1.0"

[dev-dependencies]
embedded-hal-mock = { version = "0.11", default-features = false, features = ["eh1"] }
