# somfy-rs partition table.
#
# The only reason this file exists is `rollcode`: the rolling-code store looks
# its region up by label at boot and refuses to run without it, rather than
# writing to a compiled-in offset that would silently collide with the app once
# the app grows. See `docs/specs/2026-08-15-config-integrity-requirements.md`.
#
# `espflash.toml` next to this file points espflash here, so a plain
# `espflash flash …` from `crates/firmware` writes this table rather than
# espflash's built-in default. Flashing with the default table instead is not
# silently wrong — the firmware reports `PartitionMissing` and stops.
#
# ## The layout is derived, not chosen
#
# Every offset below is forced by one of four rules, and the whole table falls
# out of them once `rollcode`'s address is treated as immovable.
#
# 1. **The low 64 KB is the ESP-IDF boot flow.** Bootloader, partition table at
#    0x8000, then `nvs` and `phy_init`. The first byte an app can occupy is
#    0x10000.
# 2. **An app partition must start on a 64 KB boundary.** That is the flash
#    MMU's page size, not a convention; `esp-idf-part` rejects anything else and
#    so does the ESP-IDF bootloader.
# 3. **`rollcode` may not move, ever.** It holds the rolling codes of motors in
#    daily use, and a lost code costs a physical re-pairing at each shade. Its
#    address has been 0x200000 since the region existed, so every already
#    provisioned board has its codes there. `wificfg` and `shades` are pinned
#    beside it for the weaker reason that moving them costs a re-provision.
# 4. **`otadata` must be exactly 0x2000 bytes of `data, ota`.** Two sectors, one
#    `esp_ota_select_entry_t` copy each, so a torn write cannot lose both.
#    `esp_bootloader_esp_idf::ota::Ota::new` refuses any other size outright.
#
# Rules 1–3 fix the first app slot completely: it starts at 0x10000 and ends
# where `rollcode` begins, so it is **0x1F0000 = 2,031,616 bytes and cannot be
# any larger without moving the rolling codes**. A/B slots must be the same size
# — an image that fits the larger slot and not the smaller one is a brick on the
# following update — so 0x1F0000 is the size of both.
#
# The second slot then follows by rule 2: the data regions plus `otadata` run to
# 0x208000, and the next 64 KB boundary is 0x210000. 0x210000 + 0x1F0000 lands
# on 0x400000 exactly, so the table ends on a 4 MB boundary and still fits a 4 MB
# board — which the single-app table it replaces also did, and which is worth
# keeping since only the ESP32-S3 board here is known to carry 8 MB.
#
# ## Is 0x1F0000 enough?
#
# Measured with `espflash save-image` on 2026-08-17, against the 2,031,616-byte
# slot:
#
#     ESP32     632,640 bytes   31.1%   1,398,976 free
#     ESP32-S3  585,376 bytes   28.8%   1,446,240 free
#     ESP32-C3  581,040 bytes   28.6%   1,450,576 free
#
# Only the middle row is a live figure: the ESP32 was dropped on 2026-08-18 and
# the ESP32-C3 on 2026-08-19. The other two are kept because the point of the
# table is the spread — three unrelated images all landing near 30%.
#
# The slot has to hold firmware **and** the web UI, because there is no
# filesystem: spec §6 puts the UI in the image with `include_bytes!` so that one
# image is firmware and UI together and an OTA is atomic. Against the largest of
# them there is 1.33 MiB spare, and spec §8 budgets the whole UI at ≤200 KB
# gzipped.
# What else is still to come — TLS, an HTTP server, the real config store, mDNS
# and SNTP — is the part with no measurement behind it yet; mbedTLS is the large
# one and is the figure to re-check when it lands.
#
# ## What is deliberately not allocated
#
# 0x20F000–0x210000, 4 KB, one erase sector. That is all that is left, and it is
# **not enough for another region**: every data region here is at least two
# sectors, because only two guarantee that erasing one can never take the newest
# record with it. So the space between the last data region and the 64 KB
# boundary `ota_1` has to start on is now spent, and the next region needs a
# layout change rather than a line.
#
# What that reserve was for, and where it went:
#
# `estate` took the first 8 KB, at 0x208000. `import` took 20 KB more, at
# 0x20A000. Both are **purely additive**: each sits above every region a
# provisioned board already has, so nothing moves and a board reflashed with
# this table keeps its rolling codes, its credentials and its shade table
# exactly as before. What it does not keep is an estate or a staged restore,
# because it never had either — and a blank region of each reads as "no rooms
# and no groups" and "nothing staged", which is what that board is running
# today.
#
# ## `import` is 20 KB, and the figure is two things at once
#
# Its first sector is a 128-byte state record — what the last upload was, and
# what the boot path did about it — and the four after it are the staged file.
# 16 KB of staged file is `firmware::restore::STAGE_MAX_BYTES`, and that
# constant carries the arithmetic: the worst C++ ESPSomfy-RTS backup a
# supported controller can produce, with 32 shades at 276 bytes each and every
# other collection full, is about 14.5 KB. This firmware's own `RTSB` container
# is 4,420.
#
# It is **also the size of the buffer the boot path reads it into**, on the main
# stack, which is the only place on this device with room to parse a backup
# whole. So it cannot simply be "the rest of the region": every byte here is a
# byte of `firmware::heap::REQUIRED_STACK_BYTES`.
#
# The state record has a sector to itself because it is rewritten on a path that
# must not disturb the file beside it, and an erase takes a whole sector whether
# it wants to be or not.
#
# On an 8 MB board 0x400000–0x800000 is unallocated too. Spending it would mean
# either slots larger than 0x1F0000 — impossible while `rollcode` stays put —
# or a data region no 4 MB board could carry.
#
# ## The data regions
#
# `rollcode` is 8 KB: two 4 KB erase sectors, which is the minimum the ring
# needs to guarantee that erasing one sector can never take the newest record
# with it.
#
# `wificfg` is the second region with the same shape and the same reasons. It is
# a separate partition rather than more slots in `rollcode` because the two have
# different write rates and very different consequences on loss, and one erase
# must never touch both.
#
# `shades` is the third, and it is a third region rather than more fields in
# `wificfg` because the shades do not fit there: a config record is 512 bytes
# with 228 free after its last field, or four shades' worth, against the 32 the
# registry holds. Its slots are 2048 bytes — one whole shade table per record,
# for the reason `somfy_store::Record` gives about erasing a ring — so two
# records tile a 4 KB sector and the region is two sectors for the same
# guarantee as the other two.
#
# `import` is the fifth, and the one that is not a ring. A staged restore is
# written once, read once at the next boot and then superseded, so wear
# levelling would be protecting against a write pattern that does not exist; and
# a torn state record reads as "nothing staged", which is the safe direction —
# the worst it costs is a report an operator has to reproduce, where a torn
# rolling-code record costs a re-pairing.
#
# `estate` is the fourth: the rooms, which room each shade is in, and the groups
# with their names, members and virtual-remote identities. It is a fourth region
# for the same reason `shades` was a third — there is nowhere else to put it.
# The shade record is full **to the byte**: a compile-time assertion in
# `somfy_config::shade` states that its linked-remote pool "must fill the record
# exactly", so a room cannot be added to it without taking a wall remote away.
# Its slots are also 2048 bytes, so it tiles a sector the same way and the region
# is two sectors for the same guarantee.
#
# **The firmware only ever reads `estate`**; the host-side `provision_shades`
# example writes it, from the same import that writes `shades`. That coupling is
# not incidental: a group's membership and a room assignment are stored as *rows
# of the shade table*, so an estate written beside a different table names the
# wrong shades. Flash the two together.
#
# ## What a board already in service keeps
#
# `rollcode`, `wificfg` and `shades` keep their offsets, their sizes and their
# contents. `estate` and `import` are new and blank, which read as no rooms and
# no groups, and nothing staged — the state that board is already in.
#
# **A board that is *not* reflashed keeps working and cannot take a restore.**
# `firmware::restore::Staging::mount` reports the missing region once at boot
# and the controller carries on; a `POST /api/v1/system/backup` is refused with
# `backupUnwritable` rather than timing out. Exporting one still works, because
# an export reads regions that board already has.
#
# Original text, which still holds for the three older regions: A provisioned board reflashed with this table keeps its rolling
# codes, its credentials and its shade table; `factory` becomes `ota_0` at the
# same offset and the same size, so even the app image lands where it already
# was. `otadata` is new and blank, and a blank `otadata` with no `factory`
# partition present is exactly what the bootloader reads as "boot `ota_0`".
#
# The one thing an operator must do is erase `otadata` when *reverting* to this
# table from a state where the device had already booted `ota_1` — see
# `docs/hardware-checklist.md`.
#
# ## There is no `factory` partition, and that has a consequence for Plan 6 Task 4
#
# Omitting `factory` is required: the bootloader prefers it over both slots
# whenever `otadata` is blank, which is what every freshly flashed board has, so
# a table carrying one would boot the factory image forever and an update
# written to a slot would simply never run. `build.rs` refuses a table that
# reintroduces it.
#
# What it costs is a trap in `esp_bootloader_esp_idf` 0.5.0 that whoever writes
# the update path should know about **before** they meet it. `Ota::
# current_app_partition` reports `AppPartitionSubType::Factory` when both
# `otadata` sequence numbers read 0xFFFFFFFF — a blank region — and it does so
# whether or not a factory partition exists. `OtaUpdater::next_ota_part` then
# maps `Factory` to `Ota0`, notices that `Ota0` is also the booted partition,
# and recovers by computing `(current.ota_app_number() + 2) % ota_count`. For
# `Factory` that inner call is `0u8 - 0x10`: it panics under the dev profile's
# overflow checks and wraps to 240 under release, and `(240 + 2) % 2` is 0 —
# `Ota0` again, the slot that is running. So on a board's **first** update the
# convenience wrapper hands back the partition it is executing from.
#
# The layout is not what is wrong here and a factory partition would not fix it,
# it would only hide it behind a worse failure. Task 4's options are to write
# `otadata` once at first boot so it is no longer blank, or to pick the slot from
# `PartitionTable::booted_partition` and drive the lower-level `ota::Ota` API
# directly. Either is a few lines; discovering it against a running image is not.
#
# Name,     Type, SubType,   Offset,   Size,     Flags
nvs,        data, nvs,       0x9000,   0x6000,
phy_init,   data, phy,       0xf000,   0x1000,
ota_0,      app,  ota_0,     0x10000,  0x1F0000,
rollcode,   data, undefined, 0x200000, 0x2000,
wificfg,    data, undefined, 0x202000, 0x2000,
shades,     data, undefined, 0x204000, 0x2000,
otadata,    data, ota,       0x206000, 0x2000,
estate,     data, undefined, 0x208000, 0x2000,
import,     data, undefined, 0x20A000, 0x5000,
ota_1,      app,  ota_1,     0x210000, 0x1F0000,
