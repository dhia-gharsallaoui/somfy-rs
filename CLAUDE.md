# somfy-rs — working rules

## Do not reinvent what a well-maintained crate already does

**Before writing any non-trivial component, search for an existing crate and
say what you found.** Prefer adopting, wrapping, or porting a proven
implementation over writing a new one.

"Well-maintained" is a claim to be checked, not assumed. Look at the actual
repository and report the numbers: **stars, forks, contributors, commit count,
date of the last commit, open issues and PRs, and whether anyone outside the
author uses it.** A single-author crate with four commits and zero stars is not
a dependency for firmware that runs unattended in someone's home, however
pleasant its API. Publication on crates.io and a tidy README are not adoption.

Then check it covers the case at hand — **against the real API, not the crate
description**.

Record the decision either way. "I looked, and here is why the existing crate
does not fit" is a required part of the work, not an optional extra — and it is
just as valuable as adopting one, because it stops the same question being
reopened later.

Reasons that justify writing our own, when they are actually true:

- **The crate does not cover the mode we need.** Check this against the real
  API rather than the crate description.
- **It is unmaintained or pre-release** in a way that matters for firmware we
  intend to run unattended in someone's home.
- **Its licence is incompatible** with this project's GPL-3.0-only, or absent.
- **The wrapping would be larger than the thing.** Rare, and suspicious when
  claimed — say concretely what the wrapper would have to do.

Reasons that do **not** justify it: it looks easy; we would learn more; the
crate's API is not quite the shape we would have chosen; we have already
started.

This applies to the reference implementation too. `docs/provenance.md` rule 1
already requires deriving from it rather than inventing protocol behaviour —
and note that the C++ reference itself gets its radio configuration from an
external library rather than hand-rolling it. Reuse is the norm on both sides
of this port.

### Recorded evaluations

| Component | Existing option | Decision |
|---|---|---|
| `somfy-rts` (protocol) | [`somfy`](https://crates.io/crates/somfy) 0.1.0 | **Own.** Frame construction only, transmit-oriented; no pulse rendering, no receive decode. 0 stars/forks, no releases, no visible licence file. Ours does 56/80-bit encode **and** decode, rolling codes, pulse rendering, dual-stream RX and repeat dedupe, and is pinned against real wall-remote captures. |
| WiFi (Plan 5) | [`esp-radio`](https://crates.io/crates/esp-radio) 0.18.0, [`esp-wifi`](https://crates.io/crates/esp-wifi) 0.15.1, [`esp-wifi-sys`](https://crates.io/crates/esp-wifi-sys) | **Adopt `esp-radio` 0.18.0.** Verified both ways against the real graph: `esp-radio` resolves and leaves `esp-hal` at 1.1.2, while **`esp-wifi 0.15` fails outright** — "failed to select a version for `esp-wifi` which could resolve this conflict" against our `esp-hal 1.1.2` / `esp-rtos 0.3`. `esp-radio` continues esp-wifi's version line (0.15 → 0.18) after the rename and lives in the esp-hal repo, so it tracks the HAL. `esp-wifi-sys` is **not an alternative** — it is bindgen FFI to Espressif's closed blobs, the layer *beneath* the driver, and we already consume it transitively. There is no independent pure-WiFi Rust driver for ESP32; the MAC/PHY is closed, so every option binds the same blob and the only choice is which layer to sit at. Requires a heap — `build-std` must gain `alloc`. |
| Network stack (Plan 5) | [`embassy-net`](https://crates.io/crates/embassy-net) 0.9.1 | **Adopt.** The standard stack for Embassy, which we already run via `esp-rtos`. |
| Allocator (Plan 5 Task 2) | [`esp-alloc`](https://crates.io/crates/esp-alloc) 0.10.0 | **Adopt, and there is effectively no choice.** `esp-radio`'s own `default` feature *is* `esp-alloc`, `esp-rtos` has a matching `esp-alloc` feature, and both allocate through its `InternalMemory` allocator specifically — a generic `linked_list_allocator` as `#[global_allocator]` would satisfy the `alloc` requirement but not the internal-memory capability the Wi-Fi driver's buffers need. Enabled its `internal-heap-stats` feature, which is what turns the heap size from an assertion into a measurement: it keeps a high-water mark, and `docs/provenance.md` records the boot the figure came from. |
| `'static` handoff for `embassy-net`'s `StackResources` (Plan 5 Task 2) | [`static_cell`](https://crates.io/crates/static_cell) 2.x | **Adopt.** `embassy_net::new` borrows `&'d mut StackResources` for the lifetime of the stack, so it needs a `'static` handed out exactly once. The alternative is a `static mut` plus an `unsafe` block asserting single use — the same guarantee with the check deleted. `StaticCell::init` panics on a second call instead. By embassy's own author, MIT/Apache-2.0, ~3 KB of code, no transitive dependencies. |
| Persisted device config (Plan 5 Task 2) | [`sequential-storage`](https://crates.io/crates/sequential-storage) 8.0.1, [`ekv`](https://crates.io/crates/ekv) 1.0.0, ESP-IDF NVS via [`esp-storage`](https://crates.io/crates/esp-storage) | **Own — `somfy-config`, and deliberately minimal, because Plan 6 replaces it. Revisit there.** `sequential-storage` is the serious candidate and is well adopted (675,620 downloads, 189,786 recent, MIT OR Apache-2.0, last release 2026-07-19); `ekv` is embassy-rs's LSM-tree key-value store for raw NOR flash but far less used (6,753 downloads, 664 recent). Both are **key-value stores, and a key-value store is not the part that was missing.** What this task needs is a *validated record*: an SSID over 32 bytes, a passphrase under 8 characters or a field containing a NUL must be **refused**, not stored, and a store would persist all three happily — the validation is where the whole value is, and it is what `somfy-config` actually consists of. What a store adds over the ring already written and hardware-proven in `somfy-store` is wear levelling across a region written a handful of times in a device's lifetime. `esp-storage` is not an alternative at all: it exposes raw NOR flash, not NVS, and there is no NVS reader in the Rust ESP stack — writing one to hold two strings would be format-compatibility work for something Plan 6 deletes. **What would change this:** Plan 6's config is a whole device model rather than two strings, and at that size `sequential-storage` under the same validation layer is the likely answer. **Plan 5 Task 3 moved that threshold closer**: the record grew to six fields and 512 bytes to carry the broker settings and the two MQTT namespaces, and it now also has to hand back the *superseded* namespace pairs still readable in the ring, which a key-value store would express far better than a hand-rolled scan. The validation argument still holds — the MQTT settings are refused, not stored, by the same rules `somfy-mqtt` builds topics with — but the storage half is now the larger part. |
| MQTT client (Plan 5 Task 3) | Surveyed the whole crates.io field, not a remembered shortlist (230 crates carry the `mqtt` keyword; every `no_std` client among them was checked). Clients: [`minimq`](https://crates.io/crates/minimq) 0.13.0, [`rust-mqtt`](https://crates.io/crates/rust-mqtt) 0.5.1, [`mcutie`](https://crates.io/crates/mcutie) 0.4.0, [`mountain-mqtt`](https://crates.io/crates/mountain-mqtt) 0.2.0, [`embedded-mqttc`](https://crates.io/crates/embedded-mqttc) 1.0.1, [`myrtio-mqtt`](https://crates.io/crates/myrtio-mqtt) 0.3.0, [`embassy-ha`](https://crates.io/crates/embassy-ha) 0.4.0, [`mqtt-async-embedded`](https://crates.io/crates/mqtt-async-embedded) 1.0.0, [`tinymqtt`](https://crates.io/crates/tinymqtt) 0.1.3, [`mqttrust`](https://crates.io/crates/mqttrust) 0.6.0, [`w5500-mqtt`](https://crates.io/crates/w5500-mqtt) 0.4.0, [`embassy-mqtt-lite`](https://crates.io/crates/embassy-mqtt-lite) 0.3.0 | **Adopt `minimq` 0.13.0. Runner-up `rust-mqtt` 0.5.1.** Both clear every hard requirement — retain, retained LWT on CONNECT, subscribe/unsubscribe, QoS 0–2, `embedded-io-async` 0.7 (the version `embassy-net` 0.9.1 itself uses, so `TcpSocket` is accepted with no adapter) — and **both were proved by building a throwaway spike for `xtensa-esp32s3-none-elf` under `build-std = ["core"]`**, exercising retained publish, retained will, zero-length retained removal, non-retained QoS 1 command, subscribe and unsubscribe. Neither needs `alloc`; `esp-hal` stays at 1.1.2. The decider is **allocation strategy and `unsafe`**. `minimq` has **0 occurrences of `unsafe`** in its source, takes caller-owned fixed `rx`/`tx` slices, and advertises the rx size as MQTT5 `MaximumPacketSize` in CONNECT so the broker will not send anything larger — inbound is bounded by construction. `rust-mqtt` alloc-free means the `bump` feature, which is a raw-pointer bump allocator that never frees: every inbound dynamically-sized field consumes it (`src/io/read.rs:147`), reclaiming needs `pub unsafe fn reset()`, and the caller must prove no borrows remain — its own 0.5.0 changelog records fixing UB in exactly that code, and its README's worked example uses the heap `AllocBuffer` instead. So `rust-mqtt` offers a choice between an `unsafe` soundness obligation on the hot path of a device that runs unattended for months, or a heap on the MQTT path that Plan 5 deliberately confines to `esp-radio`. `minimq` also drives keepalive/PINGREQ itself and exposes `ConnectEvent::Connected` vs `Reconnected`, which is precisely the R5 hook for "republish discovery only on a fresh broker session". **Maintenance, both:** `minimq` 191 stars, 19 forks, 656 commits, 13 contributors (11 non-maintainer), last commit 2026-07-14, 1 open issue / 0 open PRs, 13 merged non-maintainer PRs, 10 distinct issue authors, ~19 third-party repos, 141,594 downloads (3,473 recent), MIT. `rust-mqtt` 119 stars, 54 forks, 124 commits, 16 contributors (15 non-author), last commit 2026-08-13, 8 open issues / 2 open PRs, 55 merged non-owner PRs, 22 distinct issue authors, **~120 third-party repos and 13,223 recent downloads**, MIT OR Apache-2.0. **`rust-mqtt` is the more widely adopted crate, and by a wide margin on our exact stack** (ESP32 + Embassy + `no_std` demos from esp-rs community authors) — that is the strongest argument against this ruling and the reason it is the runner-up rather than an elimination. Countervailing: both current APIs are recent rewrites with little field time (`minimq` 0.11 "breaks most of the API surface" 2026-05-05, then 0.12 and 0.13 broke it again by 2026-07-14; `rust-mqtt` 0.5.0 was a rewrite 2026-03-27), and quartiq's own deployed firmware — `stabilizer`, `booster`, `thermostat-eem` — is still pinned to `minimq ^0.9`, so the async rewrite is no more field-proven than `rust-mqtt` 0.5. **What would change the choice:** if the broker turns out to be MQTT 3.1.1-only, *both* are out — `minimq` is v5-only and `rust-mqtt`'s `v3` feature is an **empty stub** (`src/v3/mod.rs` is 1 byte; its own README says "`v3`: Unused"); if we ever accept a heap on the MQTT path, `rust-mqtt`'s adoption advantage wins on `alloc`; if `minimq` breaks its API a fourth time before Task 3 lands, revisit. **Cost this implies:** neither crate reconnects or backs off — `rust-mqtt`'s README says so explicitly — so **R9's bounded-backoff reconnect loop is ours to write**, along with re-subscribing and republishing retained state on `ConnectEvent::Connected`. Budget that as real work, not glue. **Eliminated, each with its cause:** `mcutie` 0.4.0 — the only candidate that *does* reconnect for us (5 s backoff) and speaks embassy-net natively, but 22 stars, 51 commits, **2 third-party repos**, **no LICENSE file in the repo or the published crate** (GitHub reports the licence as null; only the Cargo.toml declares MIT), its codec is `mqttrs` 0.4.1 **unmaintained since 2021-05-26**, its broker port is hardcoded to 1883, and its HA module takes the discovery prefix from a **compile-time** `option_env!("HA_DISCOVERY_PREFIX")` and has no `cover` component — R1/R3 need it runtime-validated and R5 needs old configs deleted when it changes. `mountain-mqtt` 0.2.0 and `embedded-mqttc` 1.0.1 — capability-complete but pinned to **`embedded-io-async` 0.6** (`embedded-mqttc` explicitly `>=0.6.0, <0.7.0`), a different trait version from the 0.7 that `embassy-net` 0.9.1's `TcpSocket` implements, so the socket does not satisfy their bounds; `embedded-mqttc` additionally caps `embassy-time` at `<0.5.0` against our 0.5. `myrtio-mqtt` 0.3.0 — pins `embassy-net` 0.7.1 and `embedded-io-async` 0.6.1, publishes **no `repository` field**, and its source repo `MyrtIO-archive/myrtio-mqtt` is **archived**. `tinymqtt` 0.1.3 — `publish(&mut self, topic: &str, payload: &[u8])` writes `Flags::zero()` as the PUBLISH header, so retain is hardcoded off; CONNECT never sets the will bit; 1 star, 10 commits, all within 39 minutes on 2024-06-04, untouched since. `mqtt-async-embedded` 1.0.0 — the strings `retain` and `will` **do not occur anywhere in its source**; its CONNECT flags handle only `clean_session`; its `repository` field is the unedited template `github.com/your-username/…`. `mqttrust` 0.6.0 — `publish(&self, topic, payload, qos)` hardcodes `retain: false` (`src/lib.rs:30`); last release 2022-09-22. `w5500-mqtt` 0.4.0 — `src/publish.rs:28` comments "retain=0, do not retain this message" and hardcodes it; it also targets the W5500 SPI Ethernet chip, not `embassy-net`. `embassy-ha` 0.4.0 — matches our stack exactly and does have `will_retain`, but 8 stars, 72 commits, 196 downloads, **no licence**, and it is an HA-discovery layer, which is the part we already own in `somfy-mqtt`. `embassy-mqtt-lite` 0.3.0 — already ruled out and confirmed: only `publish(&mut self, topic: &str, payload: &[u8])`, no retain; 0 stars, 4 commits, 0 third-party uses. Everything else with real download volume (`rumqttc`, `paho-mqtt`, `ntex-mqtt`, `mqtt5`, `gneiss-mqtt`, `mqttier`) is `std`/tokio, and the rest (`mqttrs`, `mqttrs2`, `gmqtt`, `mqute-codec`, `mqtt-format`, `mqtt-proto`, `embedded-mqtt`) are packet codecs with no client, which is the hand-rolling this rule exists to prevent. Do **not** hand-roll a client: the hard parts here are topic construction, retention and lifecycle, not packet framing. |
| `somfy-mqtt` (topics + HA discovery, Plan 5 Task 1) | [`hass-mqtt-discovery`](https://crates.io/crates/hass-mqtt-discovery) 0.2.0, [`hamqtt`](https://crates.io/crates/hamqtt) 0.1.10 | **Own.** `hass-mqtt-discovery` is MIT and models the payloads well, but it is `std`-only (no `no_std` attribute anywhere in it, and it pulls `thiserror`/`serde`), last published 2022-11-27, and — decisively — it contains **no discovery-topic construction at all**: grepping its source for `discovery_prefix`, `node_id`, `object_id` or `/config` returns nothing. It models the payload, and the payload was never the bug. `hamqtt` 0.1.10 is `no_std` and current (2025-11-04), but it is licensed **LGPL-2.1-only**, which has no "or later" clause and so cannot be combined with our GPL-3.0-only; it also requires a heap (`serde_json` with `alloc`), which the allocation-free crates in this workspace do not have. |
| `ConfigurationUrl` (validating the `configuration_url` a discovery payload carries) | [`fluent-uri`](https://crates.io/crates/fluent-uri) 0.4.1, [`url`](https://crates.io/crates/url), [`uriparse`](https://crates.io/crates/uriparse) | **Own — and `fluent-uri` was tested, not dismissed.** It is the only serious `no_std`-without-`alloc` candidate and it is well adopted: 123 stars, 8 forks, 4 contributors, MIT, last push 2026-05-20, 11 open issues, **7,901,069 downloads a month, used in 2,549 crates**. `Uri::parse`, `scheme`, `authority` and `as_str` are all outside its `alloc` gate, so it would build here. **A throwaway spike against the real API decided it, and the result was the opposite of the crate description:** `Uri::parse("http:///shades")` and `Uri::parse("http://")` both return **`Ok`** with `authority = Some("")` — RFC 3986 permits an empty host — and those two are exactly the inputs that make Home Assistant's `cv.configuration_url` raise `vol.Invalid` and discard the **entire** discovery payload, so the entity never appears at all. It also accepts `ftp://` and `homeassistant://`. So the two rules that carry the weight — a non-empty authority and a scheme allowlist — stay ours either way, and so does the byte bound. What it *would* replace is `is_uri_char`, six lines, which is also the one place a compile-time assertion proves that no admitted character expands under the payload's JSON escaper — the proof `WORST_DEVICE_BLOCK_LEN` rests on. Trading that for a dependency (plus `borrow-or-share`, `ref-cast` and a proc-macro chain) in a crate that today has two, built for four bare-metal targets, is the wrong direction. `url` and `uriparse` are not candidates at all: both need `alloc`, and `url` pulls `idna`. **What would change this:** `configuration_url` becoming operator-supplied rather than derived from this device's own hostname — at that point the input is arbitrary text, full RFC 3986 parsing earns its place, and the emptiness and scheme checks sit on top of it. |
| `somfy-cc1101` (radio driver) | [`cc1101`](https://crates.io/crates/cc1101) 0.1.3, [`cc1101-embassy`](https://crates.io/crates/cc1101-embassy) 0.1.0 | **Own — but this was never consciously evaluated, which was a process failure.** The high-level `cc1101` API is packet-oriented (sync words, address filtering, packet length); this project runs the chip in asynchronous-serial OOK where every one of those is switched off, so we would be using its `lowlevel` raw-register module and writing the same bytes by hand anyway. Revisit if that crate grows async-serial support. |
| Persisted shade table (`somfy_config::ShadeRecord`) | [`postcard`](https://crates.io/crates/postcard) 1.1.3, and the key-value stores already weighed above ([`sequential-storage`](https://crates.io/crates/sequential-storage), [`ekv`](https://crates.io/crates/ekv)) | **Own, narrowly, and for a reason that is about validation rather than bytes.** `postcard` is the serious candidate and is by far the most adopted `no_std` serializer there is: 1,491 stars, 140 forks, ~66 contributors, 385 commits, last push 2026-07-20, 74 open issues / 24 open PRs, **52,932,072 downloads (19,789,013 recent)**, MIT OR Apache-2.0, and licence-compatible with our GPL-3.0-only. It is `no_std` without `alloc` through `to_slice`, and its `use-crc` feature would genuinely have covered the checksum for free — **verified against its own docs, not remembered.** Three things decided against it. **(1) The encoding is the small half.** Every decoded field goes back through `ShadeConfig::new`, `ShadeKind::from_raw`, `TiltMode::from_raw` and the zero-travel-time rule, so that flash cannot deliver a shade `Registry::add_shade` would then refuse — serde deserialises straight into fields past all of that unless a mirror type and a conversion are written, which is where most of `shade.rs` already is. Postcard replaces the ~90 lines of byte-shuffling and none of the ~200 that matter. **(2) The slot is fixed and the ring reads bytes.** A slot is 2048 bytes, "blank" means every byte is `0xFF`, and equal records must encode identically because that is how a write is proved to have landed; postcard's output is varint and variable-length, so a framing header — magic, version, count, seq, padding, CRC — would still be ours, and that header *is* this format. **(3) It would be the odd one of three.** `RTSC`, `RTSW` and `RTSS` are read by the same `SectorRing`/`newest_slot` machinery, and a varint record among two fixed-offset ones costs the property that a hex dump of flash can be read against the file that wrote it. **What would change this:** the same thing that would change the `sequential-storage` ruling — Plan 6's configuration being a whole device model rather than one table. At that size a serde format under the same validation layer, inside a length-framed slot, is the likely answer, and the two rows should be revisited together. **Plan 6 Task 2 grew the format and the ruling did not flip, but it moved.** Version 2 added a header word, a per-record link count, two entry bytes and a 58-word shared pool, and it has to read version 1 as well — so there are now two layouts, not one. Reasons (2) and (3) hold *harder*: a slot is still fixed at 2048, "blank" is still every byte `0xFF`, equal records must still encode identically because the firmware now proves a write landed by comparing bytes rather than decoded values, and the pool is sized as *exactly what is left of the record*, which a varint encoding cannot express at all. Reason (1) also holds: every decoded link goes back through `Shade::link_remote`'s rules, and every decoded shade through `ShadeConfig::new`, `FrameWidth::from_raw` and `RadioProtocol::from_raw`. What has changed is that the migration is now the interesting part, and `postcard` would not have helped with it either — a schema change there is still a hand-written reader for the old shape. **What would flip it:** groups, rooms and per-shade favourites joining the record, which is Plan 6 Task 3 and would push it past what fixed offsets are pleasant to hand-write. **Plan 6 Task 3 landed and it did not flip — but it did not join this record either**, because there was no room: rooms and groups went into a fourth region and a format of their own, and the reopened postcard evaluation is the row below. |
| Persisted rooms and groups (`somfy_config::EstateRecord`, Plan 6 Task 3) | [`postcard`](https://crates.io/crates/postcard) 1.1.3, re-checked against its own docs rather than against the row above; and the key-value stores already weighed ([`sequential-storage`](https://crates.io/crates/sequential-storage), [`ekv`](https://crates.io/crates/ekv)) | **Own — and this is the row the shade-table ruling above said would flip it, so it was reopened properly rather than assumed.** `postcard` remains by a wide margin the most adopted `no_std` serializer there is, and the numbers were re-read on 2026-08-18 rather than carried over: **1.1.3, released 2025-07-24**, 53,244,281 downloads (19,873,417 recent), 1,490 stars, 140 forks, 66 contributors, 385 commits, last push 2026-07-20, 50 open issues / 24 open PRs, MIT OR Apache-2.0 — compatible with our GPL-3.0-only. **Two things checked this time that had not been, and one of them favours postcard.** (a) `use-crc` is genuinely a free checksum *and* it fits: `to_slice_crc32` takes a `crc::Digest<'_, u32>` the caller supplies, so we would hand it the same `CRC_32_ISO_HDLC` the other three records use and the four would agree rather than merely coexist. That is a real point in its favour and it is recorded as one. (b) **Its wire format puts schema evolution explicitly out of scope** — `postcard.jamesmunns.com/wire-format`: "Backwards/forwards compatibility between revisions of a postcard schema are considered outside of the scope of the postcard wire format", and "Postcard is NOT considered a Self Describing Format". The stability claim is about the *format*, not about a struct that changes. That is the property the shade record's four-version reader needed and the one this record will need at its first change, and it is the thing the previous ruling guessed at and can now cite. **What decided it, in order.** (1) **The slot is 2048 bytes and the ring reads slots.** postcard's integers are varint/LEB128 and its output is variable-length, with **no fixed-width or padded mode anywhere in the spec** — `u8`/`i8`/`f32`/`bool` are fixed by their *type*, not by a switch. So a framing header — magic, version, two counts, seq, padding, CRC — would still be ours, and that header *is* most of this format. (2) **Validation is the content, not the bytes.** A decoded group goes back through a remote's address rule (0 and 0xFFFFFF refused, 24 bits wide), a room index has to name a room the record has, a name length has to fit the field it describes, and an unknown flag bit is refused rather than masked. serde deserialises straight past all of that unless a mirror type and a conversion are written, which is where most of `estate.rs` already is — postcard would replace the ~120 lines of byte-shuffling and none of the ~250 that matter. (3) **It would be the odd one of four.** `RTSC`, `RTSW`, `RTSS` and `RTSE` are read by the same `SectorRing`/`newest_slot` machinery, and a varint record among three fixed-offset ones costs the property that a hex dump of flash can be read against the file that wrote it. **The honest weakness in this ruling**, since the shade row leaned on it and this one cannot: **the estate record is *not* full to the byte.** 716 of its 2048 bytes are spare, so "the pool is exactly what is left, which a varint encoding cannot express" — reason (2) of the shade ruling — simply does not apply here. What is left is the fixed slot, the validation and the uniformity, and they are enough but they are less. **What would flip it:** a length-framed slot with a postcard body inside it, the moment either record's fields outgrow what fixed offsets are pleasant to hand-write. The two rows should be revisited together, and now the third — a serde format under the same validation layer, with the version gate still hand-written because postcard says it will not provide one. |
| Partition-table validation in `crates/firmware/build.rs` (Plan 6 Task 1) | [`esp-idf-part`](https://crates.io/crates/esp-idf-part) 0.6.0 | **Adopt.** The alternative was splitting a nine-line CSV on commas, which is small enough that the reuse rule could plausibly have been waived — and would have been the wrong call, because **this is the parser espflash itself uses**. That is the whole value: the build asserts what the flashing tool will read rather than a second opinion about the same file, and it inherits the generic ESP-IDF rules for free — app partitions on 64 KB boundaries, `data, ota` sized exactly 0x2000, overlaps, duplicate names, the 0x9000 floor. All three were confirmed to fire against the real API, not the description: a misaligned `ota_1` is rejected with "The partition is not correctly aligned", a 0x1000 `otadata` with "must have size of 0x2000 (8k) bytes", and a table past the flash size with "The partition table does not fit into the flash (4MB)". `build.rs` is then left with only the project-specific invariants (the three pinned data offsets, equal A/B slots, no `factory`, the 4 MB ceiling), each of which was proved to fire by breaking the table and restoring it. **Maintenance:** esp-rs's own crate, 1,013,458 downloads (317,455 recent), 12 stars, 8 forks, 0 open issues, last push 2025-04-16, MIT OR Apache-2.0 — licence-compatible with our GPL-3.0-only. The low star count is not adoption evidence here and does not need to be: espflash is the third-party user that matters, and it is the one whose behaviour we are trying to match. A **build** dependency, so none of it — nor `deku`, `csv` or `regex` beneath it — reaches the image. |
| Announced-shade set (`somfy_config::Announced`, Plan 6 Task 2) | [`bitflags`](https://crates.io/crates/bitflags) 2.13.1, [`enumset`](https://crates.io/crates/enumset) 1.1.14, [`enumflags2`](https://crates.io/crates/enumflags2) 0.7.12, [`bitvec`](https://crates.io/crates/bitvec) 1.1.1, [`modular-bitfield`](https://crates.io/crates/modular-bitfield) 0.13.1 | **Own — about forty lines, and the reason is that these bits have no names.** `bitflags` is the serious candidate and is about as adopted as a Rust crate gets: 1,678,874,973 downloads (374,002,223 recent), 1,152 stars, 166 forks, ~96 contributors, last commit 2026-07-16, 10 open issues / 0 open PRs, MIT OR Apache-2.0 (compatible with our GPL-3.0-only), `no_std` without `alloc` by default, and its generated type carries **twenty inherent `const fn`s** including all three unknown-bit policies (`from_bits` rejects, `from_bits_truncate` drops, `from_bits_retain` preserves) with `u32` storage a one-line choice — every capability this needed, verified against the generated API rather than the README. **But all three flag-set crates model a set of *named* flags.** Bit *n* here means registry slot *n*: there is no `const READ = 1 << 0;` to write, only thirty-two identical lines naming `SHADE_0` … `SHADE_31`, and the one operation that matters — "is the bit for *this* `ShadeId` set?" — is an index, which a flag-set type deliberately does not offer. Strip the naming and what is left is `1 << n`; the part that needed writing is the **bound** — a `ShadeId` outside the registry must not shift past the word — and no flag crate performs that check because a flag crate has no ids to bound. `enumset` 1.1.14 (41,541,231 downloads, 127 stars, MIT/Apache-2.0) and `enumflags2` 0.7.12 (97,862,093 downloads, 144 stars) are the same shape with weaker const stories: **every** `enumset` set operation is non-const, and `enumflags2` needs `_c`-suffixed variants with a `ConstToken`. `enumset` additionally warns that without an explicit `repr = "u32"` it picks the representation itself and makes no guarantee which — a hazard for something written to flash. `bitvec` 1.1.1 is aimed at dynamic bit *sequences*, indexes numerically rather than by name, its own docs redirect named-field use elsewhere, and its default branch has been untouched since **2023-04-12** while 1.1.1 was published 2026-06-18 from somewhere else. `modular-bitfield` packs multi-bit struct fields, not flag sets. **What would change this:** the announced set becoming per-shade *and* per-entity — `somfy_mqtt::Component` **is** a named set, and `enumset` with `repr = "u32"` and the default `map = "lsb"` pins a flash-stable layout. |
| Debounced shade-table writes (Plan 6 Task 2) | crates.io search for `debounce` (743 matches; the top 40 by relevance inspected). Nearest: [`debounced`](https://crates.io/crates/debounced) 0.2.0, [`async-debounce`](https://crates.io/crates/async-debounce) 0.3.0, [`tokio-debouncer`](https://crates.io/crates/tokio-debouncer) 0.3.1, `notify-debouncer-full`/`-mini`, and eight GPIO debouncers | **Own — and the gap is real but smaller than any crate that would fill it.** Nothing in the field is a `no_std` async trailing-edge debouncer for *requests*. `debounced` 0.2.0 has exactly the right semantics ("this library currently only implements trailing debounce", yielding the most recent item after a quiet window) and real volume at 296,810 downloads — but it depends on `futures-timer` 3.0.2, which is `std` and spawns a timer thread on native builds, and it is 10 stars / 3 commits total, last published 2024-08-17. `async-debounce` 0.3.0 is the only `no_std` async one and its dependency set matches this stack exactly (`embedded-hal` 1.0, `embedded-hal-async` 1.0, **`embassy-time` 0.5**, MIT OR Apache-2.0) — but it debounces **pins**, not requests: its whole API is over `Wait` + `InputPin`, there is no way to feed it a write intent, and it is 3 stars / 14 commits with no description and no releases. `tokio-debouncer` and the two `notify-debouncer-*` crates are tokio/`std` and filesystem-specific. What was actually needed is two timestamps and one `min` — `Catalog::due_at` — plus an `embassy_time::Timer::at` arm in a `select` the state task already runs. It is pure, so the schedule is host-tested without a clock, which no crate here would have given us. |
| Per-shade command rate limit (`somfy_tasks::CommandLimiter`) | Swept the `rate limit` and `ratelimit` keywords on crates.io (6,726 matches; the top 30 by downloads inspected). Serious candidates: [`governor`](https://crates.io/crates/governor) 0.10.4, [`gcra`](https://crates.io/crates/gcra) 0.6.0, [`ratelimit`](https://crates.io/crates/ratelimit) 2.0.0, [`leaky-bucket`](https://crates.io/crates/leaky-bucket) 1.1.2 | **Own — about forty lines, and both serious candidates fail on the one thing this workspace cannot give them.** `governor` is by a wide margin the most adopted rate limiter in the language (71,024,657 downloads, 13,903,719 recent, MIT, 0.10.4, and it *advertises* `no_std` — the keyword is in its own crates.io metadata), so it was the default assumption. **But its `no_std` feature means `no_std` + `alloc`**: the feature's own definition is "uses hashbrown's alloc support", and hashbrown is there because a *keyed* limiter — which is exactly what "per shade" means — is a hash map of states. Every crate in this workspace is allocation-free, proved on every CI run by building for `thumbv7em-none-eabihf`, a target with no allocator at all; taking `governor` would mean either an allocator on the command path or its unkeyed limiter, which is one bucket for the whole device and therefore not the thing being asked for. Its `std` feature additionally pulls `parking_lot`, `futures-timer` and `quanta` — a clock source, where the state task already holds the only clock that matters and passes it in as `now_ms`. `gcra` 0.6.0 is the small, focused GCRA implementation this ended up using the algorithm of, and it is **`std`-only**: no `#![no_std]` anywhere in its source, `thiserror` as a non-optional dependency, and its `RateLimiter` is `dashmap` + `rustc-hash` — verified against the real `Cargo.toml` and `lib.rs`, not the description. It is also 1,995,560 downloads against a repository last updated **2024-08-01**. `ratelimit` 2.0.0 is a lock-free token bucket over `std::time` and atomics; `leaky-bucket` 1.1.2 is an async tokio limiter. **What was actually needed** is a fixed `[u32; MAX_SHADES]` indexed by a registry slot, advanced by a timestamp the caller already holds — no clock, no map, no allocator, and no locking, because the state task is the single owner. The algorithm itself is not the work and is not invented here: it is GCRA, the same one `governor` and `gcra` implement, which is why the numbers in `somfy_tasks::limit` are the interesting part and the arithmetic is four lines. **What would change this:** an allocator reaching the command path — which Plan 5 deliberately confined to `esp-radio` — at which point `governor`'s keyed limiter is the right answer and this row should be reopened with it. |
| `Origin`/`Host` validation (`somfy_api::origin`, design spec §7.3) | Swept the `csrf` keyword (361 matches; the top 15 by downloads inspected) and the CORS middleware field: [`csrf`](https://crates.io/crates/csrf) 0.5.0, [`actix-csrf`](https://crates.io/crates/actix-csrf) 0.8.0, [`axum_csrf`](https://crates.io/crates/axum_csrf) 0.11.0, [`salvo-csrf`](https://crates.io/crates/salvo-csrf) 0.95.2, and `tower-http`'s `CorsLayer` | **Own — and the whole field solves a different problem, which is the reason rather than the size.** Every CSRF crate here is a **token** implementation: `csrf` 0.5.0 is "CSRF protection primitives" and its primitives are AES-GCM-encrypted cookie/form token pairs (109,915 downloads, 11,117 recent) — a session, a cookie and a hidden field, which is precisely the machinery the owner deferred along with authentication, and which this check exists to do without. The three framework crates are middleware for `actix`, `axum` and `salvo`, all `std` and none of them speaking `picoserve`. **CORS middleware is worse than inapplicable — it is the inverse of what is wanted**: `tower-http`'s `CorsLayer` exists to *grant* a named cross-origin caller access by emitting `Access-Control-Allow-Origin`, and this device deliberately emits no such header at all, which is half of why an `Origin`-less cross-origin `GET` is harmless here. Nothing in the field validates `Host` against the device's own identity, which is the DNS-rebinding half and the half a token would not have covered either. What was written is ~90 lines of two rules over two headers, `no_std` without `alloc`, host-tested against the three attacks it is for; the parsing is the small half and the rules are the content. |
| Web UI framework + build (Plan 7) | Fixed by design spec §8: [`preact`](https://www.npmjs.com/package/preact) 10.29.8 + [`vite`](https://www.npmjs.com/package/vite) 8.2.1 + TypeScript, with [`@preact/preset-vite`](https://www.npmjs.com/package/@preact/preset-vite) 2.10.6 | **Adopt — the spec names the stack, and nothing found argues against it.** All MIT (compatible with our GPL-3.0-only), all current (preact 2026-08-10, vite 2026-08-06, preset-vite 2026-07-18). The preset is the officially supported Preact integration and costs `@babel/core` as a dev-only peer; the alternative is Vite's built-in esbuild JSX (`jsx: 'automatic'`, `jsxImportSource: 'preact'`), which drops babel but also drops component-level HMR — a bad trade with six screens still to build. Neither choice reaches the client bundle. |
| UI router (Plan 7) | [`preact-iso`](https://www.npmjs.com/package/preact-iso) 2.12.2, [`wouter-preact`](https://www.npmjs.com/package/wouter-preact) 3.10.0 | **Adopt `preact-iso` 2.12.2.** Both are viable and the gap is small. `preact-iso` is the Preact org's own (`preactjs/preact-iso`): 246 stars, 18 forks, 4 open issues, last push 2026-08-11, 29,566 weekly downloads, MIT, 51 KB unpacked, **zero runtime dependencies**. `wouter-preact` is the Preact build of the well-adopted `wouter` (molefrog/wouter: 7,865 stars, 189 forks, 30 open issues, last push 2026-08-10) at 25,339 weekly downloads — more stars, marginally fewer downloads — but it is licensed **Unlicense**, which is a public-domain dedication with a disclaimer rather than a licence grant and is [rejected outright by some organisations' policies](https://opensource.google/documentation/reference/patching), and it pulls two runtime deps (`mitt`, `regexparam`). The decider is first-party maintenance: the router will follow Preact's own signals/compat changes, and the one that lives in the Preact org follows them first. **One real cost, handled:** `preact-iso` peer-depends on `preact-render-to-string` for its `prerender` export, which this app has no use for — so it is a devDependency and the app imports `preact-iso/router`, a separate entry in the package's `exports` map, keeping the SSR half out of the bundle by construction rather than by trusting tree-shaking. |
| UI i18n (Plan 7) | [`i18next`](https://www.npmjs.com/package/i18next) 26.3.6, [`typesafe-i18n`](https://www.npmjs.com/package/typesafe-i18n) 5.27.1, [`preact-i18n`](https://www.npmjs.com/package/preact-i18n) 1.5.0 | **Own — ~90 lines in `ui/src/i18n/`, and the size argument here is measured rather than asserted.** `i18next` is overwhelmingly the most adopted option (14,034,773 weekly downloads, MIT, last published 2026-07-09) and was the default assumption. Bundled and minified **with our own toolchain** (`bun build --minify --target=browser` over a probe that initialises it with two bundled catalogues and calls `t()` with an interpolation), i18next core alone is 44,284 bytes raw / **13,691 bytes gzipped**. That is larger than the *entire current application bundle* (11.9 KiB gzipped) and 6.7% of design spec §8's 200 KB budget, spent on a feature set — async backend loading, namespaces, context, nesting, plural rules — that no string in `en.ts` uses. **And the headline feature is one we are forbidden to use:** the app ships inside the firmware image via `include_bytes!` and is served from flash with no filesystem, so runtime resource fetching is off the table and both catalogues are bundled regardless. `typesafe-i18n` 5.27.1 (MIT, 45,861 weekly, last published 2026-02-11) has a genuinely tiny runtime and better types, but it is a code generator with a file watcher — a second build step and a generated-artifact drift problem, in a repo that already has one generated-artifact gate to keep green. `preact-i18n` 1.5.0 is **unmaintained** (BSD-3-Clause, last published 2022-06-24, 3,651 weekly). **What we get instead of what we gave up:** `fr.ts` is typed `Record<MessageKey, string>`, so an untranslated key is a build failure rather than a silent fallback to English — stronger than i18next's runtime `fallbackLng`. **What would change this:** the first message needing a real plural rule; reach for `Intl.PluralRules` (already in every browser) before a library, and reopen this row if the catalogues outgrow flat strings. |
| Mock-server WebSocket (Plan 7) | [`ws`](https://www.npmjs.com/package/ws) 8.21.3 | **Adopt.** MIT, last published 2026-08-07, and the de-facto Node WebSocket server — Vite itself depends on it for HMR. Used with `noServer: true` so the mock claims only `/api/v1/events` and leaves every other upgrade to Vite's own handler. **devDependency only**: it runs in the dev server, never in the browser bundle, so it costs nothing against the 200 KB budget. |
| Add-shade form state + validation (Plan 7, shade lifecycle) | [`react-hook-form`](https://www.npmjs.com/package/react-hook-form) 7.85.0, [`formik`](https://www.npmjs.com/package/formik) 2.4.9, [`final-form`](https://www.npmjs.com/package/final-form) 5.0.1 / [`react-final-form`](https://www.npmjs.com/package/react-final-form) 7.0.1, [`@felte/preact`](https://www.npmjs.com/package/@felte/preact) 1.2.14, [`@modular-forms/preact`](https://www.npmjs.com/package/@modular-forms/preact) 0.12.1, [`@tanstack/preact-form`](https://www.npmjs.com/package/@tanstack/preact-form), plus the schema libraries [`zod`](https://www.npmjs.com/package/zod) 4.4.3 and [`valibot`](https://www.npmjs.com/package/valibot) 1.4.2 | **Own — `useState` in `routes/shade-new.tsx`, and the deciding argument is fit rather than size.** `react-hook-form` is overwhelmingly the most adopted (50,473,107 weekly, 44,821 stars, 404 contributors, 6 open issues, last commit 2026-08-16, MIT, zero runtime deps) and was the default assumption; its published bundle uses only hooks `preact/compat` provides, so it *would* work. But **what these libraries are good at is not what this form needs.** Their value is uncontrolled-field registration and re-render minimisation across large dynamic forms with field arrays; this form is six fields on one screen — a text input, two `<select>`s, three numbers — where that machinery is inert. Cost is real: 13.5 KiB gzipped plus `preact/compat` at 4,123 B, so 17.6 KiB, which is **above the 13,691 B at which this table already rejected i18next** for being "larger than the entire current application bundle". Same disposal for `formik` (13.9 KiB, *eight* transitive deps, last publish 2025-11-10, 838 open issues) and `final-form` (needs `@babel/runtime`; `react-final-form` has 377 open issues). **A schema library is worse than inert, and this is the real ruling:** validation is authoritative in Rust — `CreateShadeDto::to_config` — and returns a typed `ApiErrorCode` the UI translates. A `zod` or `valibot` schema would be **a second copy of those rules in another language, free to drift**, which is the failure the `somfy-config` and `postcard` rows already reasoned about. The client-side check that exists is deliberately not a schema: it disables the submit button, and the device's answer is what gets rendered. (`zod` would also cost 60.3 KiB — four times the whole app; `valibot` at 14.2 KiB is the far better choice of the two and is the row to reopen if client-side schema validation is ever genuinely wanted.) **The Preact-native field is effectively empty** and worth recording so it is not re-searched: every Preact-native form package on npm combined is under 8,000 weekly downloads — `@modular-forms/preact` 4,538 (last publish 2025-03-01), `@tanstack/preact-form` 2,449, `@felte/preact` **120 and dead since 2024-11**, the rest in the low hundreds — against react-hook-form's 50M. |
| Pairing-assistant stepper (Plan 7, shade lifecycle) | [`@stepperize/react`](https://www.npmjs.com/package/@stepperize/react) 7.0.0, [`react-step-wizard`](https://www.npmjs.com/package/react-step-wizard) 5.3.11, [`@headlessui/react`](https://www.npmjs.com/package/@headlessui/react) 2.2.10 | **Own — a five-variant discriminated union in `routes/shade-pair.tsx`, and two of the three candidates fail on their own terms before the fit question arises.** `@stepperize/react` is the genuinely good one (102,246 weekly, 1,593 stars, 4 open issues, last publish 2026-06-05, 3.5 KiB) and is **eliminated on licence**: GitHub's licence API returns 404 for `damianricobelli/stepperize`, there is no LICENSE file in the repo root, in `packages/react/`, or in the published 7.0.0 tarball — only the `package.json` field claims MIT. That is the identical pattern this table used to eliminate `mcutie` ("no LICENSE file in the repo or the published crate"), and it matters more here because our licence is GPL-3.0-only. `react-step-wizard` is **unmaintained**: last publish 2021-12-15, last commit 2024-07-19, 17 open PRs. `@headlessui/react` **has no stepper at all** — its component list is button/checkbox/combobox/dialog/disclosure/…/tabs, and `Tabs` is free navigation between panels with no order, no next/prev and no gating — for 60.7 KiB. **There is no Preact stepper on npm**: `keywords:preact,stepper` returns zero packages. And the fit argument would have ruled anyway: this flow is four states with one retry edge, and its hard part is not sequencing but *what each screen is allowed to claim* — the send step must never render a success it cannot observe. A generic stepper does not help with that and would obscure it. **Re-checked when the flow grew:** merging "add a shade" into it added a step and two exits (discard an unfinished shade, confirm one that already works), so it is now six states with two edges out — and the ruling did not move. The licence and maintenance eliminations are unchanged, and the fit argument got *stronger*: the new step is the one that decides whether the shade is announced at all, and what a stepper would abstract away is exactly the question of which screen may claim what. |
| HTTP server + WebSocket (Plan 7, firmware) | [`picoserve`](https://crates.io/crates/picoserve) 0.19.0, [`edge-http`](https://crates.io/crates/edge-http) 0.8.0 + [`edge-ws`](https://crates.io/crates/edge-ws) 0.8.0, [`nanofish`](https://crates.io/crates/nanofish) 0.12.1, [`nanooctopus`](https://crates.io/crates/nanooctopus) 0.2.0, [`hotaru`](https://crates.io/crates/hotaru) 0.8.5, [`peaweb`](https://crates.io/crates/peaweb) 0.7.1, [`esp-wifi-manager`](https://crates.io/crates/esp-wifi-manager) 0.3.2, [`wifi-caddy`](https://crates.io/crates/wifi-caddy) / [`esp-wifi-caddy`](https://crates.io/crates/esp-wifi-caddy) 0.8.0, [`wtx`](https://crates.io/crates/wtx), [`embedded-svc`](https://crates.io/crates/embedded-svc) 0.29, [`esp-idf-svc`](https://crates.io/crates/esp-idf-svc) 0.52.1, [`embedded-websocket`](https://crates.io/crates/embedded-websocket) 0.9.5 | **Adopt `picoserve` 0.19.0. Runner-up `edge-http` 0.8.0 + `edge-ws` 0.8.0.** Both were **proved by building a throwaway spike** for `xtensa-esp32s3-none-elf` *and* `riscv32imc-unknown-none-elf` under `build-std = ["core"]` — core only, so `alloc` was not even linkable — exercising a gzipped static file with custom headers, JSON bodies in and out, `201`/`202`/`204`/`404`/`409` with a `Location` header, path parameters, and a WebSocket upgrade with a caller-owned frame buffer. Resolution was checked against this crate's real dependency set (204 packages, no conflict, `esp-hal` stays at 1.1.2). The decider is **`unsafe` and adoption**, the same axis this table used to choose `minimq` over `rust-mqtt`. `picoserve` carries `#![deny(unsafe_code)]` with exactly **one** `unsafe` in non-test code (`response/ws.rs:836`, a documented `from_utf8_unchecked` over base64 output); `edge-http` has about ten in its connection pool (`MaybeUninit::assume_init_mut`, `map_unchecked_mut`, raw-pointer `as_mut`). **Maintenance:** `picoserve` 116,321 downloads (15,010 recent), 392 stars, 53 forks, 19 contributors, 231 commits, last commit 2026-08-07, 2 open issues / 1 open PR, MIT, and **113 GitHub dependent repos — including `esp-rs/esp-hal` itself (2,057 stars) and `ariel-os/ariel-os` (1,206)**, which is adoption on this exact stack. `edge-net` has 227 stars but 9 dependent repos, three of them the author's own. **What it gave us for free and would otherwise have been written:** a compile-time SHA-1 `ETag` on every embedded asset with `304` handling; TCP keep-alive at 30 s and a socket timeout at 45 s on every accepted connection, which is what reaps a WebSocket held by a laptop that went to sleep; `KeepAlive::Close` as the default, which is what makes a REST task unholdable. **Costs, and they are real.** (1) `Json<T>`'s `IntoResponse` hardcodes `Response::ok`, `JsonBody` is private and `Response`'s fields are `pub(crate)`, so `201` and `200`-after-`PATCH` need our own ~20-line `Content` impl. (2) The router is a type per route wrapping the previous one, so trait resolution recurses to the bottom and needs `#![recursion_limit = "512"]` — the failure without it is `error: queries overflow the depth limit!` with **no span**. (3) It costs **13,096 bytes of DRAM per connection task**, measured with `-Zprint-type-sizes`, which is what puts the server out of the ESP32's reach entirely (`crates/firmware/src/heap.rs`). (4) MSRV moved 1.85 → 1.93 in one release, and the Xtensa fork lags upstream — a future bump could block the Xtensa builds while leaving RISC-V green. (5) The author's own README advises against exposing it directly to the internet; this is a LAN device behind no proxy, which is a risk to record rather than one this task closes. **Eliminated on `embedded-io-async` 0.6**, the same trait-version mismatch that eliminated `mountain-mqtt` and `embedded-mqttc`: `hotaru` 0.8.5 (its only embassy-net path pins `0.6`) and `esp-wifi-manager` 0.3.2 (`0.6.1`, plus `embassy-net ^0.7.1`). **Eliminated on `embassy-net` 0.7/0.8:** `peaweb`, `nanofish-client`, `wifi-caddy`, `esp-wifi-caddy`. **Eliminated on capability:** `nanofish` 0.12.1 clears every version bound and is MIT but has **no WebSocket at all**; `nanooctopus` 0.2.0 has one and has 1 star and 91 downloads. **Not applicable:** `embedded-svc` 0.29 is six traits and no listener — there is no transport in it; `esp-idf-svc` is the ESP-IDF C-framework route and is mutually exclusive with our bare-metal `esp-hal`; `httparse` is a parser (picoserve has its own); `embedded-websocket` 0.9.5 is a sans-IO codec on `heapless 0.7` with no `embedded-io-async`, and is unnecessary given picoserve's built-in `ws`. **Licence note worth carrying:** four candidates declare a licence in `Cargo.toml` and ship **no LICENSE file** (GitHub reports null) — `esp-wifi-manager`, `wifi-caddy`, `esp-wifi-caddy`, `provision32` — the same pattern that eliminated `mcutie` and `@stepperize/react`. `wtx` is MPL-2.0, which *is* GPL-3.0-compatible, and was eliminated for having no HTTP/1.1 server. |
| `no_std` JSON codec for the REST surface (Plan 7, firmware) | [`serde-json-core`](https://crates.io/crates/serde-json-core) 0.6.0 | **Adopt, and it is close to forced.** It is what `picoserve`'s own `json` feature depends on, so pinning the same major means one copy rather than two `Serialize` traits with the DTOs implementing one of them. It is also the only `no_std` serde JSON codec with real adoption: 4,129,193 downloads (486,496 recent), 198 stars, 74 forks, 17 open issues, last push 2025-11-18, MIT OR Apache-2.0, `no_std` without `alloc` through `to_slice`. The reason it matters beyond convenience is **drift**: `somfy-api`'s DTOs already derive `Serialize`/`Deserialize` and are the source `ui/src/api/generated/` is written from, so serialising through the derive makes the bytes this device sends and the TypeScript the UI compiles against two views of one definition. A hand-written writer — which `somfy-mqtt` legitimately uses for its payloads, where the shapes are Home Assistant's rather than ours — would be a second copy of the wire format, and the CI gate that catches DTO drift would not see it. The manual `Deserialize` impls in `somfy-api` were written for exactly this consumer: they avoid serde's `Content` buffer so the firmware stays allocator-free. |
| Compressing the web UI into the image (Plan 7, `build.rs`) | [`flate2`](https://crates.io/crates/flate2) 1.1.9, and the alternative of gzipping in the UI's own build | **Adopt `flate2`, as a build dependency.** 620,040,372 downloads (137,601,615 recent), MIT OR Apache-2.0, last release 2026-02-03, and it lives under `rust-lang/`. `default-features = false` with `rust_backend` keeps it off `libz`/`zlib-ng`, so this crate needs no C toolchain and no system library — which matters because three chip targets already bring their own. None of it reaches the image: the device never inflates anything, it serves bytes the build produced. The alternative was emitting `.gz` from `bun run build` — `ui/scripts/size.ts` already gzips at level 9 to measure the budget — and it was rejected because it puts the compression level, which is a *flash* decision, in the UI's build rather than in the firmware's. It would also add a second artefact kind for `build.rs` to depend on without removing the dependency on `dist/` itself. |
| Main-stack high-water measurement (`crate::stack_used`) | [`esp-hal`](https://crates.io/crates/esp-hal) 1.1.2's stack guard, [`esp-rtos`](https://crates.io/crates/esp-rtos) 0.3.0's three stack-overflow options, [`embassy-executor`](https://crates.io/crates/embassy-executor) 0.10 | **Own — about forty lines, and the field is empty rather than unsuitable.** Searched the stack we already depend on before writing anything, because the reuse rule applies hardest to the boot path. What exists is *detection*, not measurement: esp-hal writes `__stack_chk_guard` 60 bytes above the stack's end and puts a hardware data watchpoint on it (`ESP_HAL_CONFIG_STACK_GUARD_OFFSET`/`_VALUE`/`_MONITORING`), and esp-rtos adds `stack-guard-monitoring`, `stack-pointer-range-check` and a software guard. All four answer "has it already overflowed?"; none answers "how close is it?", which is the question whose absence cost a boot loop — the guard fired correctly and told us nothing until it was too late. Grepping esp-hal, esp-rtos and embassy-executor for `watermark`/`high_water`/`stack_usage` returns nothing outside an I2C FIFO. `embassy-executor` is stackless by design and has no per-task stack to mark. **What we wrote instead is deliberately small and reuses what is there**: it takes both bounds from esp-hal's own linker symbols and treats `__stack_chk_guard` as a hazard to step around rather than a number to duplicate, so a change to esp-hal's guard offset moves our floor with it. **What would change this:** an `esp-rtos` release exposing a per-task watermark — its task struct already owns the stack slice, so it is the natural home — at which point this becomes a call into it.
| mDNS responder (Plan 6 Task 7) | Swept every crate carrying the `mdns` or `dns-sd` keyword (72 + 24 hits) rather than a remembered shortlist. Responders: [`edge-mdns`](https://crates.io/crates/edge-mdns) 0.8.0, [`hick-embassy`](https://crates.io/crates/hick-embassy) 0.2.0, [`mdns-sd`](https://crates.io/crates/mdns-sd) 0.21.0, [`libmdns`](https://crates.io/crates/libmdns) 0.10.1, [`simple-mdns`](https://crates.io/crates/simple-mdns) 0.7.0, [`searchlight`](https://crates.io/crates/searchlight) 0.3.2, [`zeroconf`](https://crates.io/crates/zeroconf) 0.18.0, [`astro-dnssd`](https://crates.io/crates/astro-dnssd) 0.3.6, [`hightower-mdns`](https://crates.io/crates/hightower-mdns) 0.1.3, [`opslag`](https://crates.io/crates/opslag) 0.2.3. Codecs: [`mdns-proto`](https://crates.io/crates/mdns-proto) 0.3.0, [`hickory-proto`](https://crates.io/crates/hickory-proto) 0.26.1, [`simple-dns`](https://crates.io/crates/simple-dns) 0.12.0, [`dnsparse`](https://crates.io/crates/dnsparse) 0.3.0. Clients: [`esp-hal-mdns`](https://crates.io/crates/esp-hal-mdns) 0.1.4, [`mdns`](https://crates.io/crates/mdns) 3.0.0, [`uwuhi`](https://crates.io/crates/uwuhi) 0.4.3 | **Adopt `edge-mdns` 0.8.0 over `edge-nal-embassy` 0.9.0, and it is close to the only option.** It is the **only `no_std`, no-alloc mDNS responder on crates.io** — its own category list says `no-std::no-alloc`, its buffers are `heapless::Vec`, and there is no `extern crate alloc` anywhere in it. **Proved by a throwaway spike** built for `xtensa-esp32s3-none-elf` *and* `riscv32imc-unknown-none-elf` under this crate's real `build-std` and its real dependency set (224 packages, no conflict, `esp-hal` stays at 1.1.2, and neither `heapless` nor `embedded-io-async` gains a version this image did not already carry). The `edge-nal-embassy` 0.9.0 sibling pins **`embassy-net ^0.9` and `embedded-io-async ^0.7`** — an exact match, so the trait-version mismatch that eliminated `mountain-mqtt` and `embedded-mqttc` has no surface here; note that the *previous* release, 0.8.1, pins `embassy-net ^0.8` and would not have worked, so this ruling is version-specific. **`edge-mdns` contains 0 occurrences of `unsafe`.** The decider beyond capability is behavioural, and it is what makes this a comfortable adoption rather than a grudging one: **its broadcast half is not a timer.** It blocks on an `embassy_sync::Signal` the caller owns, so every unsolicited multicast packet this device sends is one `crate::mdns` asked for — exactly what a well-behaved responder on a shared group needs, and what a periodic announcer would have made us fight. It also answers legacy one-shot queries unicast (§6.7) and applies the §6 20-120 ms jitter on the broadcast path. **Maintenance:** `sysgrok/edge-net` (the repo moved from `ivmarkov/`) 227 stars, 43 forks, 448 commits, 13 contributors of whom 12 are not the author, last push 2026-07-31, 3 open issues / 0 open PRs; `edge-mdns` 22,151 downloads (3,034 recent), `edge-nal-embassy` 77,600 (13,379), `edge-nal` 99,438 (17,319); MIT OR Apache-2.0 with both LICENSE files at the repo root. Its codec is `domain` 0.12.2 (NLnet Labs — the Unbound and NSD people), BSD-3-Clause, **11,327,366 downloads**, used with `default-features = false, features = ["heapless"]` so no allocator. **The honest weaknesses, since they are the argument against:** `edge-mdns` itself has only **2 genuinely third-party crates.io dependents** (`enody`, `tosca-esp32c3`; `edge-net` and `rs-matter-stack` are the author's own) and **`rs-matter` is *not* a user** — it has its own mDNS, so "battle-tested by Matter" is not an argument available here. ivmarkov wrote 410 of 448 commits, so the family is effectively one author. And `edge-nal-embassy` carries **22 `unsafe` occurrences** in the `NonNull` socket-buffer pool it uses to hand out sockets — a pool this caller needs exactly one entry of. **Two real traps found by reading rather than by the README, both of which a spike alone would have shipped:** `edge_mdns::io::DEFAULT_SOCKET` is an alias for the **IPv6** wildcard and its own doc comment says "don't use in production code" — binding it on this IPv4-only stack compiles perfectly and fails at run time, so `crate::mdns` uses `IPV4_DEFAULT_SOCKET`; and a response that overruns the send buffer is a `PushError` that propagates out of `Mdns::run` and **ends the responder for good**, which is why `RESPONSE_BYTES` is derived with 3x margin and the task restarts around it. **Eliminated, each with its cause:** `hick-embassy` 0.2.0 is the closest miss and the one to revisit — it speaks `embassy-net` 0.9 natively with no `edge-nal` layer and pins `embassy-sync` 0.8 / `embassy-time` 0.5 / `rand_core` 0.10 exactly — but it **requires `alloc` on the mDNS path** (its own description says "`no_std` + `alloc`"), and `al8n/hick` is 37 stars, 124 commits, 4 contributors, **39 open issues / 3 open PRs**, first released 2026-06-05, **0 third-party dependents**. `opslag` 0.2.3 is genuinely `no_std` on `heapless 0.8` and MIT/Apache-2.0, but it is **sans-I/O with no announce loop**, and it is 3 stars, 17 commits, one contributor, **27 downloads in 90 days**. `esp-hal-mdns` 0.1.4 is the only ESP-named candidate and is a **query client, not a responder** (`MdnsQuery::new`, `parse_mdns_query`); it also declares MIT in Cargo.toml and **ships no LICENSE file**, which this table has twice treated as disqualifying. `mdns-proto`, `hickory-proto`, `simple-dns` and `dnsparse` are packet codecs with no responder — the hand-rolling this rule exists to prevent — and `hickory-proto` needs a heap unconditionally. `mdns-sd` 0.21.0 has by far the best adoption in the field (4,217,348 downloads, 219 stars) and is **`std`-only**: `mio`, `socket2`, `if-addrs`, `flume`. `libmdns`, `simple-mdns`, `searchlight`, `hightower-mdns`, `mdns`, `uwuhi` and the rest are `std`/tokio, FFI to Bonjour/Avahi, or dead. **Nothing in the esp-rs stack covers this**: `esp-radio` stops at the Wi-Fi driver, and ESP-IDF's `mdns` component is reachable only through `esp-idf-svc`, which is the `std` branch and mutually exclusive with our bare-metal `esp-hal`. |
| SNTP client (Plan 6 Task 7) | Swept the `ntp` and `sntp` keywords. Clients: [`sntpc`](https://crates.io/crates/sntpc) 0.11.0 with [`sntpc-net-embassy`](https://crates.io/crates/sntpc-net-embassy) 0.11.0 and [`sntpc-time-embassy`](https://crates.io/crates/sntpc-time-embassy) 0.6.0, [`rsntp`](https://crates.io/crates/rsntp) 4.1.2, [`ntp-proto`](https://crates.io/crates/ntp-proto) 1.9.0 (ntpd-rs), [`ntp_usg-proto`](https://crates.io/crates/ntp_usg-proto) 5.0.0, [`ntp`](https://crates.io/crates/ntp) 0.5.0, [`sntp`](https://crates.io/crates/sntp) 0.1.2, [`smolapps`](https://crates.io/crates/smolapps) 0.1.4, [`w5500-sntp`](https://crates.io/crates/w5500-sntp) 0.5.0, [`rtime-ntp`](https://crates.io/crates/rtime-ntp) 0.14.2, plus `embassy-net`, `embassy-time` and `esp-idf-svc` | **Adopt `sntpc` 0.11.0 + `sntpc-net-embassy` 0.11.0. Reject its third sibling `sntpc-time-embassy` 0.6.0, and that rejection is the interesting half.** `sntpc` is `no_std` by default (`default = []`), has **zero `alloc` and zero `unsafe`** in its source — verified against the published crate, not the description — and its only non-optional dependency is `cfg-if`. Its 48-byte packet lives in a `[u8; 48]`. It models the protocol properly rather than minimally: Kiss-o'-Death codes (RFC 5905 §7.4), the leap-indicator alarm, root-distance rejection, origin-timestamp identity, and rollover-safe Unix seconds. `sntpc-net-embassy` is **201 lines**, `#![no_std]`, zero `unsafe`, and **wraps `embassy_net::udp::UdpSocket` directly — it depends on neither `embedded-nal-async` nor `embedded-io-async`**, so the trait-version hazard that eliminated two MQTT candidates has no surface on the UDP path at all; it pins `embassy-net = ">=0.9, <0.10"`, which our 0.9.1 satisfies. **Proved by the same spike** as `edge-mdns`, on both instruction sets. **Maintenance:** `vpetrigo/sntpc` 74 stars, 31 forks, 546 commits, 13 contributors of whom 11 are human non-maintainers, last push 2026-07-06, 1 open issue / 0 open PRs, **739,071 downloads (100,579 recent)**, MIT OR Apache-2.0 with both LICENSE files present, 6 third-party crates.io dependents. The adapter is younger and thinner — first released 2026-01-08, 8,842 downloads, 2 dependents — which is the honest weak point, and it is 201 lines we could read end to end. **Why `sntpc-time-embassy` is refused, which is a capability gap rather than taste.** `NtpTimestampGenerator::timestamp_sec` is not only the round-trip stamp; it is the **pivot** `sntpc` reconstructs the NTP era from, choosing whichever of the three eras nearest the pivot best fits the wire value — *silently*, with no error when it picks wrong, which the crate's own `test_reconstruct_timestamp_uses_bad_pivot_wrong_era` asserts. `EmbassyTimestampGenerator` hardcodes uptime, so the pivot sits near the epoch forever, and its field is private with a `Default` impl and **no constructor** — there is no way to hand it a better one. Being precise about the size of that, because overstating it would be its own error: a zero pivot is *correct* until roughly **2106**, since era-0 candidates below the Unix epoch are skipped. So this is not urgent. It is refused because our own 20-line generator uses the device's wall clock as the pivot once there is one and falls back to uptime before that — identical on the first exchange of a boot, exact rather than lucky on every one after — and because the crate is **555 total / 536 recent downloads** for ~30 lines of substance. **Costs this implies, budgeted as real work rather than glue:** `get_time` has **no timeout** and will await a datagram a firewalled port 123 never sends, so `embassy_time::with_timeout` is ours (the crate's own repository example does the same); the adapter's address conversion answers an IPv6 `SocketAddr` with **`unreachable!()`** unless its `ipv6` feature is on, so resolving `A` records only is a guard rather than a preference; and it enables `embassy-net/medium-ip` on our `medium-ethernet` build, which is additive and costs a little smoltcp code. **It also forced a decision this table should record: there is no sane default NTP server that is not a *name*.** That is what put `embassy-net/dns` in the image, and the resolver is now one call from making `somfy_config::MqttSettings`'s broker a hostname too. **Eliminated, each with its cause:** `rsntp` 4.1.2 is by far the most adopted (**3,962,691 downloads**) and is **`std`-only** — its default features are `async` + `chrono`, and `async` is tokio. `ntp-proto` 1.9.0 is ntpd-rs's daemon core: `std`, 19,143 LoC, rustls and aws-lc-rs by default. `ntp_usg-proto` 5.0.0 is the only other genuinely `no_std`-without-alloc candidate and is **codec-only** — we would still hand-roll the socket I/O, the client state machine, response validation and KoD handling, which is precisely what this rule exists to prevent — with **1 star, 0 forks, 8 open issues, zero third-party dependents** and three majors in four months. `ntp` 0.5.0 is unmaintained since **2018**; `sntp` 0.1.2 and `smolapps` 0.1.4 are `no_std` over **raw smoltcp sockets** rather than `embassy-net` and both died in 2020; `w5500-sntp` targets a Wiznet SPI Ethernet chip; `rtime-ntp` is 91 downloads old. **Embassy ships nothing**: there is no `embassy-net-sntp` (the `embassy-net-*` companions are all link-layer drivers), and `embassy-time`'s own docs say it has "no direct support for wall-clock time" and tell you to keep the epoch offset yourself — which is exactly what `crate::sntp`'s `Anchor` does. `esp-idf-svc::sntp` needs the ESP-IDF framework and `std`. |
| `rand_core` 0.10 adapter for `esp-hal`'s RNG (Plan 6 Task 7) | [`rand_core`](https://crates.io/crates/rand_core) 0.10.1 itself, and the ESP RNG adapters that exist | **Own — four lines in `crate::mdns`, and the field is empty for the version we need.** `edge-mdns` bounds its jitter source on `rand_core::Rng` at **0.10**, whose trait shape changed: `Rng` is now a blanket impl over a fallible `TryRng`, so an implementation is three methods returning `Result<_, Infallible>`. Nothing in the esp-rs stack implements it — `esp-hal` 1.1.2's `Rng` has an inherent `random()` and no `rand_core` impl at any version — and the community adapters target 0.6 or 0.9, which are different traits rather than compatible ones. This is the one row where "the wrapping would be larger than the thing" needs no argument: the wrapper is `Ok(self.0.random())` three times. |

| Position estimation with dead-time and dead-band compensation, and the guided calibration that measures them (position-accuracy spec R2/R5/R8) | The estimator field generally: Kalman/state-observer crates (`adskalman`, `kalmanfilt` and the rest of the `kalman` keyword), and the `no_std` numeric helpers around them (`micromath`) | **Own — eliminated on capability, and the elimination is structural enough that adoption never entered into it.** Every estimator crate of this kind is a **filter**: it fuses a prediction with a *measurement*, and the fusion is the entire product. They all want an observation model and a measurement-noise term. **There is no measurement to give them.** RTS is one-way — no encoder, no limit switch, no current sense, nothing comes back from the motor — so a Kalman filter here degenerates to its prediction step, which is `pos = start + elapsed / travel_time`: the twenty lines already in `motion.rs`, wrapped in a matrix library and a dependency. **What was written is not an estimator at all.** It is a piecewise-linear *travel model* whose two extra phases are facts about this hardware — a command's air time plus a motor's soft-start, and a perforated shutter's slats separating before the curtain rises — plus a conversation that turns an operator's taps into those two figures. Nothing on crates.io models a shutter. **Adoption numbers were deliberately not gathered**, because no level of adoption makes a filter usable without observations, and quoting figures that did not decide anything would be decoration. **What would change this:** any sensor at all on a shade — a reed switch at the top, a current-sense on the motor — at which point there *is* an observation, and this row must be reopened properly, with the numbers, because then the crates are the right answer rather than a category error. |
| Multi-step movement sequencing (the vent command's three timed legs, and R3's route-via-a-limit) | The `no_std` state-machine field: `statig`, `smlang`, `sm`, `finny` | **Own — about sixty lines, eliminated on fit rather than on quality, and the fit argument is unusually concrete here.** Any of these would express "Down, wait, Up, wait, stop" perfectly well; that part is a four-arm `match` in `Shade::advance`. Two things decided against reaching for one. **(1) The hard part is not the sequencing.** It is what each wait is *for* — that the Down leg waits a whole `down_time_ms` from the moment the command arrives rather than from wherever the estimate says the shade is, because the estimate is precisely what this command exists not to depend on. A state-machine library has nothing to say about that, and wrapping it would hide the one line that carries the argument. **(2) The binding constraint is memory placement, not control flow.** This state had to move *out* of the per-shade struct: a byte on a `Shade` is about 170 bytes of the deepest boot-stack chain, so the first implementation — a vent deadline and a calibration run per shade — grew that chain by 13,936 bytes against ~1,720 of headroom. What fits is a four-entry side table on the `Controller` (`somfy_domain::MAX_ACTIVITIES`), and a generic state machine *stored per shade* is exactly the shape that does not. **Adoption numbers not gathered, for the same reason as the row above** — neither elimination turns on them. **What would change this:** tilt landing, which really is a mode-dependent state machine (per-`TiltMode` press-length semantics), and which should reopen this row with the numbers. |

| Wi-Fi credential trial + revert (`somfy_config::WifiTrial`, Plan 7) | Nothing on crates.io does this; the field searched was "network-configuration rollback with a dead-man timer", and the comparable prior art is `netplan try`, `ip route ... change` with a timer, and Cisco `reload in`. | **Own — about 120 lines of decision, and it is the decision that is the work.** No crate offers it because the *policy* is the whole thing: what counts as proof that a credential works. Every ready-made answer is "did the station associate?", which is why the C++ reference has no rollback at all and why one would not have helped — an operator on network A who moves the device to network B gets a clean association and is still locked out. The mechanism this needs is a confirmation *from the far side*, and that has no library shape: it is two deadlines, a link-state input and four endings. What was reused instead is everything around it — the credential type and its rules from `somfy-config`, the untouched flash record as the revert, `esp_hal::system::software_reset` as the way back. |
| Captive-portal onboarding (Plan 7, **not built**) | [`edge-dhcp`](https://crates.io/crates/edge-dhcp) 0.8.0 and [`edge-captive`](https://crates.io/crates/edge-captive) 0.8.0 (a DHCP server and a captive-portal DNS responder), against `esp-radio` 0.18's `Config::AccessPoint` / `Config::AccessPointStation` | **Would adopt both — and the portal is deferred anyway, for a reason that is not about the crates.** They are the right dependencies: same `edge-net` release train as the `edge-mdns` 0.8.0 this firmware already ships, so the same `edge-nal` 0.7 and `edge-nal-embassy` 0.9 are already in the graph; MIT OR Apache-2.0, compatible with GPL-3.0-only; 227 stars, 43 forks, 13 contributors, last push 2026-07-31, 3 open issues; `edge-dhcp` 80,949 downloads (11,065 recent) across 52 distinct third-party repositories, `edge-captive` 11,582 (703). `esp-radio` genuinely supports AP mode — `Interfaces::access_point` is a real `Interface`, and `Config::AccessPoint` needs no second `embassy-net` stack because a board with no credentials is not running a station at all. **What stops it is that it cannot be tested.** Every part of a captive portal that matters — the beacon, a phone taking a DHCP lease, the OS's own probe following the DNS hijack — is hardware-and-client behaviour, and the one board that exists must not be flashed by an agent. A first-boot AP that compiles and does not work is a brick from the operator's side: no serial, no network, nothing. The heap is the second reason and the smaller one: AP mode adds a second netif and RX queue inside a closed blob, unmeasurable from here, on a C3 whose heap this plan already took from 65 KiB to 55 KiB. **Note also that the C++ reference does not have a captive portal** — it opens an *open* SoftAP with no DNS hijack, no idle timeout and no probe paths (`src/Network.cpp:516`), so there is nothing to port and the brief's premise was wrong. **What would change this:** a hardware session, or a second board. |
| Over-the-air updates (Plan 6 Tasks 4 and 5) | [`esp-hal-ota`](https://crates.io/crates/esp-hal-ota) 0.6.0, [`esp-ota`](https://crates.io/crates/esp-ota) 0.2.2, [`esp-ota-nostd`](https://crates.io/crates/esp-ota-nostd) 0.1.0, [`esp-idf-svc`](https://crates.io/crates/esp-idf-svc) 0.52.1's OTA, and the crate this firmware **already** depends on, [`esp-bootloader-esp-idf`](https://crates.io/crates/esp-bootloader-esp-idf) 0.5.0 | **Adopt `esp-bootloader-esp-idf`'s `ota::Ota` — which is already a non-optional dependency — and decline `esp-hal-ota`. Own the image verification and the self-test, because nothing in the field has either.** `esp-hal-ota` is the serious candidate and is a genuinely good crate: `no_std`, MIT with a LICENSE file present, 25,397 downloads (4,993 recent), last push 2026-08-09, 33 stars, 10 forks, 0 open issues, 5 contributors of whom 4 are not the author. **It resolves cleanly against this exact graph** — verified against the real lockfile rather than assumed: its PAC pins (`esp32` 0.40.2, `esp32s3` 0.35.2, `esp32c3` 0.32.2) and `esp-rom-sys` are the versions `esp-hal` 1.1.2 already pulls in, so there would be no second copy of anything. It also **solves the first-update trap by construction**: `set_target_ota_boot_partition` walks the sequence number upward until it maps to the wanted slot and is non-zero, with a `saturating_sub` in `seq_to_part`, which is exactly the defect `crates/firmware/partitions.csv` documents in the crate we do use. That is the strongest argument for it and the reason this is a ruling rather than an elimination. **What decided against it, in order.** (1) **`Ota<S>` owns its flash.** `S: ReadStorage + Storage`, held for the life of the session because `FlashProgress` lives inside it — and this firmware has exactly one `FlashStorage`, owned by the state task, because `esp_storage::FlashStorage::new` takes the `FLASH` peripheral and panics on a second call. Adopting it means either handing that singleton over for the length of an upload — which would make a rolling-code commit, and therefore every shade command, fail for ~30 s — or reconstructing `Ota` per chunk through `ota_resume`, which **re-reads and re-CRCs the whole written prefix each time**: O(n²), about 130 MB of flash reads for one image. (2) **It writes the record that decides which image boots with its `Result`s discarded** — `_ = flash.write(...)` three times in `set_target_ota_boot_partition`, in three separate calls, where `esp_bootloader_esp_idf` writes one 32-byte entry and returns a `Result`. (3) **Its CRC silently becomes a no-op** without the `esp-rom-sys` feature: `#[cfg(not(feature = "esp-rom-sys"))] fn crc32_le(crc, _buf) -> u32 { crc }`. The chip features do enable it, so this is a landmine rather than a live bug — but a verification that can silently verify nothing is the wrong shape for this. (4) It brings a **second partition-table parser** into an image that already carries esp-rs's, with a weaker one: no label check, a hard requirement that OTA subtypes be contiguous, and a `BUG:` comment in its own source about layouts not starting at `ota0`. **What it would not have given us either way**, and this is the larger half of the work: `ota_begin(size, target_crc)` takes a CRC the *server* supplies and performs **no structural check on the image at all** — an ESP32 build uploaded to an ESP32-S3 is accepted, written and marked bootable — and `ota_mark_app_invalid_rollback` only sets a state byte, which does nothing on a bootloader built without `CONFIG_BOOTLOADER_APP_ROLLBACK_ENABLE` (the ESP-IDF default, and unknowable for the prebuilt `espflash` ships). So the image verifier, the self-test and the partition switch were ours regardless; what we declined is the ~100 lines of sequence arithmetic that `esp-bootloader-esp-idf` already gives us with `Result`s. **Eliminated, each with its cause:** `esp-ota` 0.2.2 and `esp-idf-svc` are the ESP-IDF/`std` route and mutually exclusive with bare-metal `esp-hal`; `esp-ota-nostd` 0.1.0 is 1,199 downloads (18 recent), last published 2024-10-06, and has no repository activity since. **What would change this:** `esp-hal-ota` growing a borrowed-flash constructor and returning its write errors, at which point points (1) and (2) both fall and it becomes the better home for the sequence arithmetic. |
| The ESP-IDF app-image verifier and the update's boot policy (`somfy-ota`) | Searched crates.io for an image-header/`esp_app_desc_t` parser — there is not one. The nearest neighbours are [`esp-idf-part`](https://crates.io/crates/esp-idf-part) 0.6.0 (partition **tables**, which is a different structure and which this workspace already uses in `build.rs`), `esp-hal-ota`'s own reader, and [`esp-p4-mini-bootloader`](https://crates.io/crates/esp-p4-mini-bootloader) 0.1.0 | **Own, and the field is genuinely empty rather than unsuitable.** No crate parses an ESP-IDF *application image* header or its descriptor: `esp-idf-part` stops at the partition table, and `esp-hal-ota` — the one crate that writes app images — never looks inside one. `esp-p4-mini-bootloader` does load segments and check a SHA-256, and is a first-stage bootloader for a chip this project does not build for, 23 downloads, published once. **What is owned is small and testable:** a streaming verifier that holds 112 bytes of the image's start and a handful of counters, plus two decision tables (what to do about the `otadata` state a boot found, and which self-test legs may refuse a release). All of it is `no_std` without `alloc`, host-tested, and pinned against the first 112 bytes of a real device image so that a wrong offset fails a test rather than a board. **A SHA-256 was considered and declined**: `sha2` `no_std` is ~110 bytes of live state, which on the tightest chip is paid four times over in Wi-Fi driver headroom, and the image already carries its own SHA-256 that the bootloader verifies — so a corruption passing the one-byte checksum costs one reboot and a fall back to the running slot, not a device. `docs/provenance.md` records what that leaves open. |


## Consult the C++ reference EARLY — it is a record of solved problems

The reference implementation runs on this same hardware, in this same room,
talking to these same motors. Every value in it and every structural choice it
made has already survived contact with reality. **Check it before deriving
anything from first principles, not after.**

Read it for three things, in this order of value:

1. **Architecture.** *How* did it solve the problem, not just with what number.
   It receives with a GPIO change-interrupt and an IRAM handler, **not** with
   the RMT peripheral — which sidesteps the "wait for the band to go quiet"
   requirement that RMT reception imposes and that cost us a long detour. A
   structural choice like that is worth more than any constant.
2. **What it delegates.** It does not hand-roll its radio configuration; it
   takes an external library's defaults (`SmartRC-CC1101-Driver-Lib`). Where the
   reference reached for a library, that is a strong signal we should too — see
   the reuse rule above.
3. **Values.** Registers, timings, thresholds. Verify them, then record them in
   [`docs/provenance.md`](docs/provenance.md) as reference-derived.

**This cost real time, concretely.** A full session went into deriving CC1101
AGC settings from the datasheet and sweeping them on hardware, while a
field-proven configuration sat one file away in the library the reference
depends on. Worse, that config contradicted the theory being swept — it caps
gain *harder* while raising the magnitude target and widening the OOK decision
boundary. No amount of first-principles derivation was going to land there.

### The restriction is narrow, and it is not "do not read it"

Only **source comments** must avoid naming the reference. Reading it is
required: `docs/provenance.md` rule 1 says "never invent protocol behaviour —
read the reference, verify the value, then record it here, not in a source
comment." `crates/somfy-migrate/**` is the one documented exception to the
comment rule, because that crate's subject matter *is* the C++ backup format.

The one real hazard is context: agents have exhausted their window wandering a
large C++ codebase. **So the orchestrator extracts the relevant excerpt and
hands it to the implementer** — a named file and line range, or the decoded
values — rather than telling an agent either "go read it all" or "do not look".
Telling an implementer not to look, when the answer is in there, is the failure
this rule exists to prevent.

## Verification

- **A transmitter reporting its own success proves nothing.** If the pulse train
  is built wrongly, the firmware's account of what it sent is wrong in the same
  way. Verify against an independent receiver. The same applies in reverse for
  receive.
- **Match the sample size to the effect, not to a ritual.** Repetition exists to
  separate signal from noise, so the question is always *how big is the effect
  next to the noise?*
  - **Comparing two configurations, or claiming a margin**: repeat, ~10 runs.
    The known boot-to-boot heap noise is ~4,216 bytes; an AMPDU change whose
    real effect was ~364 bytes looked like 3,820 because two samples landed at
    opposite ends of that band. A control experiment is not a substitute for a
    sample size.
  - **A single run is enough when the effect dwarfs the noise** — a heap moving
    from 56 KiB to 200 KiB needs no repetition — and for functional checks: does
    it boot, associate, connect, decode.
  - **A null result is the dangerous case.** "Nothing happened" needs repetition
    almost always, because absence is what noise looks like. One 3-frame burst
    decoding nothing was read as a broken RMT path; it was not, and that cost
    hours.
  - When a result lands within a few multiples of the noise, say so rather than
    reporting the single number as though it settled the matter.
- **Match the CI matrix exactly.** Clippy runs on the dev profile and builds run
  on release, so four green release builds do not imply four green clippy runs —
  the ESP32 clippy job was silently red for three tasks that way.
- Unexplained constants are treated as fabricated. Where a value is empirical or
  a table lookup, say so and give the measurement. A fabricated derivation is
  worse than an honest "this is measured".

## Hardware

Two physically identical ESP32-S3 boards exist. **Verify the MAC before every
flash** — see [`docs/hardware-checklist.md`](docs/hardware-checklist.md).
Flashing the wrong one destroys the working device and the reference receiver in
a single action.

Never modify `crates/somfy-rts/tests/fixtures/*.pulses` or
`crates/somfy-migrate/tests/fixtures/*.backup`: real hardware captures and a
real user's private device data.
